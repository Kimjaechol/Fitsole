---
phase: 05-admin-dashboard-order-management
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/app/(admin)/admin/dashboard/layout.tsx
  - src/app/(admin)/admin/dashboard/page.tsx
  - src/app/(admin)/admin/dashboard/orders/page.tsx
  - src/components/admin/admin-sidebar.tsx
  - src/components/admin/order-table.tsx
  - src/components/admin/order-filters.tsx
  - src/components/admin/dashboard-stats.tsx
  - src/app/api/admin/orders/route.ts
autonomous: true
requirements:
  - ADMN-01
  - ORDR-04
must_haves:
  truths:
    - "Admin can see dashboard with order statistics overview"
    - "Admin can view all orders in a filterable, sortable data table"
    - "Admin can filter orders by status, date range, customer, and design line type"
  artifacts:
    - path: "src/app/(admin)/admin/dashboard/layout.tsx"
      provides: "Admin layout with sidebar navigation and role check"
    - path: "src/app/(admin)/admin/dashboard/page.tsx"
      provides: "Dashboard overview with stats cards"
    - path: "src/app/(admin)/admin/dashboard/orders/page.tsx"
      provides: "Order list page with filters"
    - path: "src/components/admin/order-table.tsx"
      provides: "Data table for orders using TanStack Table"
    - path: "src/app/api/admin/orders/route.ts"
      provides: "GET all orders for admin"
      exports: ["GET"]
  key_links:
    - from: "src/app/(admin)/admin/dashboard/orders/page.tsx"
      to: "/api/admin/orders"
      via: "server-side fetch for order list"
      pattern: "fetch.*api/admin/orders"
    - from: "src/components/admin/order-table.tsx"
      to: "src/lib/types/order.ts"
      via: "OrderSummary type for table columns"
      pattern: "import.*OrderSummary"
---

<objective>
Admin dashboard shell with order list and filtering.

Purpose: Admin needs a dedicated dashboard (D-04) separate from Payload CMS to view and manage all orders (D-05, ADMN-01, ORDR-04). This plan creates the admin layout, dashboard overview, and the filterable order table.
Output: Working admin dashboard at /admin/dashboard with order statistics and filtered order list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-dashboard-order-management/05-CONTEXT.md
@src/lib/db/schema.ts
@src/lib/types/order.ts

<interfaces>
From src/lib/types/order.ts (created in Plan 01):
- OrderStatus type
- ORDER_STATUS_LABELS map
- OrderSummary interface
- OrderDetail interface

From src/lib/admin-auth.ts (created in Plan 02):
- requireAdmin(): checks role === "admin"
- isAdmin(userId): boolean check
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin layout, sidebar, dashboard overview, and orders API</name>
  <files>src/app/(admin)/admin/dashboard/layout.tsx, src/components/admin/admin-sidebar.tsx, src/app/(admin)/admin/dashboard/page.tsx, src/components/admin/dashboard-stats.tsx, src/app/api/admin/orders/route.ts</files>
  <read_first>src/lib/admin-auth.ts, src/lib/types/order.ts, src/lib/db/schema.ts, src/app/(main)/layout.tsx</read_first>
  <action>
1. Create `src/app/(admin)/admin/dashboard/layout.tsx` (per D-04):
   - Server component with auth() + requireAdmin() check — redirect to /login if not authenticated, show 403 page if not admin
   - Layout: sidebar (240px fixed left) + main content area
   - Use (admin) route group to avoid inheriting (main) layout with bottom tab bar
   - Import Pretendard font setup from existing layout
   - Include Toaster (sonner) for notifications

2. Create `src/components/admin/admin-sidebar.tsx`:
   - Navigation links:
     - 대시보드 (/admin/dashboard) — LayoutDashboard icon
     - 주문 관리 (/admin/dashboard/orders) — ShoppingBag icon
     - SALTED 세션 (/admin/dashboard/salted) — Activity icon
     - 매장 예약 (/admin/dashboard/reservations) — CalendarDays icon
   - Active link highlighting based on current pathname (usePathname)
   - FitSole logo at top, "관리자" label
   - Collapsible on mobile (hamburger menu)

3. Create `src/app/(admin)/admin/dashboard/page.tsx`:
   - Server component
   - Query DB for stats: total orders count, orders by status, revenue total, today's new orders
   - Render DashboardStats component with 4 stat cards

4. Create `src/components/admin/dashboard-stats.tsx`:
   - 4 stat cards in a grid (2x2 on mobile, 4x1 on desktop):
     - 전체 주문: total count
     - 오늘 신규: today's count
     - 제작 대기: designing + manufacturing count
     - 총 매출: total revenue formatted as KRW
   - Use shadcn Card component
   - Each card shows icon, label, value

5. Create `GET /api/admin/orders` route:
   - requireAdmin() check
   - Accept search params: status, dateFrom, dateTo, search (customer name/email), lineType (general/professional)
   - Query orders with joins: orders -> users (for name/email), orders -> orderItems -> insoleDesigns (for lineType)
   - Filter by params when provided
   - Sort by createdAt desc
   - Return array of admin-enriched order summaries (OrderSummary + customerName, customerEmail, lineType)
  </action>
  <acceptance_criteria>
    - Admin layout renders with sidebar navigation
    - Dashboard page shows 4 stats cards with real data
    - Admin orders API returns all orders with customer info
    - Non-admin users get 403 on dashboard pages
    - Sidebar highlights current active page
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Admin dashboard shell with layout, sidebar, stats overview, and orders API endpoint</done>
</task>

<task type="auto">
  <name>Task 2: Order list page with filterable data table</name>
  <files>src/app/(admin)/admin/dashboard/orders/page.tsx, src/components/admin/order-table.tsx, src/components/admin/order-filters.tsx</files>
  <read_first>src/app/api/admin/orders/route.ts, src/lib/types/order.ts</read_first>
  <action>
1. Install TanStack Table: `npx shadcn@latest add table` for shadcn Table component, then `npm install @tanstack/react-table`

2. Create `src/components/admin/order-filters.tsx` (per D-05):
   - Filter bar with:
     - Status dropdown: 전체, 주문확인, 인솔설계, 제작중, 배송중, 완료, 취소됨
     - Date range: from/to date inputs
     - Search: text input for customer name/email
     - Design line: 전체, 카메라 측정 (Line 1), SALTED 정밀 (Line 2)
   - Filters stored in URL searchParams (same pattern as product catalog per Phase 3 convention)
   - Use shadcn Select, Input components

3. Create `src/components/admin/order-table.tsx` (per D-05):
   - Client component using @tanstack/react-table
   - Columns: 주문번호, 고객명, 상태 (badge), 설계 유형 (Line 1/Line 2 badge), 금액, 주문일
   - Sortable columns: 주문일, 금액
   - Row click navigates to /admin/dashboard/orders/[id]
   - Use shadcn Table component for styling
   - Status badges with same colors as user-facing (blue/purple/orange/sky/green/red)

4. Create `src/app/(admin)/admin/dashboard/orders/page.tsx`:
   - Server component, fetches orders with filter params from searchParams
   - Renders OrderFilters + OrderTable
   - Page title: "주문 관리"
  </action>
  <acceptance_criteria>
    - Order table displays all orders with correct columns
    - Filters update URL and refetch data
    - Status and line type shown as colored badges
    - Columns sortable by date and amount
    - Row click navigates to order detail
    - Korean labels throughout
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Admin can view all orders in a filterable, sortable data table with status/date/customer/lineType filters per D-05, ADMN-01, ORDR-04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /admin/dashboard | Admin-only pages |
| client -> /api/admin/orders | Admin-only data access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Elevation of Privilege | /admin/dashboard layout | mitigate | requireAdmin() in server component, redirect if not admin |
| T-05-07 | Information Disclosure | /api/admin/orders | mitigate | requireAdmin() check — non-admin cannot see all customer orders |
</threat_model>

<verification>
- Navigate to /admin/dashboard as admin user, see stats cards
- Navigate to /admin/dashboard/orders, see filterable order table
- Apply filters (status, date range, search, line type) and verify table updates
- Click an order row to navigate to detail (placeholder for Plan 04)
- Access as non-admin user returns 403
</verification>

<success_criteria>
Admin dashboard with sidebar navigation, stats overview, and fully filterable order list table. Separate from Payload CMS admin per D-04.
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-dashboard-order-management/05-03-SUMMARY.md`
</output>
