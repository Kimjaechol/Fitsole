---
phase: 05-admin-dashboard-order-management
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/app/(admin)/admin/dashboard/orders/[id]/page.tsx
  - src/components/admin/order-detail-view.tsx
  - src/components/admin/scan-data-viewer.tsx
  - src/components/admin/design-spec-viewer.tsx
  - src/components/admin/status-controls.tsx
  - src/lib/email/factory-dispatch.ts
  - src/app/api/admin/orders/[id]/route.ts
  - src/app/api/admin/orders/[id]/dispatch/route.ts
autonomous: true
requirements:
  - ADMN-02
  - ADMN-03
  - ADMN-04
must_haves:
  truths:
    - "Admin can view customer 3D scan data and pressure heatmap for an order"
    - "Admin can view and download insole design specs (STL, parameters, TPU map)"
    - "Admin can update order status and dispatch to factory with design specs"
    - "Factory dispatch sends email with STL download link and parameter sheet"
  artifacts:
    - path: "src/app/(admin)/admin/dashboard/orders/[id]/page.tsx"
      provides: "Admin order detail page"
    - path: "src/components/admin/scan-data-viewer.tsx"
      provides: "3D foot model + measurements + pressure heatmap viewer"
    - path: "src/components/admin/design-spec-viewer.tsx"
      provides: "3D insole preview + design params + STL download"
    - path: "src/components/admin/status-controls.tsx"
      provides: "Status update buttons + factory dispatch trigger"
    - path: "src/lib/email/factory-dispatch.ts"
      provides: "Factory dispatch email with design specs"
    - path: "src/app/api/admin/orders/[id]/dispatch/route.ts"
      provides: "POST endpoint for factory dispatch"
      exports: ["POST"]
  key_links:
    - from: "src/components/admin/scan-data-viewer.tsx"
      to: "src/components/scan/foot-model-3d.tsx"
      via: "reuses existing 3D foot viewer"
      pattern: "import.*FootModel3D"
    - from: "src/components/admin/design-spec-viewer.tsx"
      to: "src/components/insole/insole-preview-3d.tsx"
      via: "reuses existing 3D insole preview"
      pattern: "import.*InsolePreview3D"
    - from: "src/app/api/admin/orders/[id]/dispatch/route.ts"
      to: "src/lib/email/factory-dispatch.ts"
      via: "sends factory email on dispatch"
      pattern: "sendFactoryDispatchEmail"
---

<objective>
Admin order detail page with scan data viewer, design spec viewer, status controls, and factory dispatch.

Purpose: Admin needs to view complete customer data for each order (D-06, D-07, D-08) and dispatch orders to factory (D-09). This is the core operational workflow for the business.
Output: Full admin order detail page with embedded 3D viewers, design spec download, and factory dispatch email.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-dashboard-order-management/05-CONTEXT.md
@src/lib/db/schema.ts
@src/components/scan/foot-model-3d.tsx
@src/components/insole/insole-preview-3d.tsx
@src/components/scan/pressure-heatmap.tsx

<interfaces>
From src/lib/types/order.ts (Plan 01):
- OrderDetail, OrderItemDetail interfaces
- ORDER_STATUS_LABELS map

From src/lib/admin-auth.ts (Plan 02):
- requireAdmin()

From src/app/api/admin/orders/[id]/status/route.ts (Plan 02):
- PATCH for status updates

Reusable components (existing):
- FootModel3D: 3D foot viewer (src/components/scan/foot-model-3d.tsx)
- InsolePreview3D: 3D insole preview (src/components/insole/insole-preview-3d.tsx)
- PressureHeatmap: pressure distribution view (src/components/scan/pressure-heatmap.tsx)
- BeforeAfterReport: SALTED verification report (src/components/insole/before-after-report.tsx)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin order detail page with scan and design viewers</name>
  <files>src/app/(admin)/admin/dashboard/orders/[id]/page.tsx, src/components/admin/order-detail-view.tsx, src/components/admin/scan-data-viewer.tsx, src/components/admin/design-spec-viewer.tsx, src/app/api/admin/orders/[id]/route.ts</files>
  <read_first>src/components/scan/foot-model-3d.tsx, src/components/insole/insole-preview-3d.tsx, src/components/scan/pressure-heatmap.tsx, src/lib/db/schema.ts, src/lib/types/order.ts</read_first>
  <action>
1. Create `GET /api/admin/orders/[id]` route:
   - requireAdmin() check
   - Fetch order with full joins: order -> user (customer info), orderItems -> insoleDesigns (design data), user's footScans -> footMeasurements + pressureDistribution + gaitAnalysis
   - Return enriched order detail: order fields + customer info + scan data + design data + pressure data
   - Return 404 if not found

2. Create `src/components/admin/scan-data-viewer.tsx` (per D-07):
   - Client component (for 3D rendering)
   - Props: scanData (foot measurements, model URL, pressure data)
   - Section 1: Embedded FootModel3D component (reuse from scan module) with dynamic import ssr:false
   - Section 2: Measurement values table: 발 길이 (mm), 볼 넓이 (mm), 아치 높이 (mm), 발등 높이 (mm), 뒤꿈치 넓이 (mm)
   - Section 3: Embedded PressureHeatmap component (reuse from scan module)
   - If no scan data available, show "스캔 데이터 없음" message

3. Create `src/components/admin/design-spec-viewer.tsx` (per D-08):
   - Client component
   - Props: designData (designParams, hardnessMap, stlUrl, lineType)
   - Section 1: Embedded InsolePreview3D (reuse, dynamic import ssr:false)
   - Section 2: Design parameters table: arch height, heel cup depth, shoe size, foot side
   - Section 3: TPU temperature map: zone-by-zone hardness values from hardnessMap (table format with zone name + Shore A value + temperature)
   - Section 4: STL download button — links to stlUrl if available, disabled with "STL 미생성" if not
   - Line type badge: "카메라 측정" (blue) or "SALTED 정밀 측정" (purple)

4. Create `src/components/admin/order-detail-view.tsx` (per D-06):
   - Assembles the full order detail view
   - Props: order (enriched OrderDetail from admin API)
   - Sections using shadcn Card + Tabs:
     - Tab 1: 주문 정보 — order items, shipping info, payment info (same layout as user detail but with customer name/email visible)
     - Tab 2: 스캔 데이터 — ScanDataViewer for the customer's scan
     - Tab 3: 인솔 설계 — DesignSpecViewer for each order item with insole
   - Status badge at top with current status
   - Back link to /admin/dashboard/orders

5. Create `src/app/(admin)/admin/dashboard/orders/[id]/page.tsx`:
   - Server component
   - Fetch order detail from admin API
   - Render OrderDetailView + StatusControls (Task 2)
  </action>
  <acceptance_criteria>
    - Admin can see customer info, order items, shipping, payment on order detail
    - 3D foot model renders in scan data tab (reusing FootModel3D)
    - Measurement values displayed in Korean-labeled table
    - Pressure heatmap renders in scan data tab
    - 3D insole preview renders in design tab (reusing InsolePreview3D)
    - Design parameters and TPU temperature map shown in table
    - STL download button works when URL available
    - Line type badge differentiates camera vs SALTED
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Admin can view complete order detail with customer scan data (3D model, measurements, pressure heatmap) and insole design specs (3D preview, parameters, TPU map, STL download) per D-06, D-07, D-08</done>
</task>

<task type="auto">
  <name>Task 2: Status controls and factory dispatch</name>
  <files>src/components/admin/status-controls.tsx, src/lib/email/factory-dispatch.ts, src/app/api/admin/orders/[id]/dispatch/route.ts</files>
  <read_first>src/app/api/admin/orders/[id]/status/route.ts, src/lib/email/order-confirmation.ts, src/lib/types/order.ts</read_first>
  <action>
1. Create `src/components/admin/status-controls.tsx` (per D-06, D-09):
   - Client component
   - Props: orderId, currentStatus, hasDesignSpecs (boolean)
   - Show current status with ORDER_STATUS_LABELS
   - Status transition buttons based on current status:
     - paid -> "인솔 설계 시작" button (sets to designing)
     - designing -> "제작 의뢰" button (triggers factory dispatch, sets to manufacturing) per D-09
     - manufacturing -> "배송 시작" button with tracking number + carrier input fields (sets to shipping)
     - shipping -> "배송 완료" button (sets to delivered)
   - "제작 의뢰" button calls POST /api/admin/orders/[id]/dispatch instead of PATCH status
   - Other buttons call PATCH /api/admin/orders/[id]/status
   - Show confirmation dialog (shadcn AlertDialog) before status change
   - Use sonner toast for success/error feedback
   - "배송 시작" requires trackingNumber and trackingCarrier fields to be filled

2. Create `src/lib/email/factory-dispatch.ts` (per D-09):
   - `FactoryDispatchParams`: orderNumber, customerName, shippingAddress, items (with sizes), designSpecs (params per item), stlDownloadUrls (array), factoryEmail
   - Build HTML email with FitSole branding:
     - Header: "FitSole 제작 의뢰서"
     - Order number and customer info
     - Items table with sizes
     - Design specification per item: arch height, heel cup depth, Shore A values per zone, foot side
     - STL download links (NOT attachments — files too large per CONTEXT.md specifics)
     - Shipping address for direct factory-to-customer delivery
   - Use FACTORY_EMAIL env var for recipient (default: factory@fitsole.kr)
   - Same Resend pattern with dev console.log fallback

3. Create `POST /api/admin/orders/[id]/dispatch` route (per D-09):
   - requireAdmin() check
   - Fetch order with items, designs, scan data
   - Update order status to "manufacturing"
   - Send factory dispatch email with all design specs
   - Send customer notification email (status -> manufacturing)
   - Return success response
   - Validate order is in "designing" status before allowing dispatch
  </action>
  <acceptance_criteria>
    - Status buttons show correct transitions based on current status
    - Factory dispatch button triggers email with design specs and STL links
    - Shipping status requires tracking number and carrier
    - Confirmation dialog before each status change
    - Toast notifications on success/error
    - Factory email includes all design parameters and STL download links
    - Dispatch endpoint validates order is in "designing" status
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Admin can update order status through all stages, factory dispatch sends email with design specs (STL link, parameter sheet, shipping address) per D-09, ADMN-04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin -> factory email | Design specs sent externally |
| admin -> status transitions | Must follow valid workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Information Disclosure | factory dispatch email | accept | Factory is a trusted partner, design specs are the product being manufactured. STL links use signed URLs if S3/R2 available. |
| T-05-09 | Tampering | status transitions | mitigate | Validate current status before allowing transition (e.g., dispatch only from "designing") |
| T-05-10 | Elevation of Privilege | /api/admin/orders/[id]/* | mitigate | requireAdmin() on all admin API routes |
</threat_model>

<verification>
- Navigate to /admin/dashboard/orders/[id] as admin
- See order info, scan data tab with 3D model, design tab with insole preview
- Click status transition button, confirm dialog, verify status updates
- Click "제작 의뢰" on a designing order, verify factory email logged in dev console
- Verify shipping transition requires tracking number input
</verification>

<success_criteria>
Admin can view complete order data (customer scan, design specs with STL download, TPU map) and manage order workflow including factory dispatch per D-06, D-07, D-08, D-09.
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-dashboard-order-management/05-04-SUMMARY.md`
</output>
