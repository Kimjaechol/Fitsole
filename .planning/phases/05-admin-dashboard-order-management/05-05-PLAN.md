---
phase: 05-admin-dashboard-order-management
plan: 05
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/app/(admin)/admin/dashboard/salted/page.tsx
  - src/components/admin/salted-session-table.tsx
  - src/components/admin/salted-session-detail.tsx
  - src/app/(admin)/admin/dashboard/reservations/page.tsx
  - src/components/admin/reservation-table.tsx
  - src/components/admin/reservation-form.tsx
  - src/lib/db/schema.ts
  - src/app/api/admin/salted/route.ts
  - src/app/api/admin/salted/[id]/route.ts
  - src/app/api/admin/reservations/route.ts
  - src/app/api/admin/reservations/[id]/route.ts
autonomous: true
requirements:
  - ADMN-05
  - ADMN-06
must_haves:
  truths:
    - "Admin can view SALTED measurement sessions with raw pressure data and analysis results"
    - "Admin can view before/after verification report for SALTED sessions"
    - "Admin can manage offline store reservations (view, create, complete)"
    - "Admin can track smart insole kit inventory"
  artifacts:
    - path: "src/app/(admin)/admin/dashboard/salted/page.tsx"
      provides: "SALTED sessions list and detail page"
    - path: "src/components/admin/salted-session-detail.tsx"
      provides: "SALTED session detail with pressure timeline and analysis"
    - path: "src/app/(admin)/admin/dashboard/reservations/page.tsx"
      provides: "Reservation management page"
    - path: "src/lib/db/schema.ts"
      provides: "reservations and kitInventory tables"
    - path: "src/app/api/admin/reservations/route.ts"
      provides: "CRUD API for reservations"
      exports: ["GET", "POST"]
  key_links:
    - from: "src/components/admin/salted-session-detail.tsx"
      to: "src/components/insole/before-after-report.tsx"
      via: "reuses existing verification report component"
      pattern: "import.*BeforeAfterReport"
    - from: "src/app/api/admin/reservations/route.ts"
      to: "src/lib/db/schema.ts"
      via: "CRUD on reservations table"
      pattern: "reservations"
---

<objective>
SALTED session viewer and offline store reservation management for admin dashboard.

Purpose: Admin needs to view SALTED measurement data (D-10, ADMN-05) and manage offline store reservations with kit inventory (D-11, ADMN-06). These are the remaining admin features that complete the dashboard.
Output: SALTED session viewer with pressure data visualization and reservation management CRUD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-dashboard-order-management/05-CONTEXT.md
@src/lib/db/schema.ts
@src/components/insole/before-after-report.tsx
@src/components/insole/salted-session-ui.tsx

<interfaces>
From src/lib/db/schema.ts:
- saltedSessions table: id, userId, scanId, sessionType, rawPressureData (jsonb), analysisResult (jsonb), durationSeconds, dataPointCount

From src/components/insole/before-after-report.tsx:
- BeforeAfterReport component for SALTED verification

From src/lib/admin-auth.ts (Plan 02):
- requireAdmin()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SALTED session viewer</name>
  <files>src/app/(admin)/admin/dashboard/salted/page.tsx, src/components/admin/salted-session-table.tsx, src/components/admin/salted-session-detail.tsx, src/app/api/admin/salted/route.ts, src/app/api/admin/salted/[id]/route.ts</files>
  <read_first>src/lib/db/schema.ts, src/components/insole/before-after-report.tsx, src/components/insole/salted-session-ui.tsx</read_first>
  <action>
1. Create `GET /api/admin/salted` route:
   - requireAdmin() check
   - Query saltedSessions with join on users (for customer name/email)
   - Return list: id, customerName, customerEmail, sessionType, durationSeconds, dataPointCount, createdAt

2. Create `GET /api/admin/salted/[id]` route:
   - requireAdmin() check
   - Fetch session with full data: rawPressureData, analysisResult, user info
   - Join with insoleDesigns if a design was generated from this session
   - Return full session detail

3. Create `src/components/admin/salted-session-table.tsx`:
   - Data table with columns: 고객명, 세션 유형, 측정 시간 (seconds), 데이터 포인트 수, 측정일
   - Row click navigates to detail view
   - Use shadcn Table (simpler than TanStack Table — small dataset)

4. Create `src/components/admin/salted-session-detail.tsx` (per D-10):
   - Props: session (full SALTED session data)
   - Section 1: 세션 정보 — customer name, session type, duration, data point count, date
   - Section 2: 생체역학 분석 — from analysisResult jsonb:
     - Landing pattern (착지 패턴): rearfoot/midfoot/forefoot
     - Pronation (내전/외전): normal/overpronation/supination with degree
     - COP trajectory description
     - Arch flexibility index
   - Section 3: 압력 데이터 타임라인 — simple line chart of total pressure over time (use recharts — Claude's discretion per CONTEXT.md). X-axis: time (seconds), Y-axis: total pressure. Sample rawPressureData at 1Hz (every 100 frames from 100Hz data) for rendering performance.
   - Section 4: 검증 보고서 — if before/after report data exists, reuse BeforeAfterReport component (dynamic import ssr:false)
   - If rawPressureData is null/empty, show "원시 데이터 없음"

5. Create `src/app/(admin)/admin/dashboard/salted/page.tsx`:
   - Server component
   - Fetch sessions list
   - URL param `?id=xxx` triggers detail view alongside list (or use dialog/sheet)
   - Page title: "SALTED 측정 세션"
  </action>
  <acceptance_criteria>
    - SALTED sessions listed with customer info
    - Session detail shows biomechanical analysis results in Korean
    - Pressure data timeline chart renders (recharts line chart)
    - Before/after verification report shown when available
    - Empty data states handled gracefully
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Admin can view SALTED measurement sessions with raw pressure data timeline, biomechanical analysis, and before/after report per D-10, ADMN-05</done>
</task>

<task type="auto">
  <name>Task 2: Offline store reservation management and kit inventory</name>
  <files>src/lib/db/schema.ts, src/app/(admin)/admin/dashboard/reservations/page.tsx, src/components/admin/reservation-table.tsx, src/components/admin/reservation-form.tsx, src/app/api/admin/reservations/route.ts, src/app/api/admin/reservations/[id]/route.ts</files>
  <read_first>src/lib/db/schema.ts</read_first>
  <action>
1. Add to `src/lib/db/schema.ts` (per D-11):
   - `reservationStatusEnum`: pgEnum with ["pending", "confirmed", "completed", "cancelled"]
   - `reservations` table: id (uuid PK), customerName (text), customerPhone (text), customerEmail (text), reservationDate (timestamp), timeSlot (text — e.g., "10:00-11:00"), serviceType (text — "measurement" | "consultation" | "pickup"), status (reservationStatusEnum default "pending"), notes (text), createdAt, updatedAt
   - `kitInventory` table: id (uuid PK), kitName (text — e.g., "SALTED Smart Insole Kit"), totalQuantity (real), availableQuantity (real), lastUpdated (timestamp)

2. Create `GET/POST /api/admin/reservations` route:
   - requireAdmin() check
   - GET: fetch all reservations sorted by reservationDate desc. Accept filter: status, dateFrom, dateTo
   - POST: create new reservation. Validate with Zod: customerName required, customerPhone required, reservationDate required, timeSlot required, serviceType required

3. Create `PATCH/DELETE /api/admin/reservations/[id]` route:
   - requireAdmin() check
   - PATCH: update reservation (status change — mark as confirmed/completed/cancelled, or edit details)
   - DELETE: soft delete by setting status to cancelled

4. Create `src/components/admin/reservation-table.tsx`:
   - Data table: 고객명, 전화번호, 예약일, 시간대, 서비스 유형, 상태, 액션
   - Service type labels: measurement="발 측정", consultation="상담", pickup="수령"
   - Status badges: pending=yellow, confirmed=blue, completed=green, cancelled=red
   - Action buttons: 확인 (confirm), 완료 (complete), 취소 (cancel) based on current status
   - Filter by date and status

5. Create `src/components/admin/reservation-form.tsx`:
   - Dialog form for creating new reservation
   - Fields: 고객명, 전화번호, 이메일 (optional), 예약일 (date picker), 시간대 (select with 1-hour slots 10:00-19:00), 서비스 유형 (select), 메모 (textarea optional)
   - Zod validation on submit

6. Create `src/app/(admin)/admin/dashboard/reservations/page.tsx`:
   - Server component
   - Page title: "매장 예약 관리"
   - "새 예약" button opens reservation form dialog
   - Below: Kit inventory section showing available/total counts for each kit item
   - Kit inventory display: simple card with kitName, "사용 가능: X / 전체: Y" format
   - For kit updates, admin can click to edit availableQuantity (inline edit or small dialog)
  </action>
  <acceptance_criteria>
    - Reservations table shows all reservations with Korean labels
    - Admin can create new reservation via form dialog
    - Admin can change reservation status (confirm, complete, cancel)
    - Kit inventory shows available vs total counts
    - Date and status filters work
    - All validation enforced (name, phone, date, time required)
    - Korean labels and time formats throughout
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Admin can manage offline store reservations (create, view, update status) and track smart insole kit inventory per D-11, ADMN-06</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin -> /api/admin/salted | Access to customer SALTED measurement data |
| admin -> /api/admin/reservations | Reservation CRUD operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Information Disclosure | /api/admin/salted | mitigate | requireAdmin() — only admins can view customer pressure data |
| T-05-12 | Tampering | /api/admin/reservations | mitigate | Zod validation on all inputs, requireAdmin() on all endpoints |
| T-05-13 | Denial of Service | reservation creation | accept | Low risk — admin-only creation, no public-facing reservation form in this phase |
</threat_model>

<verification>
- Navigate to /admin/dashboard/salted, see SALTED sessions list
- Click session to see pressure timeline chart and analysis results
- Navigate to /admin/dashboard/reservations, see reservation list
- Create new reservation via form, verify it appears in list
- Change reservation status (confirm, complete)
- See kit inventory with available/total counts
</verification>

<success_criteria>
Admin can view SALTED sessions with pressure data and manage offline store reservations with kit inventory, completing the full admin dashboard per D-10, D-11, ADMN-05, ADMN-06.
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-dashboard-order-management/05-05-SUMMARY.md`
</output>
