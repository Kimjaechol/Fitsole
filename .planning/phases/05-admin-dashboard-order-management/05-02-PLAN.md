---
phase: 05-admin-dashboard-order-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/email/order-status-notification.ts
  - src/app/api/admin/orders/[id]/status/route.ts
  - src/lib/admin-auth.ts
  - src/middleware.ts
autonomous: true
requirements:
  - ORDR-02
  - ORDR-03
  - ADMN-04
must_haves:
  truths:
    - "Email is sent to customer when order status changes"
    - "Admin can update order status via API"
    - "Only users with role='admin' can access /admin routes"
  artifacts:
    - path: "src/lib/email/order-status-notification.ts"
      provides: "Email notification for each status transition"
    - path: "src/app/api/admin/orders/[id]/status/route.ts"
      provides: "PATCH endpoint to update order status"
      exports: ["PATCH"]
    - path: "src/lib/admin-auth.ts"
      provides: "Admin auth helper function"
    - path: "src/middleware.ts"
      provides: "Route protection for /admin paths"
  key_links:
    - from: "src/app/api/admin/orders/[id]/status/route.ts"
      to: "src/lib/email/order-status-notification.ts"
      via: "sends email after status update"
      pattern: "sendOrderStatusEmail"
    - from: "src/middleware.ts"
      to: "src/lib/db/schema.ts"
      via: "checks user role"
      pattern: "role.*admin"
---

<objective>
Order status update API with email notifications and admin auth middleware.

Purpose: Admin needs to update order status (D-09 partial, ORDR-03) and users need email notifications at each transition (D-02, ORDR-02). Admin access control (D-12, D-13) must be established before building admin UI.
Output: Working status update endpoint with automatic email notifications and admin route protection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-dashboard-order-management/05-CONTEXT.md
@src/lib/db/schema.ts
@src/lib/auth.ts
@src/lib/email/order-confirmation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Order status notification emails</name>
  <files>src/lib/email/order-status-notification.ts</files>
  <read_first>src/lib/email/order-confirmation.ts</read_first>
  <action>
Create `src/lib/email/order-status-notification.ts` following the exact same pattern as `order-confirmation.ts` (per D-02):

1. Define `StatusEmailParams` interface: to (email), orderNumber, newStatus (OrderStatus type), trackingNumber? (string), trackingCarrier? (string)

2. Define Korean status messages map:
   - paid: "주문이 확인되었습니다"
   - designing: "맞춤 인솔 설계가 시작되었습니다"
   - manufacturing: "인솔 제작이 시작되었습니다"
   - shipping: "주문하신 상품이 발송되었습니다"
   - delivered: "배송이 완료되었습니다"

3. Build email HTML using same FitSole branded template style (dark header, white body, gray footer) as order-confirmation:
   - Header: "FitSole" + "주문 상태 업데이트"
   - Body: order number, status change message, status progress bar (text-based: 주문확인 > 인솔설계 > 제작중 > 배송중 > 완료 with current step bold)
   - If shipping status and trackingNumber provided, show tracking section with carrier name and tracking number
   - Footer: support@fitsole.kr

4. Export `sendOrderStatusEmail(params)` async function:
   - Same Resend pattern: check RESEND_API_KEY, send via Resend if available, console.log fallback for dev
   - Subject: `[FitSole] 주문 상태 업데이트 (${orderNumber})`
   - Fire-and-forget pattern (same as order confirmation)
  </action>
  <acceptance_criteria>
    - Email template renders for all 5 notification-worthy statuses
    - Tracking info shown only when status is "shipping" and trackingNumber provided
    - Dev fallback logs email content when RESEND_API_KEY not set
    - Korean text throughout
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Order status email notification function ready, following existing Resend pattern with Korean templates for all stages per D-02</done>
</task>

<task type="auto">
  <name>Task 2: Admin auth middleware and order status update API</name>
  <files>src/lib/admin-auth.ts, src/middleware.ts, src/app/api/admin/orders/[id]/status/route.ts</files>
  <read_first>src/lib/auth.ts, src/lib/db/schema.ts, src/middleware.ts</read_first>
  <action>
1. Create `src/lib/admin-auth.ts` (per D-12, D-13):
   - Export `async function requireAdmin()`: calls auth(), gets session, queries users table for role column, throws/returns 403 if role !== "admin"
   - Export `async function isAdmin(userId: string): Promise<boolean>`: queries users table, returns role === "admin"
   - Note in comment: "Admin role is set directly in DB per D-13 (no self-registration)"

2. Create or update `src/middleware.ts` (per D-12):
   - If middleware.ts exists, extend it. If not, create new.
   - Match paths starting with `/admin/dashboard` (NOT /admin which is Payload CMS per D-04)
   - For matched paths: check session via auth(), if no session redirect to /login
   - Note: Full role check happens in API routes and server components (middleware can only check session existence, not DB role — keep it lightweight)
   - Export config.matcher to include `/admin/dashboard/:path*`

3. Create `PATCH /api/admin/orders/[id]/status/route.ts` (per D-09 partial, ORDR-03):
   - Call requireAdmin() — returns 403 if not admin
   - Accept JSON body: { status: OrderStatus, trackingNumber?: string, trackingCarrier?: string }
   - Validate status is a valid OrderStatus value using Zod
   - Update order in DB: set status, updatedAt, and tracking fields if provided
   - After successful update, fetch the order's user email
   - Call sendOrderStatusEmail fire-and-forget (do NOT await — same pattern as checkout confirm)
   - Return updated order
   - Return 404 if order not found
  </action>
  <acceptance_criteria>
    - requireAdmin() returns 403 for non-admin users
    - Middleware redirects unauthenticated users from /admin/dashboard to /login
    - PATCH endpoint validates status enum values
    - Status update triggers email notification fire-and-forget
    - Tracking fields saved when provided with shipping status
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Admin auth system established (D-12, D-13), order status update API works with automatic email notifications (D-02, ORDR-03)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/admin/* | Admin-only endpoints |
| admin -> order status | Status transitions must be valid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Elevation of Privilege | /api/admin/* | mitigate | requireAdmin() checks user.role === "admin" in DB, not just session existence |
| T-05-04 | Tampering | /api/admin/orders/[id]/status | mitigate | Zod validation of status enum, only valid transitions accepted |
| T-05-05 | Spoofing | /admin/dashboard | mitigate | Middleware redirects unauthenticated users to /login |
</threat_model>

<verification>
- PATCH /api/admin/orders/[id]/status with admin user returns 200
- PATCH /api/admin/orders/[id]/status with non-admin returns 403
- Email notification logged in dev console after status update
- Middleware redirects unauthenticated /admin/dashboard access to /login
</verification>

<success_criteria>
Admin can update order status via API, email notifications sent at each transition, admin routes protected by role check.
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-dashboard-order-management/05-02-SUMMARY.md`
</output>
