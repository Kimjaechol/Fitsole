---
phase: 06-segmentation-support-offline-store
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/components/ui/accordion.tsx
  - src/app/(main)/faq/page.tsx
  - src/app/(main)/guarantee/page.tsx
  - src/app/(main)/remake-policy/page.tsx
  - src/app/(main)/support/page.tsx
  - src/app/api/support/contact/route.ts
  - src/lib/email/support-contact.ts
  - src/components/support/contact-form.tsx
  - src/components/layout/site-footer.tsx
  - src/app/(main)/layout.tsx
autonomous: true
requirements:
  - SUPP-01
  - SUPP-02
  - SUPP-03

must_haves:
  truths:
    - "/faq page renders accordion sections: 측정 정확도, 배송, 반품/교환, 맞춤 인솔, 오프라인 매장"
    - "/support page has a working contact form that submits to /api/support/contact and sends an email via Resend (or logs in dev)"
    - "/guarantee page describes the 90-day satisfaction guarantee and free remake policy"
    - "Landing page footer visibly shows '90일 만족 보장' with link to /guarantee"
    - "Invalid contact form submissions (missing fields, invalid email) return 400 with Korean error messages"
  artifacts:
    - path: "src/components/ui/accordion.tsx"
      provides: "shadcn Accordion primitive (Radix-based)"
      min_lines: 40
    - path: "src/app/(main)/faq/page.tsx"
      provides: "FAQ page with 5 accordion sections (D-07)"
      contains: "측정 정확도"
    - path: "src/app/(main)/guarantee/page.tsx"
      provides: "90-day satisfaction guarantee page (D-09)"
      contains: "90일"
    - path: "src/app/(main)/remake-policy/page.tsx"
      provides: "Free remake policy page (D-10)"
      contains: "무료 재제작"
    - path: "src/app/(main)/support/page.tsx"
      provides: "Contact form page (D-08)"
      min_lines: 20
    - path: "src/app/api/support/contact/route.ts"
      provides: "POST endpoint that validates form and sends email via sendSupportContactEmail"
      exports: ["POST"]
    - path: "src/lib/email/support-contact.ts"
      provides: "sendSupportContactEmail() following Resend + dev-console fallback pattern"
      exports: ["sendSupportContactEmail"]
    - path: "src/components/layout/site-footer.tsx"
      provides: "Site footer with 90-day guarantee badge + links to /faq, /guarantee, /support"
      contains: "90일"
  key_links:
    - from: "src/components/support/contact-form.tsx"
      to: "src/app/api/support/contact/route.ts"
      via: "POST fetch with Zod-validated body"
      pattern: "fetch\\(.*api/support/contact"
    - from: "src/app/api/support/contact/route.ts"
      to: "src/lib/email/support-contact.ts"
      via: "await sendSupportContactEmail(...)"
      pattern: "sendSupportContactEmail"
    - from: "src/app/(main)/layout.tsx"
      to: "src/components/layout/site-footer.tsx"
      via: "<SiteFooter /> rendered at bottom of (main) layout"
      pattern: "SiteFooter"
---

<objective>
Implement support & guarantee features (SUPP-01, SUPP-02, SUPP-03) per locked decisions D-07 through D-10. Build FAQ with accordion, contact form that emails support, 90-day satisfaction guarantee page, and free remake policy page. Surface the 90-day guarantee in the site footer per D-09.

Purpose: Customers buying custom insoles need confidence (guarantee), answers (FAQ), and a path to reach humans (contact form) before and after purchase. This is table-stakes trust infrastructure for a premium custom product.

Output: /faq, /support, /guarantee, /remake-policy pages exist; site footer shows 90-day guarantee; contact form submits via POST and email is sent via existing Resend integration (dev-console fallback).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-segmentation-support-offline-store/06-CONTEXT.md

# Existing email pattern to follow exactly
@src/lib/email/order-status-notification.ts
@src/lib/email/order-confirmation.ts
@src/app/(main)/layout.tsx
</context>

<interfaces>
<!-- Contracts the executor must follow. -->

From src/lib/email/order-status-notification.ts (the pattern to mirror):
```typescript
// Resend + dev-console fallback pattern used by all existing email functions
export async function sendOrderStatusEmail(params: {...}): Promise<void> {
  // ...
  if (process.env.RESEND_API_KEY) {
    const { Resend } = await import("resend");
    const resend = new Resend(process.env.RESEND_API_KEY);
    await resend.emails.send({ from, to, subject, html });
  } else {
    console.log("[email:dev-fallback]", { to, subject, html });
  }
}
```

Support contact email contract to build:
```typescript
// src/lib/email/support-contact.ts
export interface SupportContactPayload {
  name: string;
  email: string;
  category: "measurement" | "order" | "remake" | "general";
  subject: string;
  message: string;
}
export async function sendSupportContactEmail(
  payload: SupportContactPayload
): Promise<void>;
```

Support env var: `SUPPORT_EMAIL` (default: `support@fitsole.kr` — follow the FACTORY_EMAIL pattern from Phase 5).

Zod schema for POST /api/support/contact:
```typescript
const contactSchema = z.object({
  name: z.string().trim().min(1, "이름은 필수입니다.").max(100),
  email: z.string().trim().email("이메일 형식이 올바르지 않습니다."),
  category: z.enum(["measurement", "order", "remake", "general"]),
  subject: z.string().trim().min(1, "제목은 필수입니다.").max(200),
  message: z.string().trim().min(10, "문의 내용은 10자 이상이어야 합니다.").max(5000),
});
```

shadcn Accordion component source (standard shadcn/ui — copy from https://ui.shadcn.com/docs/components/accordion, which uses @radix-ui/react-accordion). Install the Radix package if not present:
```bash
pnpm add @radix-ui/react-accordion
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shadcn Accordion, FAQ page, Guarantee page, Remake policy page, site footer</name>
  <read_first>
    - src/components/ui/card.tsx (shadcn pattern reference)
    - src/components/ui/dialog.tsx (Radix-based shadcn pattern reference)
    - src/app/(main)/layout.tsx (where to inject SiteFooter)
    - src/app/(main)/page.tsx (landing page — guarantee footer visibility)
  </read_first>
  <behavior>
    - Test 1: /faq page renders and contains section titles: 측정 정확도, 배송, 반품/교환, 맞춤 인솔, 오프라인 매장
    - Test 2: /guarantee page contains "90일" and "만족 보장"
    - Test 3: /remake-policy page contains "무료 재제작"
    - Test 4: SiteFooter component renders "90일 만족 보장" text and link to /guarantee
    - Test 5: Accordion items expand and collapse on click (Radix state test)
  </behavior>
  <files>
    src/components/ui/accordion.tsx,
    src/app/(main)/faq/page.tsx,
    src/app/(main)/guarantee/page.tsx,
    src/app/(main)/remake-policy/page.tsx,
    src/components/layout/site-footer.tsx,
    src/app/(main)/layout.tsx,
    src/app/(main)/faq/__tests__/page.test.tsx,
    src/components/layout/__tests__/site-footer.test.tsx,
    package.json
  </files>
  <action>
    Implements D-07, D-09, D-10 and part of footer visibility for D-09.

    1. **Install Radix accordion**:
       - Run `pnpm add @radix-ui/react-accordion` (or `npm install` — match Phase 1-5 lockfile convention by checking package-lock vs pnpm-lock)

    2. **shadcn Accordion component** (src/components/ui/accordion.tsx):
       - Copy the canonical shadcn accordion source verbatim (AccordionItem, AccordionTrigger, AccordionContent) wrapped around Radix primitives
       - Uses the `cn()` util from src/lib/utils.ts (already exists)
       - Match the style/formatting of existing src/components/ui/*.tsx files

    3. **FAQ page** (src/app/(main)/faq/page.tsx) — D-07 locked:
       - Server component with `type="single" collapsible` Accordion containing EXACTLY these 5 sections (Korean):
         * **측정 정확도** — 3 Q&A pairs about ±0.15mm accuracy, A4 paper calibration, scan quality score
         * **배송** — 2-3 Q&A pairs about 제작 기간 (2-3주), 배송비 (50,000원 이상 무료)
         * **반품/교환** — 2-3 Q&A pairs referencing 90-day guarantee and link to /guarantee
         * **맞춤 인솔** — 3 Q&A pairs about Varioshore TPU, zone hardness, Line 1 vs Line 2 difference
         * **오프라인 매장** — 2 Q&A pairs about 강남역 지하상가 location, SALTED kit service, link to /stores/gangnam
       - Content is boilerplate Korean text (per CONTEXT.md "Claude's Discretion")
       - H1: "자주 묻는 질문" with short intro paragraph

    4. **Guarantee page** (src/app/(main)/guarantee/page.tsx) — D-09 locked:
       - Server component
       - Hero: "90일 만족 보장"
       - Sections explaining: 보장 범위, 신청 방법 (/support 링크), 무료 재제작 안내 (/remake-policy 링크)

    5. **Remake policy page** (src/app/(main)/remake-policy/page.tsx) — D-10 locked:
       - Server component
       - H1: "무료 재제작 정책"
       - Sections: 재제작 조건, 프로세스 (1. 문의 접수 → 2. 원인 분석 → 3. 재제작 → 4. 배송), 기간 (2-3주), 연락처 (/support)

    6. **Site footer** (src/components/layout/site-footer.tsx):
       - Server component
       - Contains a prominently-visible "90일 만족 보장" badge/text with link to /guarantee (D-09 requires landing footer visibility)
       - Links row: /faq, /guarantee, /remake-policy, /support, /stores/gangnam
       - Copyright notice and company info
       - Mobile-first responsive (stack on mobile, row on desktop)

    7. **Wire footer into layout** (src/app/(main)/layout.tsx):
       - Add `<SiteFooter />` after `{children}` but BEFORE closing tags of main/body. Do NOT break existing tab bar or nav.
       - Ensure the BottomTabBar (mobile) doesn't overlap footer on small screens — add bottom padding `pb-24 md:pb-0` to the layout main wrapper if not already present.
  </action>
  <acceptance_criteria>
    - /faq, /guarantee, /remake-policy all return 200 and render server-side
    - FAQ page contains all 5 section titles listed above
    - Guarantee page contains "90일 만족 보장"
    - Remake policy page contains "무료 재제작"
    - SiteFooter rendered on every (main) route including landing page
    - Accordion items expand/collapse (verify via Radix data-state in test)
    - pnpm typecheck && pnpm test && pnpm build all pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm test -- src/app/\(main\)/faq/__tests__ src/components/layout/__tests__ && pnpm build</automated>
  </verify>
  <done>D-07, D-09, D-10 fully implemented. FAQ uses real Radix Accordion, 90-day guarantee page exists, remake policy exists, footer surfaces guarantee on landing page. SUPP-01 and SUPP-03 verifiable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contact form page, /api/support/contact endpoint, email sender</name>
  <read_first>
    - src/lib/email/order-status-notification.ts (exact Resend + dev-fallback pattern to mirror)
    - src/lib/email/factory-dispatch.ts (env var default pattern — FACTORY_EMAIL)
    - src/components/cart/ or src/components/checkout/ (existing form patterns with Zod)
    - src/app/api/admin/reservations/route.ts (Zod validation + error shape pattern)
  </read_first>
  <behavior>
    - Test 1: POST /api/support/contact with valid payload returns 200 and triggers sendSupportContactEmail
    - Test 2: POST with invalid email returns 400 with Korean error "이메일 형식이 올바르지 않습니다."
    - Test 3: POST with missing required fields returns 400
    - Test 4: POST with message < 10 chars returns 400
    - Test 5: sendSupportContactEmail without RESEND_API_KEY logs via console.log (dev fallback)
    - Test 6: sendSupportContactEmail with RESEND_API_KEY mocked calls resend.emails.send with correct payload
  </behavior>
  <files>
    src/app/(main)/support/page.tsx,
    src/components/support/contact-form.tsx,
    src/app/api/support/contact/route.ts,
    src/lib/email/support-contact.ts,
    src/app/api/support/contact/__tests__/route.test.ts,
    src/lib/email/__tests__/support-contact.test.ts
  </files>
  <action>
    Implements D-08 (contact form) and SUPP-02 (email contact).

    1. **Email sender** (src/lib/email/support-contact.ts):
       - Export `SupportContactPayload` interface (name, email, category, subject, message)
       - Export `sendSupportContactEmail(payload)` mirroring the exact pattern from src/lib/email/order-status-notification.ts:
         * Read `process.env.SUPPORT_EMAIL ?? "support@fitsole.kr"` as recipient (follow Phase 5 FACTORY_EMAIL default pattern)
         * Build Korean-language HTML email with customer name, email, category label, subject, message
         * If `process.env.RESEND_API_KEY` exists: dynamic-import Resend and send
         * Else: `console.log("[email:dev-fallback]", {...})`
         * Never throw from send failure in dev; log and continue (tests assert both branches)
       - Category labels map: measurement → "측정 문의", order → "주문 문의", remake → "재제작 문의", general → "일반 문의"

    2. **API route** (src/app/api/support/contact/route.ts):
       - POST handler only
       - Parse body with Zod schema from interfaces section above
       - On validation failure: return `{error, issues}` with 400 (match reservations/route.ts error shape)
       - On success: await sendSupportContactEmail(payload), return `{ok: true}` with 200
       - Wrap email send in try/catch — if email sending throws in production, log to console.error and STILL return 200 to the user (we don't want to leak backend failures; the dev-fallback path handles missing API key)
       - Rate limit consideration: accept as-is for v1 (no auth required for public contact); note in threat model

    3. **Contact form client** (src/components/support/contact-form.tsx):
       - "use client"
       - react-hook-form + zod resolver (check if react-hook-form is in package.json — Phase 1-5 auth forms used it; if not, use controlled useState inputs, matching the style already in src/components/cart/ or src/components/checkout/ — grep first to confirm)
       - Fields: 이름, 이메일, 문의 유형 (select with 4 categories), 제목, 문의 내용 (textarea)
       - On submit: POST to /api/support/contact, show success toast via sonner (already in src/components/ui/sonner.tsx), reset form. On error: show error toast with Korean message.
       - Loading state disables submit button

    4. **Support page** (src/app/(main)/support/page.tsx) — D-08 locked:
       - Server component shell
       - H1: "문의하기"
       - Intro paragraph explaining response time (1-2 business days)
       - Renders `<ContactForm />`
       - Secondary contact: email address text (support@fitsole.kr) + link to /faq for quick answers
  </action>
  <acceptance_criteria>
    - POST /api/support/contact validates payload and calls sendSupportContactEmail on success
    - Invalid payloads return 400 with Korean error issues array
    - sendSupportContactEmail branches on RESEND_API_KEY (dev fallback logs; prod sends via Resend)
    - /support page renders contact form, submitting it triggers POST
    - Success shows toast, form resets
    - pnpm test passes for route.test.ts and support-contact.test.ts (mock Resend module)
    - pnpm typecheck && pnpm build pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm test -- src/app/api/support/contact/__tests__ src/lib/email/__tests__/support-contact.test.ts && pnpm build</automated>
  </verify>
  <done>SUPP-02 fully implemented. Contact form works end-to-end with dev-console fallback (so it works without RESEND_API_KEY in test). D-08 delivered at full fidelity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/support/contact | Unauthenticated public endpoint accepting user-submitted form data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | POST /api/support/contact | mitigate | Zod schema enforces field types, email format, length limits (name ≤100, subject ≤200, message 10-5000 chars); reject with 400 |
| T-06-06 | Denial of Service | POST /api/support/contact (no auth) | accept | v1 has no rate limit; low risk because email send is fire-and-forget and Resend has its own quota guards; revisit if abuse appears in logs |
| T-06-07 | Information Disclosure | Contact form error responses | mitigate | Generic 500 on unexpected errors; no stack traces leaked to client; only validation issue paths exposed |
| T-06-08 | Repudiation | Contact form submission audit | accept | v1 does not persist contact messages to DB; Resend delivery log is sufficient audit trail for low-volume support |
| T-06-09 | Injection | HTML email rendering | mitigate | Escape all user-provided fields when interpolating into HTML email body; no raw HTML injection |
</threat_model>

<verification>
- /faq renders all 5 accordion sections and items expand/collapse
- /guarantee shows 90-day satisfaction guarantee
- /remake-policy shows free remake policy
- /support contact form submits → toast shown → server console shows [email:dev-fallback] log (no RESEND_API_KEY in test env)
- Footer visible on landing page (/) with 90-day guarantee badge
- pnpm typecheck && pnpm test && pnpm build all pass
</verification>

<success_criteria>
- SUPP-01: FAQ page covers measurement accuracy and fit concerns ✓
- SUPP-02: Contact via email (Resend send or dev-fallback) ✓
- SUPP-03: 90-day satisfaction guarantee displayed on landing footer + FAQ + /guarantee ✓
- All 4 decisions D-07 through D-10 implemented at full fidelity
</success_criteria>

<output>
After completion, create `.planning/phases/06-segmentation-support-offline-store/06-02-SUMMARY.md`
</output>
