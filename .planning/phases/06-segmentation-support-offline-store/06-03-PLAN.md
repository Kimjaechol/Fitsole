---
phase: 06-segmentation-support-offline-store
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/stores/constants.ts
  - src/app/(main)/stores/gangnam/page.tsx
  - src/components/stores/store-hero.tsx
  - src/components/stores/store-info-section.tsx
  - src/components/stores/kit-service-section.tsx
  - src/components/stores/kit-rental-section.tsx
  - src/components/stores/store-location-map.tsx
  - src/components/stores/public-reservation-form.tsx
  - src/app/api/reservations/route.ts
autonomous: true
requirements:
  - OFFL-01
  - OFFL-02
  - OFFL-03
  - OFFL-04

must_haves:
  truths:
    - "/stores/gangnam page renders 5 sections: 히어로, 위치 안내, 스마트 인솔 키트 서비스, 예약 폼, FAQ"
    - "Store page displays address '서울특별시 강남구 강남역 지하상가' and hours '평일 10:00-21:00, 주말 10:00-20:00'"
    - "Location is shown via Kakao Maps static image embed (URL built from STORE_CONSTANTS.lat/lng)"
    - "Public reservation form posts to /api/reservations (not /api/admin/reservations) and creates a reservation row with status=pending"
    - "Athlete kit rental section exists with anchor #kit-rental so /segment/athlete can link directly"
    - "Athlete kit rental section describes 1-2주 rental program per D-15"
  artifacts:
    - path: "src/lib/stores/constants.ts"
      provides: "STORE_GANGNAM constant with address, hours, phone, lat/lng per D-12"
      exports: ["STORE_GANGNAM"]
    - path: "src/app/(main)/stores/gangnam/page.tsx"
      provides: "Offline store page composing all 5 sections"
      min_lines: 30
    - path: "src/components/stores/kit-rental-section.tsx"
      provides: "Athlete kit rental info section with #kit-rental anchor (D-15)"
      contains: "1-2주"
    - path: "src/components/stores/public-reservation-form.tsx"
      provides: "Public client-side reservation form with 3 service types"
      contains: "SALTED"
    - path: "src/app/api/reservations/route.ts"
      provides: "Public POST /api/reservations endpoint (no admin gate)"
      exports: ["POST"]
  key_links:
    - from: "src/app/(main)/stores/gangnam/page.tsx"
      to: "src/lib/stores/constants.ts"
      via: "import { STORE_GANGNAM }"
      pattern: "STORE_GANGNAM"
    - from: "src/components/stores/public-reservation-form.tsx"
      to: "src/app/api/reservations/route.ts"
      via: "POST fetch with Zod-validated body"
      pattern: "fetch\\(.*api/reservations"
    - from: "src/app/api/reservations/route.ts"
      to: "src/lib/db/schema.ts reservations table"
      via: "drizzle insert"
      pattern: "db\\.insert\\(reservations\\)"
    - from: "src/app/(main)/segment/athlete/page.tsx (Plan 01)"
      to: "src/components/stores/kit-rental-section.tsx"
      via: "anchor link /stores/gangnam#kit-rental"
      pattern: "#kit-rental"
---

<objective>
Implement the offline store page for 강남역 지하상가 with smart insole kit service, reservation system, and athlete kit rental info (OFFL-01, OFFL-02, OFFL-03, OFFL-04) per locked decisions D-11 through D-15.

Purpose: CONTEXT.md deferred-from-v1 decision changed: offline store is now in scope for v1. The page introduces the 강남역 지하상가 location, explains the SALTED smart insole kit measurement service, allows public reservations (reusing Phase 5 reservations table via a new public endpoint), and links athletes to the kit rental program. This is a pure frontend composition plan with one new public API route.

Output: /stores/gangnam page with all 5 sections, public /api/reservations POST endpoint creating reservations with status='pending', Kakao Maps static location embed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-segmentation-support-offline-store/06-CONTEXT.md

# Existing reservations schema + admin endpoint to mirror (public version reuses schema, not the admin route)
@src/lib/db/schema.ts
@src/app/api/admin/reservations/route.ts
@src/components/admin/reservation-form.tsx
</context>

<interfaces>
<!-- Contracts the executor must follow. -->

From src/lib/db/schema.ts (existing reservations table — DO NOT modify):
```typescript
export const reservations = pgTable("reservations", {
  id: uuid("id").defaultRandom().primaryKey(),
  customerName: text("customer_name").notNull(),
  customerPhone: text("customer_phone").notNull(),
  customerEmail: text("customer_email"),
  reservationDate: timestamp("reservation_date").notNull(),
  timeSlot: text("time_slot").notNull(),
  serviceType: text("service_type").notNull(), // 'measurement' | 'consultation' | 'pickup'
  status: reservationStatusEnum("status").default("pending").notNull(),
  notes: text("notes"),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
});
```

From src/app/api/admin/reservations/route.ts (existing Zod schema to reuse):
```typescript
// Re-export path that the admin route already provides:
import { createReservationSchema } from "@/app/api/admin/reservations/route";
```

Store constants contract to create (src/lib/stores/constants.ts):
```typescript
export interface StoreInfo {
  slug: string;
  name: string;
  address: string;
  addressDetail: string;
  hours: { weekday: string; weekend: string };
  phone: string;
  lat: number;
  lng: number;
}

export const STORE_GANGNAM: StoreInfo = {
  slug: "gangnam",
  name: "FitSole 강남점",
  address: "서울특별시 강남구 강남역 지하상가",
  addressDetail: "상가 번호는 실제 입점 후 업데이트 예정",
  hours: { weekday: "평일 10:00-21:00", weekend: "주말 10:00-20:00" },
  phone: "02-XXX-XXXX",
  lat: 37.497942,   // 강남역 approximate
  lng: 127.027621,
};
```

Service type mapping (reservation form shows 3 human labels but POSTs the existing ServiceType enum values):
```typescript
// D-14 locked options → existing ServiceType enum
const SERVICE_TYPE_OPTIONS = [
  { value: "measurement",   label: "일반 측정" },
  { value: "consultation",  label: "SALTED 정밀 측정" },
  { value: "pickup",        label: "운동선수 키트 대여" },
] as const;
```
Rationale: The Phase 5 reservations table has enum values `measurement | consultation | pickup`. D-14 specifies 3 user-facing options: 일반측정, SALTED 정밀측정, 운동선수 키트 대여. Map them to the existing enum values so we don't need a migration. Document this mapping in the task summary.

Kakao Maps static image URL pattern (D-13):
```
https://dapi.kakao.com/v2/maps/staticmap?...
```
Per D-13 "static image or embedded JS SDK" — use the static image variant to avoid requiring a Kakao API key in v1. If KAKAO_MAP_KEY env var is set, use the static map URL; otherwise show a placeholder `<div>` with address text. Document the fallback in the task summary.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store constants, store page scaffold, Kakao map, hero/info/kit-service/kit-rental sections</name>
  <read_first>
    - src/components/ui/card.tsx (for section cards)
    - src/components/landing/hero-section.tsx (hero style reference)
    - src/app/(main)/layout.tsx (to confirm pathing and SiteFooter wiring from Plan 02 — but Plan 02 is parallel, so DO NOT depend on Plan 02 files)
  </read_first>
  <behavior>
    - Test 1: STORE_GANGNAM constant exports required fields (name, address, hours, phone, lat, lng)
    - Test 2: /stores/gangnam page renders and contains address "강남역 지하상가"
    - Test 3: /stores/gangnam page contains hours "평일 10:00-21:00" and "주말 10:00-20:00"
    - Test 4: /stores/gangnam page contains section with id="kit-rental"
    - Test 5: /stores/gangnam page contains text "SALTED" (kit service description per CONTEXT.md specifics)
    - Test 6: /stores/gangnam page contains text "1-2주" in kit rental section (D-15 rental period)
    - Test 7: StoreLocationMap without KAKAO_MAP_KEY renders fallback with address text; with key set renders img tag pointing at dapi.kakao.com
  </behavior>
  <files>
    src/lib/stores/constants.ts,
    src/lib/stores/__tests__/constants.test.ts,
    src/app/(main)/stores/gangnam/page.tsx,
    src/app/(main)/stores/gangnam/__tests__/page.test.tsx,
    src/components/stores/store-hero.tsx,
    src/components/stores/store-info-section.tsx,
    src/components/stores/kit-service-section.tsx,
    src/components/stores/kit-rental-section.tsx,
    src/components/stores/store-location-map.tsx
  </files>
  <action>
    Implements D-11 (page sections), D-12 (store info), D-13 (Kakao map), D-15 (kit rental), OFFL-01, OFFL-02, OFFL-04.

    1. **Constants** (src/lib/stores/constants.ts):
       - Implement `StoreInfo` interface and `STORE_GANGNAM` constant exactly as shown in the interfaces block above.
       - Export a helper `getKakaoStaticMapUrl(info: StoreInfo, width = 600, height = 400): string | null` that returns the Kakao static map URL if `process.env.KAKAO_MAP_KEY` is set, else null.

    2. **Store hero** (src/components/stores/store-hero.tsx):
       - Server component
       - Renders: store name, a visual placeholder (since D-11 specifies 매장 사진 but CONTEXT.md Claude Discretion says use abstract illustrations), short tagline
       - Uses a colored gradient div with emoji/icon as placeholder (no external image — per deferred list "Store photo gallery")

    3. **Store info section** (src/components/stores/store-info-section.tsx) — D-12 locked:
       - Server component; props `{info: StoreInfo}`
       - Two-column layout on desktop (stacked on mobile):
         * Left: 주소 (address + addressDetail), 운영시간 (weekday + weekend), 전화 (phone)
         * Right: `<StoreLocationMap info={info} />`
       - Must literally render `info.address`, `info.hours.weekday`, `info.hours.weekend`, `info.phone`

    4. **Kakao map component** (src/components/stores/store-location-map.tsx) — D-13 locked:
       - Server component; props `{info: StoreInfo}`
       - If `process.env.KAKAO_MAP_KEY` set: render `<img src={getKakaoStaticMapUrl(info)} alt={...} width={600} height={400} />`
       - Else: render fallback `<div>` with border showing `info.address` as text + "지도 준비 중" note
       - Do NOT use next/image for external Kakao URL (would require remotePatterns config churn); use native `<img>` and mark with `/* eslint-disable-next-line @next/next/no-img-element */`

    5. **Kit service section** (src/components/stores/kit-service-section.tsx) — D-11 locked, OFFL-02:
       - Server component
       - H2: "스마트 인솔 키트 측정 서비스"
       - Per CONTEXT.md specifics: explicitly mentions this is the Moticon/SALTED smart insole measurement service introduced in Phase 2-3
       - 3 bullet points explaining: 어떻게 측정하는지 (5분 walking session), 무엇을 측정하는지 (landing pattern, COP, 압력), 누구에게 적합한지 (health + athlete segments)
       - CTA button linking to /segment/health and /segment/athlete

    6. **Kit rental section** (src/components/stores/kit-rental-section.tsx) — D-15 locked, OFFL-04:
       - Server component with `id="kit-rental"` on the outer wrapping element (so /segment/athlete can deep-link)
       - H2: "운동선수 키트 대여 프로그램"
       - Explain: 축구화/마라톤화 맞춤 제작용, 1-2주 대여 기간, SALTED 스마트 인솔 기반 측정
       - CTA linking to reservation form (`#reservation-form` anchor) with service type "pickup" pre-highlighted in copy
       - Must contain literal text "1-2주" to satisfy test 6 and D-15

    7. **Store page shell** (src/app/(main)/stores/gangnam/page.tsx) — D-11 locked:
       - Server component
       - Renders sections in this exact order per D-11: StoreHero → StoreInfoSection → KitServiceSection → PublicReservationForm (Task 2) → KitRentalSection → FAQ link back to /faq#offline
       - Imports STORE_GANGNAM and passes to info sections
       - Sets page metadata (title: "FitSole 강남점", description about SALTED service)
       - For Task 1, the `<PublicReservationForm />` import can be a placeholder that Task 2 replaces; alternatively create this page AFTER Task 2. Either order works — the task is idempotent.

    Note: The SiteFooter from Plan 02 is NOT wired here. This page sits under (main)/layout.tsx which Plan 02 will add SiteFooter to. Since both plans are Wave 1 and touch different layout concerns, DO NOT edit src/app/(main)/layout.tsx in this plan (file ownership: Plan 01 and Plan 02 both touch it, Plan 03 does NOT).
  </action>
  <acceptance_criteria>
    - STORE_GANGNAM constant matches interfaces block exactly
    - /stores/gangnam page returns 200 and renders 5 sections (hero, info, kit-service, reservation placeholder, kit-rental) in correct order
    - Page contains "강남역 지하상가", "평일 10:00-21:00", "주말 10:00-20:00", "SALTED", "1-2주"
    - kit-rental section has id="kit-rental" anchor
    - StoreLocationMap fallback works without KAKAO_MAP_KEY in test env
    - pnpm typecheck && pnpm test src/lib/stores src/app/\(main\)/stores passes
    - pnpm build succeeds
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm test -- src/lib/stores src/app/\(main\)/stores && pnpm build</automated>
  </verify>
  <done>D-11, D-12, D-13, D-15 implemented. OFFL-01, OFFL-02, OFFL-04 verifiable on page. Store page renders with all locked info.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Public reservation form + POST /api/reservations endpoint</name>
  <read_first>
    - src/app/api/admin/reservations/route.ts (Zod schema to reuse, error shape pattern, drizzle insert pattern)
    - src/components/admin/reservation-form.tsx (form UX reference — field layout, Korean labels)
    - src/lib/db/index.ts (drizzle db instance)
    - src/components/ui/sonner.tsx (toast for success/error)
  </read_first>
  <behavior>
    - Test 1: POST /api/reservations with valid body returns 201 and inserts row with status='pending'
    - Test 2: POST with missing customerName returns 400
    - Test 3: POST with invalid email format returns 400
    - Test 4: POST with invalid serviceType returns 400
    - Test 5: POST with reservationDate in the past returns 400 (new check not in admin version)
    - Test 6: PublicReservationForm renders 3 service type options mapping to {measurement, consultation, pickup}
    - Test 7: Successful form submission calls fetch("/api/reservations", ...) and shows success toast
  </behavior>
  <files>
    src/app/api/reservations/route.ts,
    src/app/api/reservations/__tests__/route.test.ts,
    src/components/stores/public-reservation-form.tsx
  </files>
  <action>
    Implements D-14 (reservation form posts to reservation API), OFFL-03 (reservation/contact info).

    **CRITICAL:** CONTEXT.md D-14 says "Posts to /api/reservations created in Phase 5." Phase 5 actually created `/api/admin/reservations` which is admin-gated via `requireAdmin()`. A public form cannot call an admin endpoint. Therefore we create a NEW public endpoint at `/api/reservations` that reuses the same schema and table. Document this divergence from the CONTEXT.md wording in the task summary.

    1. **Public API route** (src/app/api/reservations/route.ts):
       - POST handler only (no GET — listing is admin-only via existing /api/admin/reservations)
       - Import `createReservationSchema` from `@/app/api/admin/reservations/route` (already exported at end of that file)
       - Add an extended public schema that ALSO validates `reservationDate` is >= today (reject past dates — a safeguard not present in the admin create flow since admins can backfill historical records)
         ```typescript
         const publicReservationSchema = createReservationSchema.extend({
           reservationDate: z.string()
             .refine((s) => !Number.isNaN(new Date(s).getTime()), { message: "예약일 형식이 올바르지 않습니다." })
             .refine((s) => new Date(s) >= new Date(new Date().toDateString()), { message: "예약일은 오늘 이후여야 합니다." }),
         });
         ```
       - Parse body, validate, insert into reservations table with default status='pending', return `{reservation: {id, createdAt}}` with 201
       - Mirror the error shape from admin route (validation issues array)
       - NO `requireAdmin()` — this is public by design
       - Wrap in try/catch; on DB failure return 500 generic message

    2. **Public reservation form** (src/components/stores/public-reservation-form.tsx):
       - "use client"
       - Outer wrapping div has `id="reservation-form"` (so the kit-rental section can anchor-link to it)
       - Uses same form pattern as src/components/admin/reservation-form.tsx but without admin-only fields (no status override)
       - Fields (D-14 locked):
         * 이름 (customerName) — required text
         * 연락처 (customerPhone) — required text
         * 이메일 (customerEmail) — optional
         * 방문 희망 날짜 (reservationDate) — date input, min=today
         * 시간대 (timeSlot) — select with predefined slots: "10:00-11:00", "11:00-12:00", "14:00-15:00", "15:00-16:00", "16:00-17:00", "17:00-18:00"
         * 관심 서비스 (serviceType) — select with 3 options mapping to enum:
           - "일반 측정" → measurement
           - "SALTED 정밀 측정" → consultation
           - "운동선수 키트 대여" → pickup
         * 추가 메시지 (notes) — optional textarea
       - On submit: POST to /api/reservations, show toast via sonner on success ("예약 요청이 접수되었습니다. 담당자가 연락드립니다."), reset form. On error: show error toast with the first validation issue message from the API response.
       - Disable submit button while pending
       - Pre-fill serviceType from `?service=` URL query param if present (so the kit-rental section CTA can link with `?service=pickup#reservation-form`)

    3. **Wire form into store page**:
       - In src/app/(main)/stores/gangnam/page.tsx, replace the Task 1 placeholder with `<PublicReservationForm />` between KitServiceSection and KitRentalSection
       - Update the kit-rental section CTA href to `/stores/gangnam?service=pickup#reservation-form` so the deep-link pre-selects pickup
  </action>
  <acceptance_criteria>
    - POST /api/reservations creates reservations row with status='pending', returns 201 with {reservation.id}
    - Validation errors return 400 with Korean issue messages
    - Past reservationDate rejected with Korean error "예약일은 오늘 이후여야 합니다."
    - PublicReservationForm renders, submits, shows success toast on 201 response
    - kit-rental CTA pre-selects pickup via ?service=pickup query param
    - pnpm test passes for /api/reservations route + form
    - pnpm typecheck && pnpm build pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm test -- src/app/api/reservations src/components/stores && pnpm build</automated>
  </verify>
  <done>D-14 fully implemented. OFFL-03 reservation/contact info fully working: public endpoint created, form posts and persists, admin can view via existing /api/admin/reservations from Phase 5 (closing the loop). Divergence from literal CONTEXT.md wording (new public endpoint vs. reusing admin route) documented in summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/reservations | Unauthenticated public POST endpoint; creates DB rows in reservations table |
| server → dapi.kakao.com | Outbound HTTP to Kakao static maps with API key in URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | POST /api/reservations | mitigate | Zod schema validates all fields, rejects invalid serviceType/email/past dates with 400 |
| T-06-11 | Denial of Service | POST /api/reservations (public, no auth, writes to DB) | mitigate | Apply sane field length limits via reused createReservationSchema (name ≤100, phone ≤40, notes ≤1000); accept v1 rate-limit gap but document as a v2 concern in summary |
| T-06-12 | Information Disclosure | GET listing endpoint | mitigate | Deliberately no public GET — only POST. Admin listing stays behind requireAdmin() on /api/admin/reservations from Phase 5 |
| T-06-13 | Information Disclosure | Kakao Maps API key in URL | accept | Kakao static map keys are client-safe by Kakao's own guidance; key restricted by referer domain in Kakao dashboard (document env var as KAKAO_MAP_KEY, not KAKAO_MAP_SECRET) |
| T-06-14 | Tampering | Reservation form service type mapping | mitigate | Frontend shows 3 Korean labels but POSTs only the locked enum values (measurement/consultation/pickup); API re-validates against enum via Zod |
| T-06-15 | Spoofing | Reservation includes customer contact info | accept | v1 does not verify phone or email ownership; admin contacts customer to confirm before confirming the reservation (status starts at 'pending'); verification deferred to v2 |
</threat_model>

<verification>
- /stores/gangnam returns 200, renders all 5 sections in order (hero, info+map, kit service, reservation form, kit rental, FAQ link)
- Page contains all locked store info strings per D-12
- Kit rental anchor #kit-rental reachable from /segment/athlete link
- POST /api/reservations with valid body returns 201 and inserts row
- POST with past date returns 400 with Korean message
- Admin dashboard (Phase 5 /admin/dashboard/reservations) shows the new row from public form
- pnpm typecheck && pnpm test && pnpm build all pass
</verification>

<success_criteria>
- OFFL-01: Offline store introduction page with 강남역 지하상가 info ✓
- OFFL-02: Smart insole kit measurement service description (SALTED) ✓
- OFFL-03: Location map, operating hours, reservation/contact info ✓
- OFFL-04: Athlete section links to smart insole kit rental program info ✓
- All 5 decisions D-11 through D-15 implemented at full fidelity
</success_criteria>

<output>
After completion, create `.planning/phases/06-segmentation-support-offline-store/06-03-SUMMARY.md`
</output>
