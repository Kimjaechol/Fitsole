---
phase: 03-insole-design-product-catalog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/payload/payload.config.ts
  - src/payload/collections/Products.ts
  - src/payload/collections/Categories.ts
  - src/payload/collections/Media.ts
  - src/app/(payload)/admin/[[...segments]]/page.tsx
  - src/app/(payload)/admin/[[...segments]]/not-found.tsx
  - src/app/(payload)/layout.tsx
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03]

must_haves:
  truths:
    - "Payload CMS admin panel is accessible at /admin"
    - "Products collection supports name, slug, description, category, price, bundleInsolePrice, images, sizes, style, status"
    - "Categories collection has Korean category names (운동화, 구두, 부츠, 샌들)"
    - "Media collection handles product image uploads"
    - "Products can be filtered by category, size, price, style via Payload query API"
  artifacts:
    - path: "src/payload/payload.config.ts"
      provides: "Payload CMS configuration with Postgres adapter"
    - path: "src/payload/collections/Products.ts"
      provides: "Product collection with all required fields"
      exports: ["Products"]
    - path: "src/payload/collections/Categories.ts"
      provides: "Category taxonomy collection"
      exports: ["Categories"]
  key_links:
    - from: "src/payload/payload.config.ts"
      to: "Neon Postgres"
      via: "@payloadcms/db-postgres adapter"
      pattern: "postgresAdapter"
    - from: "src/app/(payload)/admin"
      to: "src/payload/payload.config.ts"
      via: "Payload admin route"
      pattern: "payload"
---

<objective>
Install and configure Payload CMS 3.x into the existing Next.js app with product catalog collections (Products, Categories, Media) for the shoe catalog.

Purpose: Payload CMS provides the product catalog CRUD admin UI, media management, and query API without building custom admin dashboard. Per D-13, shoe catalog uses Payload CMS collections.
Output: Working Payload admin at /admin, product collections ready for data entry
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-insole-design-product-catalog/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Payload CMS and configure with Postgres adapter</name>
  <files>src/payload/payload.config.ts, src/app/(payload)/admin/[[...segments]]/page.tsx, src/app/(payload)/admin/[[...segments]]/not-found.tsx, src/app/(payload)/layout.tsx, package.json</files>
  <read_first>package.json, next.config.ts, src/app/layout.tsx</read_first>
  <action>
Install Payload CMS packages per RESEARCH:
```
npm install payload @payloadcms/db-postgres @payloadcms/richtext-lexical @payloadcms/next
```

Create `src/payload/payload.config.ts`:
- Use `buildConfig()` from `payload`
- Set `db: postgresAdapter({ pool: { connectionString: process.env.DATABASE_URL } })` — using same Neon Postgres
- Set `editor: lexicalEditor()` from `@payloadcms/richtext-lexical`
- Set `secret: process.env.PAYLOAD_SECRET` (add to .env.example)
- Import and register Products, Categories, Media collections (created in Task 2)
- Set `admin.user: 'users'` or create a simple payload-users collection
- Set `typescript.outputFile` to a generated types path

Create Payload admin route files per Payload 3.x Next.js App Router pattern:
- `src/app/(payload)/admin/[[...segments]]/page.tsx` — re-export from `@payloadcms/next/views`
- `src/app/(payload)/admin/[[...segments]]/not-found.tsx` — re-export NotFoundPage
- `src/app/(payload)/layout.tsx` — re-export RootLayout from `@payloadcms/next/layouts`

Update `next.config.ts` to include `withPayload` wrapper if required by Payload 3.x.

Add `PAYLOAD_SECRET` to `.env.example` with a placeholder value. Generate a random secret for `.env.local` if it exists.

IMPORTANT: Payload CMS manages its own tables via its internal Drizzle instance. Do NOT manually create Payload tables in the app's schema.ts. Let Payload handle its own migrations separately per RESEARCH pitfall #3.
  </action>
  <verify>
    <automated>cd /Users/kimjaechol/Documents/test && npx tsc --noEmit src/payload/payload.config.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Payload packages installed in package.json
    - payload.config.ts compiles with Postgres adapter pointing to DATABASE_URL
    - Admin route files exist at (payload)/admin path
    - PAYLOAD_SECRET in .env.example
  </acceptance_criteria>
  <done>Payload CMS installed and configured with Postgres adapter, admin route accessible</done>
</task>

<task type="auto">
  <name>Task 2: Create Products, Categories, and Media collections</name>
  <files>src/payload/collections/Products.ts, src/payload/collections/Categories.ts, src/payload/collections/Media.ts</files>
  <read_first>src/payload/payload.config.ts</read_first>
  <action>
Create `src/payload/collections/Categories.ts` per D-13:
- slug: 'categories'
- fields: name (text, required), slug (text, required, unique), description (textarea optional), sortOrder (number, default 0)
- admin.useAsTitle: 'name'
- Seed data concept (not auto-seeded, but document in comments): 운동화, 구두, 부츠, 샌들

Create `src/payload/collections/Media.ts`:
- slug: 'media'
- upload config: mimeTypes image/*, max file size ~5MB
- fields: alt (text), caption (text optional)
- imageSizes: thumbnail (300x300), card (600x400), detail (1200x800)

Create `src/payload/collections/Products.ts` per D-13, D-14, D-15, D-16:
- slug: 'products'
- admin.useAsTitle: 'name', defaultColumns: ['name', 'category', 'price', 'status']
- fields:
  - name (text, required)
  - slug (text, required, unique, admin.position sidebar)
  - description (richText via lexical editor)
  - category (relationship to 'categories', required) — per D-13
  - price (number, required, min 0) — base shoe price in KRW
  - bundleInsolePrice (number, min 0, admin.description "신발+인솔 세트 추가 가격") — per D-14
  - images (array of upload relationship to 'media') — per D-14
  - sizes (array): size (number, required), lastLength (number mm), lastWidth (number mm), stock (number, default 0) — per D-15
  - style (select: 'casual', 'formal', 'athletic', 'outdoor') — per D-16
  - status (select: 'draft', 'published', 'archived', default 'draft', admin.position sidebar)
  - brand (text optional)

Ensure all three collections are imported in payload.config.ts collections array.
  </action>
  <verify>
    <automated>cd /Users/kimjaechol/Documents/test && npx tsc --noEmit src/payload/collections/Products.ts src/payload/collections/Categories.ts src/payload/collections/Media.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Products collection has all fields per D-13/14/15/16: name, slug, description, category, price, bundleInsolePrice, images, sizes (with lastLength/lastWidth), style, status
    - Categories collection exists with name, slug fields
    - Media collection supports image uploads with resize presets
    - All collections registered in payload.config.ts
  </acceptance_criteria>
  <done>Products, Categories, Media collections defined with full field schemas per D-13 through D-16</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /admin | Payload admin panel access |
| admin -> database | Product data mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Elevation of Privilege | Payload admin | mitigate | Payload access control config with admin-only role; /admin route behind authentication |
| T-03-04 | Information Disclosure | Media uploads | mitigate | Validate mime types (image/* only), enforce file size limits |
</threat_model>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Payload config valid: Collections imported and registered
- Admin route exists at /admin path
</verification>

<success_criteria>
Payload CMS running with Products, Categories, Media collections. Admin can create/edit products with images, categories, sizes, and pricing. Product data queryable via Payload Local API.
</success_criteria>

<output>
After completion, create `.planning/phases/03-insole-design-product-catalog/03-02-SUMMARY.md`
</output>
