---
phase: 03-insole-design-product-catalog
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/lib/salted/ble-client.ts
  - src/lib/salted/store.ts
  - services/measurement/app/salted/data_parser.py
  - services/measurement/app/salted/biomechanical.py
  - services/measurement/app/salted/session_manager.py
  - services/measurement/app/api/salted.py
  - services/measurement/app/main.py
  - services/measurement/tests/test_biomechanical.py
  - services/measurement/tests/test_salted_session.py
autonomous: true
requirements: [SALT-01, SALT-02, SALT-03]

must_haves:
  truths:
    - "BLE client provides mockable adapter interface for SALTED connection"
    - "Mock data provider generates realistic 5-min walking session data"
    - "Biomechanical analysis extracts landing pattern, pronation, COP trajectory from pressure data"
    - "Session manager stores and retrieves SALTED walking sessions"
    - "API endpoints accept session data and return biomechanical analysis"
  artifacts:
    - path: "src/lib/salted/ble-client.ts"
      provides: "Web Bluetooth adapter with mock fallback"
      exports: ["SaltedBleClient", "MockSaltedProvider", "createSaltedClient"]
    - path: "src/lib/salted/store.ts"
      provides: "Zustand store for SALTED session state"
      exports: ["useSaltedStore"]
    - path: "services/measurement/app/salted/biomechanical.py"
      provides: "Biomechanical analysis from pressure data"
      exports: ["analyze_biomechanics"]
    - path: "services/measurement/app/salted/session_manager.py"
      provides: "SALTED session CRUD"
      exports: ["store_session", "get_session"]
  key_links:
    - from: "src/lib/salted/ble-client.ts"
      to: "src/lib/salted/types.ts"
      via: "imports SaltedPressureFrame, BleConnectionState"
      pattern: "import.*from.*salted/types"
    - from: "services/measurement/app/salted/biomechanical.py"
      to: "services/measurement/app/salted/models.py"
      via: "imports BiomechanicalAnalysis model"
      pattern: "from.*models import"
---

<objective>
Build the SALTED smart insole integration layer: BLE client adapter (with mock provider since SDK is unavailable), biomechanical analysis algorithms, session management, and API endpoints.

Purpose: Per the orchestrator note, SALTED SDK has no public documentation. Build with a mockable adapter interface so development/testing proceeds now, and real BLE integration activates when SDK access is obtained. Per D-05 through D-07.
Output: BLE adapter with mock, biomechanical analysis engine, session storage API
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-insole-design-product-catalog/03-01-SUMMARY.md
@.planning/phases/03-insole-design-product-catalog/03-RESEARCH.md

@src/lib/salted/types.ts
@services/measurement/app/salted/models.py
</context>

<interfaces>
From src/lib/salted/types.ts (Plan 01):
```typescript
export type BleConnectionState = 'disconnected' | 'connecting' | 'connected' | 'streaming' | 'error';
export interface SaltedPressureFrame {
  timestamp: number;
  pressureArray: number[];
  imuData: { accelX: number; accelY: number; accelZ: number; gyroX: number; gyroY: number; gyroZ: number } | null;
}
export interface BiomechanicalAnalysis {
  landingPattern: 'heel_strike' | 'midfoot_strike' | 'forefoot_strike';
  pronationDegree: number;
  copTrajectory: { x: number; y: number; t: number }[];
  archFlexibility: { staticIndex: number; dynamicIndex: number };
  weightDistribution: { forefoot: number; midfoot: number; hindfoot: number };
}
export interface SaltedSession { ... }
```

From services/measurement/app/salted/models.py (Plan 01):
```python
class SaltedPressureFrame(BaseModel): ...
class BiomechanicalAnalysis(BaseModel): ...
class SaltedSessionInput(BaseModel): ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: BLE client adapter with mock provider and Zustand store</name>
  <files>src/lib/salted/ble-client.ts, src/lib/salted/store.ts</files>
  <read_first>src/lib/salted/types.ts, src/lib/scan/store.ts</read_first>
  <action>
Create `src/lib/salted/ble-client.ts` — adapter interface with mock per D-05, D-06:

1. Define `SaltedAdapter` interface:
   - `connect(): Promise<void>` — connect to insole
   - `disconnect(): Promise<void>` — disconnect
   - `startSession(onFrame: (frame: SaltedPressureFrame) => void): Promise<void>` — start streaming
   - `stopSession(): Promise<SaltedPressureFrame[]>` — stop and return all frames
   - `getConnectionState(): BleConnectionState`
   - `isSupported(): boolean` — check if adapter can work in current environment

2. `WebBluetoothSaltedAdapter` class implementing SaltedAdapter:
   - connect(): use `navigator.bluetooth.requestDevice({ filters: [{ namePrefix: 'SALTED' }] })` per RESEARCH
   - Request Wake Lock during session per RESEARCH pitfall #6
   - Buffer frames in memory, chunk-upload to server every 10 seconds during session
   - isSupported(): check `navigator.bluetooth` existence
   - Mark with TODO comments for actual SALTED service UUIDs

3. `MockSaltedProvider` class implementing SaltedAdapter:
   - Generates realistic mock pressure data: 100Hz sampling, ~300K points over 5 minutes
   - Simulates walking pattern: alternating left/right foot pressure cycles
   - Includes mock IMU data (6-axis) with realistic walking acceleration patterns
   - Simulates heel strike -> midfoot -> toe-off pressure progression per gait cycle
   - connect()/disconnect() are instant, state transitions work correctly
   - isSupported(): always returns true

4. `createSaltedClient(useMock?: boolean): SaltedAdapter`:
   - Factory function: returns MockSaltedProvider if useMock=true or if WebBluetooth not available
   - Returns WebBluetoothSaltedAdapter otherwise

Create `src/lib/salted/store.ts` — Zustand store per existing scan store pattern:
- State: connectionState (BleConnectionState), sessionFrames (SaltedPressureFrame[]), sessionDuration (number), isRecording (boolean), analysis (BiomechanicalAnalysis | null), error (string | null)
- Actions: setConnectionState, addFrame, startRecording, stopRecording, setAnalysis, reset
- Do NOT persist to localStorage (transient session data, per Phase 2 pattern)
  </action>
  <verify>
    <automated>cd /Users/kimjaechol/Documents/test && npx tsc --noEmit src/lib/salted/ble-client.ts src/lib/salted/store.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - SaltedAdapter interface exported with connect/disconnect/startSession/stopSession/isSupported
    - MockSaltedProvider generates realistic 100Hz pressure data with walking patterns
    - WebBluetoothSaltedAdapter uses navigator.bluetooth with SALTED name filter
    - createSaltedClient factory returns mock when WebBluetooth unavailable
    - Zustand store manages SALTED session state without persistence
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>BLE adapter with mock/real implementations, Zustand store for session state, TypeScript compiles</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Biomechanical analysis engine, session manager, and API endpoints</name>
  <files>services/measurement/app/salted/data_parser.py, services/measurement/app/salted/biomechanical.py, services/measurement/app/salted/session_manager.py, services/measurement/app/api/salted.py, services/measurement/app/main.py, services/measurement/tests/test_biomechanical.py, services/measurement/tests/test_salted_session.py</files>
  <read_first>services/measurement/app/salted/models.py, services/measurement/app/gait/analyzer.py, services/measurement/app/api/scan.py</read_first>
  <behavior>
    - analyze_biomechanics with heel-dominant pressure pattern -> landing_pattern='heel_strike'
    - analyze_biomechanics with medial COP shift -> positive pronation_degree
    - analyze_biomechanics produces weight_distribution percentages summing to ~100
    - parse_pressure_frames converts raw BLE data format to SaltedPressureFrame list
    - store_session saves session with frame count validation
  </behavior>
  <action>
Create `services/measurement/app/salted/data_parser.py`:
- `parse_pressure_frames(raw_data: list[dict]) -> list[SaltedPressureFrame]`: Convert raw BLE frame dicts to validated Pydantic models
- `validate_session_data(frames: list[SaltedPressureFrame], min_duration_s: float = 240) -> tuple[bool, str]`: Validate session has enough data (at least 4 min of 5 min target)

Create `services/measurement/app/salted/biomechanical.py` per D-07:
1. `analyze_biomechanics(frames: list[SaltedPressureFrame]) -> BiomechanicalAnalysis`:
   - Landing pattern detection: analyze initial contact pressure distribution per gait cycle. Heel region >60% = heel_strike, forefoot >60% = forefoot_strike, else midfoot_strike
   - Pronation/supination: calculate COP medial-lateral deviation from center line. Positive = pronation, negative = supination, degrees
   - COP trajectory: extract center of pressure path over time as (x, y, t) sequence
   - Arch flexibility: compare static standing arch index vs dynamic walking arch index
   - Weight distribution: average forefoot/midfoot/hindfoot pressure percentages across all frames

2. `_detect_gait_cycles(frames: list[SaltedPressureFrame]) -> list[tuple[int, int]]`: Identify individual gait cycles (heel strike to heel strike) from pressure data

3. `_calculate_cop(pressure_array: list[float]) -> tuple[float, float]`: Center of pressure from pressure array

Create `services/measurement/app/salted/session_manager.py`:
- `store_session(session_input: SaltedSessionInput) -> str`: Store session to database (saltedSessions table), return session ID
- `get_session(session_id: str, user_id: str) -> dict`: Retrieve session with IDOR check (filter by user_id)
- `get_user_sessions(user_id: str) -> list[dict]`: List all sessions for user

Create `services/measurement/app/api/salted.py` with FastAPI router:
1. `POST /api/salted/session`: Accept SaltedSessionInput, store session, run biomechanical analysis, return session_id + analysis
2. `GET /api/salted/session/{session_id}`: Get session details (with user_id filter)
3. `POST /api/salted/analyze`: Accept session_id, run/re-run biomechanical analysis, return BiomechanicalAnalysis

Register salted router in `services/measurement/app/main.py`.

Write tests FIRST:
- `test_biomechanical.py`: Test landing pattern detection (heel/mid/forefoot), pronation calculation, weight distribution sums to ~100
- `test_salted_session.py`: Test session storage, validation (min duration), data parsing
  </action>
  <verify>
    <automated>cd /Users/kimjaechol/Documents/test/services/measurement && python -m pytest tests/test_biomechanical.py tests/test_salted_session.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Biomechanical analysis correctly classifies heel/mid/forefoot strike patterns
    - COP trajectory extraction produces valid (x,y,t) sequences
    - Weight distribution percentages sum to approximately 100
    - Session storage and retrieval works with user_id filtering (IDOR prevention)
    - All tests pass
    - API endpoints registered in main.py
  </acceptance_criteria>
  <done>Biomechanical analysis engine with tests, session CRUD with IDOR prevention, API endpoints registered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE device -> browser | Pressure data from potentially spoofed BLE device |
| browser -> API | Session data upload from client |
| API -> DB | Session storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Spoofing | BLE data | mitigate | Server-side validation of pressure data ranges (0-1000 kPa); flag anomalous readings |
| T-03-12 | Tampering | session_id IDOR | mitigate | All session queries filtered by user_id from auth (same as Phase 2 scan IDOR pattern) |
| T-03-13 | Denial of Service | Session upload | mitigate | Limit frame count to 500K max; validate duration <= 600s; rate limit session creation |
</threat_model>

<verification>
- Tests pass: `python -m pytest tests/test_biomechanical.py tests/test_salted_session.py -x -v`
- TypeScript compiles: `npx tsc --noEmit src/lib/salted/ble-client.ts src/lib/salted/store.ts`
- API routes registered in main.py
</verification>

<success_criteria>
SALTED integration layer complete with mockable adapter, biomechanical analysis from pressure data, session storage with IDOR prevention. Mock provider enables full development/testing without physical hardware.
</success_criteria>

<output>
After completion, create `.planning/phases/03-insole-design-product-catalog/03-05-SUMMARY.md`
</output>
