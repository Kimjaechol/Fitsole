---
phase: 03-insole-design-product-catalog
plan: 07
type: execute
wave: 3
depends_on: [03-03, 03-04, 03-05]
files_modified:
  - services/measurement/app/insole/report_generator.py
  - services/measurement/tests/test_report.py
  - src/app/insole/salted/page.tsx
  - src/components/insole/before-after-report.tsx
  - src/components/insole/salted-session-ui.tsx
  - src/app/api/insole/design/route.ts
  - src/app/api/salted/session/route.ts
autonomous: false
requirements: [SALT-04, SALT-06]

must_haves:
  truths:
    - "SALTED measurement page allows connecting device, running 5-min session, viewing results"
    - "Precision insole design uses SALTED real pressure data instead of AI estimation"
    - "Before/after report compares pressure distribution with/without custom insole"
    - "Report shows peak pressure reduction and contact area increase metrics"
    - "Next.js API routes proxy insole design and SALTED session requests to Python backend"
  artifacts:
    - path: "services/measurement/app/insole/report_generator.py"
      provides: "Before/after verification report generation"
      exports: ["generate_verification_report"]
    - path: "src/app/insole/salted/page.tsx"
      provides: "SALTED measurement session page"
    - path: "src/components/insole/before-after-report.tsx"
      provides: "Before/after comparison visualization"
      exports: ["BeforeAfterReport"]
    - path: "src/app/api/insole/design/route.ts"
      provides: "Next.js proxy to Python insole design API"
    - path: "src/app/api/salted/session/route.ts"
      provides: "Next.js proxy to Python SALTED session API"
  key_links:
    - from: "src/app/insole/salted/page.tsx"
      to: "src/lib/salted/ble-client.ts"
      via: "uses createSaltedClient for BLE connection"
      pattern: "createSaltedClient"
    - from: "src/app/api/insole/design/route.ts"
      to: "services/measurement/app/api/insole.py"
      via: "HTTP proxy to Python backend"
      pattern: "fetch.*MEASUREMENT_API"
    - from: "services/measurement/app/insole/report_generator.py"
      to: "services/measurement/app/salted/session_manager.py"
      via: "fetches two sessions for comparison"
      pattern: "get_session"
---

<objective>
Complete the SALTED precision design flow: measurement session UI page, precision insole design using real pressure data, before/after verification report, and Next.js API proxy routes wiring frontend to Python backend.

Purpose: Per D-08 and D-09, Line 2 uses real SALTED pressure data for higher accuracy insole design and generates before/after reports showing improvement. This plan also wires the Next.js frontend to the Python backend via API proxy routes.
Output: SALTED session page, precision design, verification report, API proxy routes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-insole-design-product-catalog/03-03-SUMMARY.md
@.planning/phases/03-insole-design-product-catalog/03-04-SUMMARY.md
@.planning/phases/03-insole-design-product-catalog/03-05-SUMMARY.md

@src/lib/salted/ble-client.ts
@src/lib/salted/store.ts
@src/lib/salted/types.ts
@src/lib/insole/types.ts
@services/measurement/app/insole/optimizer.py
@services/measurement/app/salted/biomechanical.py
</context>

<interfaces>
From src/lib/salted/ble-client.ts (Plan 05):
```typescript
export interface SaltedAdapter {
  connect(): Promise<void>;
  disconnect(): Promise<void>;
  startSession(onFrame: (frame: SaltedPressureFrame) => void): Promise<void>;
  stopSession(): Promise<SaltedPressureFrame[]>;
  getConnectionState(): BleConnectionState;
  isSupported(): boolean;
}
export function createSaltedClient(useMock?: boolean): SaltedAdapter;
```

From src/lib/salted/types.ts (Plan 01):
```typescript
export interface VerificationReport {
  initialSessionId: string;
  verificationSessionId: string;
  peakPressureReduction: number;
  contactAreaIncrease: number;
  zoneComparisons: { zone: string; before: number; after: number; improvement: number }[];
}
```

From services/measurement/app/insole/optimizer.py (Plan 03):
```python
def generate_insole_design(input, measurements, pressure_data, gait_data) -> InsoleDesignResult
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Before/after verification report generator and API proxy routes</name>
  <files>services/measurement/app/insole/report_generator.py, services/measurement/tests/test_report.py, src/app/api/insole/design/route.ts, src/app/api/salted/session/route.ts</files>
  <read_first>services/measurement/app/salted/session_manager.py, services/measurement/app/salted/biomechanical.py, src/app/api/scan/route.ts</read_first>
  <behavior>
    - generate_verification_report with improved pressure -> peak_pressure_reduction > 0
    - generate_verification_report with larger contact area -> contact_area_increase > 0
    - Report produces zone_comparisons for all 5 zones
    - Report flags success when reduction >= 30% and area increase >= 40% per D-09
  </behavior>
  <action>
Create `services/measurement/app/insole/report_generator.py` per D-09:

1. `generate_verification_report(initial_session_id: str, verification_session_id: str, user_id: str) -> VerificationReport`:
   - Fetch both SALTED sessions from session_manager (with user_id IDOR check)
   - Compare pressure distributions:
     - Peak pressure reduction: (initial_peak - verification_peak) / initial_peak * 100
     - Contact area increase: (verification_area - initial_area) / initial_area * 100
   - Zone-by-zone comparison: for each of 5 zones (forefoot, midfoot, hindfoot, medial, lateral), compare average pressure
   - Target metrics per D-09: >=30% peak pressure reduction, >=40% contact area increase
   - Return VerificationReport model with all comparison data
   - Include success flag: True if both targets met

2. `_calculate_peak_pressure(frames: list) -> float`: Max pressure value across all frames
3. `_calculate_contact_area(frames: list) -> float`: Count of sensor points above threshold, normalized
4. `_compare_zones(initial_frames, verification_frames) -> list[dict]`: Per-zone pressure comparison

Write tests FIRST in `services/measurement/tests/test_report.py`:
- Test peak pressure reduction calculation
- Test contact area increase calculation
- Test zone comparison with known before/after data
- Test success flag logic (>=30% reduction AND >=40% area increase)

Create `src/app/api/insole/design/route.ts`:
- POST handler proxying to Python backend `${MEASUREMENT_API_URL}/api/insole/design`
- Extract userId from auth session, include in request
- Follow existing scan API proxy pattern (see src/app/api/scan/route.ts)
- Error handling: 401 if not authenticated, forward 4xx/5xx from Python

Create `src/app/api/salted/session/route.ts`:
- POST handler proxying to Python backend `${MEASUREMENT_API_URL}/api/salted/session`
- GET handler proxying to Python backend for session retrieval
- Extract userId from auth session, include in request
- IDOR prevention: always include userId in proxy request
  </action>
  <verify>
    <automated>cd /Users/kimjaechol/Documents/test/services/measurement && python -m pytest tests/test_report.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Verification report correctly calculates peak pressure reduction percentage
    - Verification report correctly calculates contact area increase percentage
    - Zone comparisons cover 5 zones with before/after/improvement values
    - Success flag true when both targets met (>=30% reduction, >=40% area increase) per D-09
    - Next.js API proxy routes forward requests to Python backend with auth userId
    - All tests pass
  </acceptance_criteria>
  <done>Before/after verification report generates correct metrics, API proxy routes connect frontend to backend</done>
</task>

<task type="auto">
  <name>Task 2: SALTED measurement session page and before/after report UI</name>
  <files>src/app/insole/salted/page.tsx, src/components/insole/salted-session-ui.tsx, src/components/insole/before-after-report.tsx</files>
  <read_first>src/lib/salted/ble-client.ts, src/lib/salted/store.ts, src/lib/salted/types.ts, src/components/insole/insole-preview-3d.tsx</read_first>
  <action>
Create `src/components/insole/salted-session-ui.tsx` per D-05, D-06:
- Props: onSessionComplete (session data callback)
- Uses createSaltedClient() from ble-client.ts
- Uses useSaltedStore() for state management
- UI flow:
  1. Connection step: "SALTED 인솔 연결" button -- calls adapter.connect()
     - Show connection state (연결 중... / 연결됨 / 오류)
     - Browser compatibility warning if Web Bluetooth not supported ("Chrome 브라우저에서만 지원됩니다")
  2. Session step: "측정 시작" button -- calls adapter.startSession()
     - Timer showing elapsed time (목표: 5분)
     - Live data counter showing received frames
     - Progress bar (0-300 seconds)
     - "측정 중지" button -- calls adapter.stopSession()
     - Keep screen on instruction: "측정 중 화면을 끄지 마세요"
  3. Results step: Show biomechanical analysis summary
     - Landing pattern: 뒤꿈치/중족부/전족부 착지
     - Pronation degree with visual indicator
     - Weight distribution pie/bar chart
     - "인솔 설계 요청" button to trigger precision design
- Uses shadcn/ui components (Button, Progress, Card, Badge)
- Korean text throughout
- Handles mock mode gracefully (shows "테스트 모드" badge)

Create `src/components/insole/before-after-report.tsx` per D-09:
- Props: report (VerificationReport)
- Layout:
  - Header: "맞춤 인솔 효과 분석 보고서"
  - Key metrics cards:
    - 최대 압력 감소: {peakPressureReduction}% (green if >=30%, yellow if <30%)
    - 접촉 면적 증가: {contactAreaIncrease}% (green if >=40%, yellow if <40%)
  - Zone comparison table: zone name (Korean), before value, after value, improvement %
  - Success/needs-improvement badge based on targets
  - Pressure heatmap comparison (two side-by-side heatmaps using existing pressure-heatmap component pattern)

Create `src/app/insole/salted/page.tsx`:
- Protected route (require auth)
- Client component (needs BLE and real-time state)
- Layout:
  - Page title: "SALTED 스마트 인솔 측정"
  - Subtitle: "전문가용 정밀 측정 -- 실제 보행 압력 데이터 기반 인솔 설계"
  - SaltedSessionUi component for measurement flow
  - After session complete:
    - Call POST /api/salted/session to store session and get analysis
    - Call POST /api/insole/design with line_type='professional' and SALTED data
    - Show InsolePreview3D with precision design results
    - If this is a verification session (user already has custom insole):
      - Call verification report API
      - Show BeforeAfterReport component
  - Navigation: back to /insole/design
  </action>
  <verify>
    <automated>cd /Users/kimjaechol/Documents/test && npx tsc --noEmit src/app/insole/salted/page.tsx src/components/insole/before-after-report.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - SALTED session page renders with BLE connection, timer, and results flow
    - Session UI shows real-time frame count and timer during 5-min recording
    - Biomechanical results displayed in Korean after session
    - Before/after report shows peak pressure reduction and contact area metrics per D-09
    - Zone comparisons displayed in table format
    - Mock mode indicated with badge when using MockSaltedProvider
    - TypeScript compiles
  </acceptance_criteria>
  <done>SALTED measurement session page with BLE flow, precision design trigger, and before/after verification report UI</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 3 -- insole design + product catalog</name>
  <action>
Verify the complete Phase 3 deliverable: optimization algorithms, CAD pipeline, SALTED integration with mock, product catalog with Payload CMS, 3D insole preview, before/after report.
  </action>
  <verify>
    1. Visit /products -- verify product listing page with category filters (운동화, 구두, 부츠, 샌들)
    2. Click a product -- verify detail page with images, bundle pricing, size recommendation
    3. Visit /insole/design -- verify 3D insole preview with zone-colored hardness (red to blue gradient)
    4. Verify hardness legend shows 5 zones with Korean labels and Shore A values
    5. Toggle pressure overlay on/off in 3D preview
    6. Visit /insole/salted -- verify SALTED connection flow (will use mock in dev)
    7. Run mock SALTED session -- verify timer, frame counter, biomechanical results
    8. Visit Payload admin at /admin -- verify product CRUD works
    9. Check Python tests: cd services/measurement and python -m pytest tests/ -x -v
    10. Check TypeScript: npx tsc --noEmit
  </verify>
  <done>All Phase 3 features visually verified and tests passing. Type "approved" or describe issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Next.js API -> Python backend | Proxy routes forward auth context |
| BLE device -> browser | Pressure data from SALTED insole |
| browser -> session API | Large session data upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Spoofing | Verification report | mitigate | Both session IDs validated against user_id; cannot compare sessions from different users |
| T-03-18 | Tampering | API proxy | mitigate | All proxy routes extract userId from auth session, never from request body |
| T-03-19 | Denial of Service | Session upload | mitigate | Frame count limit (500K), duration limit (600s) validated server-side |
</threat_model>

<verification>
- Python tests: `cd services/measurement && python -m pytest tests/ -x -v`
- TypeScript compiles: `npx tsc --noEmit`
- SALTED page renders with mock BLE flow
- Before/after report shows comparison metrics
- API proxy routes forward to Python backend
</verification>

<success_criteria>
SALTED precision design flow complete: BLE connection (mock), 5-min session, biomechanical analysis, precision insole design, before/after verification report. All API proxy routes wired. Full Phase 3 functionality verified visually.
</success_criteria>

<output>
After completion, create `.planning/phases/03-insole-design-product-catalog/03-07-SUMMARY.md`
</output>
