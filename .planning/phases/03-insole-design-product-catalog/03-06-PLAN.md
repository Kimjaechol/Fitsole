---
phase: 03-insole-design-product-catalog
plan: 06
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - src/app/products/page.tsx
  - src/app/products/[slug]/page.tsx
  - src/components/products/product-card.tsx
  - src/components/products/product-grid.tsx
  - src/components/products/product-filters.tsx
  - src/components/products/bundle-pricing.tsx
  - src/components/insole/insole-preview-3d.tsx
  - src/components/insole/hardness-legend.tsx
  - src/app/insole/design/page.tsx
  - src/components/insole/design-summary.tsx
  - src/lib/insole/store.ts
autonomous: true
requirements: [INSL-03, INSL-04, PROD-01, PROD-02, PROD-03, PROD-04, PROD-05]

must_haves:
  truths:
    - "User can browse shoes by category (운동화, 구두, 부츠, 샌들) on /products page"
    - "User can filter shoes by category, size, price, style"
    - "Product detail page shows multiple images, description, and shoe+insole bundle pricing"
    - "Product detail page shows insole customization preview with zone hardness if user has scan data"
    - "Product detail page shows size recommendation based on scan data"
    - "User can preview custom insole in 3D with zone-by-zone hardness color coding"
    - "Insole design page shows design parameters and 3D preview"
  artifacts:
    - path: "src/app/products/page.tsx"
      provides: "Product listing page with category browsing and filters"
    - path: "src/app/products/[slug]/page.tsx"
      provides: "Product detail page with images, bundle pricing, insole preview"
    - path: "src/components/insole/insole-preview-3d.tsx"
      provides: "R3F 3D insole model with zone-colored hardness"
      exports: ["InsolePreview3D"]
    - path: "src/components/products/product-filters.tsx"
      provides: "Filter UI for category, size, price, style"
      exports: ["ProductFilters"]
    - path: "src/components/products/bundle-pricing.tsx"
      provides: "Shoe + insole bundle pricing display"
      exports: ["BundlePricing"]
  key_links:
    - from: "src/app/products/page.tsx"
      to: "Payload CMS"
      via: "Payload Local API query"
      pattern: "getPayload|payload.find"
    - from: "src/app/products/[slug]/page.tsx"
      to: "src/components/insole/insole-preview-3d.tsx"
      via: "Renders insole preview with user's scan data"
      pattern: "InsolePreview3D"
    - from: "src/components/insole/insole-preview-3d.tsx"
      to: "src/lib/insole/types.ts"
      via: "imports VARIOSHORE_ZONES for color mapping"
      pattern: "VARIOSHORE_ZONES"
---

<objective>
Build the product catalog UI (browse, filter, detail pages) and insole design preview UI (3D insole with zone hardness, design summary page). Connect product pages to Payload CMS data and insole preview to design engine output.

Purpose: This delivers the user-facing experience per D-13 through D-18 — browsing shoes with bundle pricing, viewing scan-based insole previews on product pages, and previewing the custom insole design in 3D.
Output: Product listing/detail pages, insole 3D preview component, design results page
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-insole-design-product-catalog/03-02-SUMMARY.md
@.planning/phases/03-insole-design-product-catalog/03-03-SUMMARY.md

@src/lib/insole/types.ts
@src/lib/scan/types.ts
@src/components/scan/foot-model-3d.tsx
@src/payload/collections/Products.ts
</context>

<interfaces>
From src/lib/insole/types.ts (Plan 01):
```typescript
export const VARIOSHORE_ZONES = {
  archCore: { tempC: 190, shoreA: 92, color: '#ef4444' },
  heelCupWall: { tempC: 195, shoreA: 85, color: '#f97316' },
  heelCupFloor: { tempC: 200, shoreA: 75, color: '#eab308' },
  forefoot: { tempC: 210, shoreA: 65, color: '#22c55e' },
  toeArea: { tempC: 220, shoreA: 55, color: '#3b82f6' },
};
export interface InsoleDesign { ... }
export interface DesignParams { archHeight, heelCupDepth, evaCushionThickness, ... }
```

From src/components/scan/foot-model-3d.tsx (Phase 2):
```typescript
// Existing R3F 3D viewer with OrbitControls — adapt pattern for insole preview
```

From Payload CMS Products collection (Plan 02):
```typescript
// Products have: name, slug, description, category, price, bundleInsolePrice, images, sizes, style, status
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Product catalog pages — listing with filters and detail page</name>
  <files>src/app/products/page.tsx, src/app/products/[slug]/page.tsx, src/components/products/product-card.tsx, src/components/products/product-grid.tsx, src/components/products/product-filters.tsx, src/components/products/bundle-pricing.tsx</files>
  <read_first>src/payload/payload.config.ts, src/payload/collections/Products.ts, src/app/(main)/page.tsx, src/components/scan/foot-model-3d.tsx</read_first>
  <action>
Create `src/components/products/product-card.tsx` per D-13:
- Props: product (name, slug, price, bundleInsolePrice, images, category)
- Card layout using shadcn/ui Card component
- Product image (first from images array), name, category badge, price in KRW (formatted with Intl.NumberFormat('ko-KR'))
- Bundle price shown as "신발+인솔 세트: ₩{price + bundleInsolePrice}" if bundleInsolePrice > 0
- Link to /products/[slug]
- Mobile-friendly card with responsive image

Create `src/components/products/product-filters.tsx` per D-16:
- Category filter: buttons or select for 운동화, 구두, 부츠, 샌들 (from categories collection)
- Size filter: number range selector (common Korean sizes: 230-300)
- Price filter: range slider or preset ranges
- Style filter: select (casual, formal, athletic, outdoor)
- Uses URL search params for filter state (server-side filtering)
- Korean labels for all filters

Create `src/components/products/product-grid.tsx`:
- Responsive grid: 2 cols mobile, 3 cols tablet, 4 cols desktop
- Maps products to ProductCard components
- Empty state with Korean message "등록된 상품이 없습니다"

Create `src/components/products/bundle-pricing.tsx` per D-14:
- Props: shoePrice, bundleInsolePrice, hasScanData
- Shows: shoe-only price, bundle price (shoe + insole), savings calculation
- If user has scan data: "맞춤 인솔 추가 가능" badge
- If no scan data: "발 측정 후 맞춤 인솔 추가 가능" with link to /scan

Create `src/app/products/page.tsx` per PROD-01, PROD-02:
- Server component using Payload Local API to query products
- Accept searchParams for category, size, price range, style filters
- Pass filters to Payload query (where clause)
- Only show status='published' products
- Page title: "상품 카탈로그"
- Render ProductFilters + ProductGrid
- Korean text throughout

Create `src/app/products/[slug]/page.tsx` per PROD-03, PROD-04, PROD-05:
- Server component fetching product by slug via Payload Local API
- Generate metadata (title, description) from product data
- Layout:
  - Image gallery (swipeable on mobile) from product.images
  - Product name, category, description (rendered from richText)
  - BundlePricing component with shoe + insole prices per D-14
  - Size selector: grid of available sizes from product.sizes
  - Size recommendation: if user has scan data, highlight recommended size per INSL-04 (call size-recommend API via server action or client fetch)
  - Insole preview section: if user has scan data, show InsolePreview3D (from Task 2) with their design per PROD-05
  - "장바구니 담기" button (disabled for now — Phase 4)
- Mobile-first responsive layout
  </action>
  <verify>
    <automated>cd /Users/kimjaechol/Documents/test && npx tsc --noEmit src/app/products/page.tsx src/app/products/\\[slug\\]/page.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Product listing page renders with filter controls for category/size/price/style per D-16
    - Product cards show name, image, price, bundle price in KRW
    - Product detail page shows image gallery, description, sizes, bundle pricing per D-14
    - Size recommendation shown when user has scan data per INSL-04
    - All UI text in Korean
    - Responsive grid layout (2/3/4 columns)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Product catalog with listing, filtering, and detail pages — Korean UI with bundle pricing and size recommendations</done>
</task>

<task type="auto">
  <name>Task 2: 3D insole preview component and insole design results page</name>
  <files>src/components/insole/insole-preview-3d.tsx, src/components/insole/hardness-legend.tsx, src/components/insole/design-summary.tsx, src/app/insole/design/page.tsx, src/lib/insole/store.ts</files>
  <read_first>src/components/scan/foot-model-3d.tsx, src/components/scan/pressure-heatmap.tsx, src/lib/insole/types.ts, src/lib/scan/store.ts</read_first>
  <action>
Create `src/components/insole/insole-preview-3d.tsx` per D-17, D-18:
- R3F Canvas component showing insole model with zone-colored hardness
- Props: designParams (DesignParams), hardnessMap (Record<HardnessZone, VarioshoreTpuZone>), showPressureOverlay (boolean)
- Generate insole geometry procedurally using Three.js:
  - Foot-shaped outline using ExtrudeGeometry or LatheGeometry based on footLength/footWidth
  - 5 zone meshes with different colors from VARIOSHORE_ZONES:
    - archCore (medial midfoot) — red #ef4444
    - heelCupWall (heel perimeter) — orange #f97316
    - heelCupFloor (heel center) — yellow #eab308
    - forefoot (ball area) — green #22c55e
    - toeArea (toe box) — blue #3b82f6
  - Each zone as separate mesh with MeshStandardMaterial using zone color
  - transparent opacity=0.85 for depth perception
- OrbitControls from drei for rotate/zoom per D-18
- Toggle button to switch between zones view and pressure overlay per D-18
- Ambient + directional lighting
- Responsive: Canvas fills container width

Create `src/components/insole/hardness-legend.tsx` per D-17:
- Color legend showing 5 zones with:
  - Color swatch, zone name (Korean: 아치 코어, 힐컵 벽, 힐컵 바닥, 전족부, 발가락 영역)
  - Shore A hardness value
  - TPU temperature
- Vertical layout on mobile, horizontal on desktop
- Uses VARIOSHORE_ZONES constant for data

Create `src/components/insole/design-summary.tsx`:
- Props: designParams (DesignParams), lineType ('general'|'professional')
- Display design parameters in a card layout:
  - 아치 높이: {archHeight}mm
  - 힐컵 깊이: {heelCupDepth}mm
  - EVA 쿠션: {evaCushionThickness}mm
  - Line type badge: "카메라 측정" or "SALTED 정밀 측정"
- Download links for STL and slicer profile (if available)

Create `src/lib/insole/store.ts` — Zustand store:
- State: currentDesign (InsoleDesign | null), isGenerating (boolean), error (string | null), showPressureOverlay (boolean)
- Actions: setDesign, setGenerating, setError, togglePressureOverlay, reset
- No persistence (transient design state)

Create `src/app/insole/design/page.tsx` per INSL-03:
- Protected route (require auth)
- Fetch user's latest scan data from API
- If scan exists: call insole design API to get/generate design
- Layout:
  - Left/top: InsolePreview3D with user's design params
  - Right/bottom: DesignSummary + HardnessLegend
  - Toggle button for pressure overlay per D-18
- If no scan: redirect to /scan with message "발 측정을 먼저 진행해주세요"
- Loading state while design generates
- Korean UI throughout
  </action>
  <verify>
    <automated>cd /Users/kimjaechol/Documents/test && npx tsc --noEmit src/components/insole/insole-preview-3d.tsx src/app/insole/design/page.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - InsolePreview3D renders R3F canvas with 5 color-coded zones per D-17
    - Zones use correct VARIOSHORE_ZONES colors (92A red to 55A blue gradient)
    - OrbitControls enable rotate/zoom per D-18
    - Toggle between zones view and pressure overlay works per D-18
    - HardnessLegend shows all 5 zones with Korean labels, Shore A values, temperatures
    - Design summary displays all parameters in Korean
    - Insole design page fetches scan data and generates design
    - TypeScript compiles
  </acceptance_criteria>
  <done>3D insole preview with zone hardness visualization, design results page with parameter summary, all in Korean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payload API -> browser | Product data served to client |
| browser -> design API | Design generation request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Information Disclosure | Product listing | accept | Product catalog is public data, no auth needed for browsing |
| T-03-15 | Tampering | Design page | mitigate | Insole design page requires auth; design API validates scan belongs to user |
| T-03-16 | Spoofing | Size recommendation | mitigate | Size rec based on server-side scan data, not client-provided dimensions |
</threat_model>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Products page renders with filter controls
- Product detail page shows bundle pricing
- Insole 3D preview shows color-coded zones
- Design page shows parameters and 3D model
</verification>

<success_criteria>
Complete product catalog browsable by category with filters. Product detail pages show bundle pricing and insole preview for scanned users. 3D insole preview renders with zone-specific hardness coloring. Design results page shows all parameters with Korean labels.
</success_criteria>

<output>
After completion, create `.planning/phases/03-insole-design-product-catalog/03-06-SUMMARY.md`
</output>
