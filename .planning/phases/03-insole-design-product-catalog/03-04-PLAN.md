---
phase: 03-insole-design-product-catalog
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - services/measurement/app/insole/scad_generator.py
  - services/measurement/app/insole/stl_exporter.py
  - services/measurement/app/insole/slicer_profile.py
  - services/measurement/app/insole/templates/insole_base.scad
  - services/measurement/tests/test_stl_export.py
  - services/measurement/Dockerfile
autonomous: true
requirements: [SALT-05]

must_haves:
  truths:
    - "Python generates valid .scad file from design parameters via Jinja2 template"
    - "OpenSCAD CLI converts .scad to .stl successfully (or mock validates the pipeline)"
    - "PrusaSlicer profile JSON generated with zone-specific temperature settings per D-03"
    - "Output includes STL path, slicer profile path, and design parameters JSON per D-12"
  artifacts:
    - path: "services/measurement/app/insole/scad_generator.py"
      provides: "Jinja2-based .scad file generation"
      exports: ["generate_scad_file"]
    - path: "services/measurement/app/insole/stl_exporter.py"
      provides: "OpenSCAD CLI invocation for STL export"
      exports: ["export_stl"]
    - path: "services/measurement/app/insole/slicer_profile.py"
      provides: "PrusaSlicer profile generation"
      exports: ["generate_slicer_profile"]
    - path: "services/measurement/app/insole/templates/insole_base.scad"
      provides: "Parametric OpenSCAD insole template"
  key_links:
    - from: "services/measurement/app/insole/scad_generator.py"
      to: "services/measurement/app/insole/templates/insole_base.scad"
      via: "Jinja2 template rendering"
      pattern: "env.get_template"
    - from: "services/measurement/app/insole/stl_exporter.py"
      to: "OpenSCAD CLI"
      via: "subprocess.run"
      pattern: "subprocess.run.*openscad"
---

<objective>
Build the parametric CAD pipeline: Python generates OpenSCAD .scad files from design parameters via Jinja2 templates, invokes OpenSCAD CLI to export STL, and generates PrusaSlicer profiles with zone-specific Varioshore TPU temperature settings.

Purpose: This is the manufacturing output pipeline per D-10, D-11, D-12 — converting design parameters into physical manufacturing files (STL for 3D printing, slicer profile for temperature control).
Output: Working .scad template, STL export function, slicer profile generator
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-insole-design-product-catalog/03-01-SUMMARY.md
@.planning/phases/03-insole-design-product-catalog/03-RESEARCH.md

@services/measurement/app/insole/models.py
</context>

<interfaces>
From services/measurement/app/insole/models.py (Plan 01):
```python
class DesignParams(BaseModel):
    arch_height: float  # 25-60mm
    heel_cup_depth: float  # 15-35mm
    eva_cushion_thickness: float
    foot_length: float
    foot_width: float
    heel_width: float
    forefoot_flex: float
    medial_post_h: float
    lateral_post_h: float

VARIOSHORE_ZONES = {
    "arch_core": {"temp_c": 190, "shore_a": 92, ...},
    "heel_cup_wall": {"temp_c": 195, "shore_a": 85, ...},
    ...
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OpenSCAD template and .scad generator</name>
  <files>services/measurement/app/insole/templates/insole_base.scad, services/measurement/app/insole/scad_generator.py, services/measurement/tests/test_stl_export.py</files>
  <read_first>services/measurement/app/insole/models.py, services/measurement/requirements.txt</read_first>
  <behavior>
    - generate_scad_file(DesignParams) returns valid .scad file content as string
    - Generated .scad contains all DesignParams as OpenSCAD variables
    - Template renders without Jinja2 errors for any valid DesignParams
    - Output .scad file is syntactically valid (no unclosed brackets)
  </behavior>
  <action>
Install Jinja2 if not already in requirements.txt: `pip install jinja2` and add to requirements.txt.

Create `services/measurement/app/insole/templates/insole_base.scad` — a parametric OpenSCAD template per D-10:

The template should use Jinja2 variable syntax `{{ var }}` for all design parameters:
- Variables: arch_height, heel_cup_depth, eva_cushion_thickness, foot_length, foot_width, heel_width, forefoot_flex, medial_post_h, lateral_post_h
- Set `$fn = {{ resolution | default(50) }};` for facet count
- Model the insole as:
  1. Base plate: hull() of foot outline (ellipse-based using foot_length, foot_width)
  2. Heel cup: cylinder section at heel end with heel_cup_depth and heel_width
  3. Arch support: raised section in medial midfoot area with arch_height
  4. EVA cushion layer: uniform thickness below base
  5. Medial/lateral posts: raised edges for pronation correction
  6. Forefoot flex zone: thinner material in forefoot (controlled by forefoot_flex)
- Use `difference()`, `hull()`, `translate()`, `scale()` for geometry
- Comment each section clearly for maintainability

Create `services/measurement/app/insole/scad_generator.py`:
- `generate_scad_file(params: DesignParams, resolution: int = 50) -> str`:
  - Load `insole_base.scad` template via Jinja2 Environment with FileSystemLoader
  - Render with all DesignParams fields + resolution
  - Return rendered string
- `save_scad_file(params: DesignParams, output_path: Path, resolution: int = 50) -> Path`:
  - Generate and write to file
  - Return output path

Write tests FIRST in `services/measurement/tests/test_stl_export.py`:
- Test that generate_scad_file produces non-empty string
- Test that all DesignParams variables appear in output
- Test with edge-case params (min/max values)
- Test template rendering doesn't raise Jinja2 errors
  </action>
  <verify>
    <automated>cd /Users/kimjaechol/Documents/test/services/measurement && python -m pytest tests/test_stl_export.py -x -v -k "scad" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - insole_base.scad template exists with parameterized OpenSCAD geometry
    - generate_scad_file renders template with all DesignParams variables
    - Tests pass for template rendering with various parameter sets
    - Jinja2 in requirements.txt
  </acceptance_criteria>
  <done>OpenSCAD template renders correctly with all design parameters, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: STL exporter and PrusaSlicer profile generator</name>
  <files>services/measurement/app/insole/stl_exporter.py, services/measurement/app/insole/slicer_profile.py, services/measurement/Dockerfile, services/measurement/tests/test_stl_export.py</files>
  <read_first>services/measurement/app/insole/scad_generator.py, services/measurement/Dockerfile</read_first>
  <action>
Create `services/measurement/app/insole/stl_exporter.py` per D-10:

1. `export_stl(scad_content: str, output_dir: Path, filename: str = "insole") -> Path`:
   - Write scad_content to temp .scad file in output_dir
   - Run `subprocess.run(["openscad", "-o", str(stl_path), str(scad_path)], capture_output=True, text=True, timeout=120)`
   - If returncode != 0, raise RuntimeError with stderr
   - Clean up temp .scad file
   - Return stl_path
   - Handle FileNotFoundError for openscad binary with clear error message

2. `generate_insole_stl(params: DesignParams, output_dir: Path, resolution: int = 50) -> Path`:
   - Call generate_scad_file(params, resolution)
   - Call export_stl with result
   - Return STL path

3. `is_openscad_available() -> bool`:
   - Check if openscad binary is in PATH
   - Return False if not found (for graceful degradation in dev without OpenSCAD)

Create `services/measurement/app/insole/slicer_profile.py` per D-11, D-12:

1. `generate_slicer_profile(params: DesignParams, hardness_map: dict) -> dict`:
   - Generate PrusaSlicer config as dict with:
     - Global settings: layer_height=0.2, infill=20%, material=Varioshore TPU
     - Zone modifiers: for each zone in hardness_map, create modifier config with zone-specific temperature, flow_rate_percent
     - Comments documenting Shore A hardness per zone
   - Return config dict

2. `save_slicer_profile(params: DesignParams, hardness_map: dict, output_path: Path) -> Path`:
   - Generate and save as JSON file
   - Return path

3. `generate_design_output(params: DesignParams, hardness_map: dict, output_dir: Path) -> dict`:
   - Orchestrate full output per D-12: generate STL (if OpenSCAD available, else skip with warning), slicer profile JSON, design parameters JSON
   - Return dict with {stl_path, slicer_profile_path, design_params_json_path} — any may be None if generation failed

Update `services/measurement/Dockerfile`:
- Add `RUN apt-get update && apt-get install -y openscad xvfb` per RESEARCH pitfall #1
- Ensure openscad is available in the Docker image for production STL generation

Add tests to `test_stl_export.py`:
- Test slicer profile contains all 5 zones with correct temperatures from D-03
- Test STL export with mock subprocess (mock openscad binary for CI)
- Test is_openscad_available returns bool
- Test generate_design_output returns all three file paths
  </action>
  <verify>
    <automated>cd /Users/kimjaechol/Documents/test/services/measurement && python -m pytest tests/test_stl_export.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - STL exporter calls OpenSCAD CLI with correct arguments
    - Graceful handling when OpenSCAD not installed (returns error, doesn't crash)
    - Slicer profile contains all 5 Varioshore zones with temperatures matching D-03
    - Design output bundle produces STL + slicer profile + design params JSON per D-12
    - Dockerfile includes openscad + xvfb installation
    - All tests pass (with mocked subprocess for OpenSCAD)
  </acceptance_criteria>
  <done>STL export pipeline and slicer profile generation working, Dockerfile updated, tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python -> OpenSCAD subprocess | Rendered .scad file passed to external binary |
| Generated files -> storage | STL and profile files written to filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | OpenSCAD input | mitigate | All .scad content generated from validated DesignParams via Jinja2 template; no user-provided .scad code |
| T-03-09 | Denial of Service | STL generation | mitigate | subprocess timeout=120s; $fn capped at 100 for production |
| T-03-10 | Information Disclosure | STL file paths | mitigate | UUID-based filenames, served via signed URLs not filesystem paths |
</threat_model>

<verification>
- Tests pass: `cd services/measurement && python -m pytest tests/test_stl_export.py -x -v`
- Template renders: generate_scad_file produces valid OpenSCAD syntax
- Dockerfile updated with openscad
</verification>

<success_criteria>
Complete CAD pipeline from design parameters to STL + slicer profile + JSON output. OpenSCAD template renders parametric insole geometry. Slicer profile contains correct Varioshore TPU temperatures per zone.
</success_criteria>

<output>
After completion, create `.planning/phases/03-insole-design-product-catalog/03-04-SUMMARY.md`
</output>
