---
phase: 01-foundation-accounts
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/app/(main)/mypage/page.tsx
  - src/components/profile/profile-tabs.tsx
  - src/components/profile/my-info-tab.tsx
  - src/components/profile/foot-profile-tab.tsx
  - src/components/profile/order-history-tab.tsx
  - src/components/empty-state.tsx
  - src/components/__tests__/foot-profile-tab.test.tsx
  - src/components/__tests__/order-history-tab.test.tsx
  - src/components/__tests__/bottom-tab-bar.test.tsx
autonomous: false
requirements:
  - ACCT-03
  - ACCT-04
  - ACCT-05
must_haves:
  truths:
    - "Profile page has 3 tabs: 내 정보, 발 프로필, 주문 내역"
    - "발 프로필 tab shows empty state with CTA to start foot measurement"
    - "주문 내역 tab shows empty state with CTA to browse products"
    - "내 정보 tab displays user email and name from session"
    - "Profile page requires authentication (redirects to login if not logged in)"
    - "Database schema is pushed to Neon Postgres and tables exist"
  artifacts:
    - path: "src/components/profile/profile-tabs.tsx"
      provides: "Tab container for profile page"
      contains: "내 정보"
    - path: "src/components/empty-state.tsx"
      provides: "Reusable empty state component"
      contains: "Lucide"
    - path: "src/components/profile/foot-profile-tab.tsx"
      provides: "Foot scan placeholder tab"
      contains: "아직 발 측정을 하지 않았어요"
    - path: "src/components/profile/order-history-tab.tsx"
      provides: "Order history placeholder tab"
      contains: "아직 주문 내역이 없어요"
  key_links:
    - from: "src/app/(main)/mypage/page.tsx"
      to: "src/lib/auth.ts"
      via: "auth() server-side session check"
      pattern: "auth\\(\\)"
    - from: "src/components/profile/my-info-tab.tsx"
      to: "session.user"
      via: "displays user email and name"
      pattern: "session.*user"
---

<objective>
Build the profile page with 3 tabs and empty states, push database schema to Neon Postgres, and run a final verification checkpoint.

Purpose: Complete the user profile experience (ACCT-03, ACCT-04, ACCT-05) and ensure the database is ready for real data. The checkpoint verifies the entire phase works end-to-end.
Output: Working profile page with tabs and empty states, database tables created, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-accounts/01-UI-SPEC.md
@.planning/phases/01-foundation-accounts/01-RESEARCH.md
@.planning/phases/01-foundation-accounts/01-01-SUMMARY.md
@.planning/phases/01-foundation-accounts/01-02-SUMMARY.md
@.planning/phases/01-foundation-accounts/01-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- auth and schema -->
From src/lib/auth.ts:
```typescript
export const { handlers, auth, signIn, signOut } = NextAuth({...});
// auth() returns session with session.user.id, session.user.email, session.user.name
```

From src/lib/db/schema.ts:
```typescript
export const users = pgTable("users", { id, name, email, hashedPassword, ... });
```

<!-- From Plan 02 -- layout -->
From src/app/(main)/layout.tsx:
- Main layout with BottomTabBar and DesktopNav
- Content area with max-w-[1280px] and appropriate padding

shadcn/ui components available: tabs, card, button, avatar, separator
Lucide icons for empty states: ScanLine (foot scan), ShoppingBag (orders), User (profile)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build profile page with 3 tabs, empty states, and remaining tests</name>
  <files>
    src/app/(main)/mypage/page.tsx,
    src/components/profile/profile-tabs.tsx,
    src/components/profile/my-info-tab.tsx,
    src/components/profile/foot-profile-tab.tsx,
    src/components/profile/order-history-tab.tsx,
    src/components/empty-state.tsx,
    src/components/__tests__/foot-profile-tab.test.tsx,
    src/components/__tests__/order-history-tab.test.tsx,
    src/components/__tests__/bottom-tab-bar.test.tsx
  </files>
  <read_first>
    src/lib/auth.ts,
    src/app/(main)/layout.tsx,
    .planning/phases/01-foundation-accounts/01-UI-SPEC.md
  </read_first>
  <action>
    1. Create src/components/empty-state.tsx (per D-11):
       - Reusable component accepting props: icon (LucideIcon), title (string), body (string), ctaText (string), ctaHref (string)
       - Layout: centered, flex-col items-center gap-4
       - Icon: 64px Lucide icon in muted color (#64748B)
       - Title: 24px/heading, bold
       - Body: 16px/body, muted color (#64748B)
       - CTA button: shadcn Button, accent color bg-[#2563EB], links to ctaHref using Next.js Link
       - All spacing per UI-SPEC spacing scale

    2. Create src/components/profile/my-info-tab.tsx:
       - Receives session user data (name, email) as props
       - Display user avatar (shadcn Avatar with first character of name or User icon fallback)
       - Display name and email in a card
       - "계정 삭제" button at bottom: destructive style (text-[#DC2626], outline)
         - On click: show confirmation dialog with "정말 계정을 삭제하시겠습니까? 모든 데이터가 영구적으로 삭제되며 복구할 수 없습니다."
         - Confirm button: "삭제하기" (destructive), Cancel: "취소"
         - Account deletion API is NOT implemented in v1 -- show toast "현재 지원되지 않는 기능입니다" if confirmed
       - "로그아웃" button: calls signOut() from next-auth/react

    3. Create src/components/profile/foot-profile-tab.tsx (per D-11, ACCT-03):
       - Uses EmptyState component with:
         - icon: ScanLine (Lucide)
         - title: "아직 발 측정을 하지 않았어요"
         - body: "스마트폰 카메라로 간편하게 발을 측정해 보세요"
         - ctaText: "발 측정 시작하기"
         - ctaHref: "/scan"

    4. Create src/components/profile/order-history-tab.tsx (per D-11, ACCT-04, ACCT-05):
       - Uses EmptyState component with:
         - icon: ShoppingBag (Lucide)
         - title: "아직 주문 내역이 없어요"
         - body: "맞춤 인솔과 신발을 둘러보세요"
         - ctaText: "상품 둘러보기"
         - ctaHref: "/catalog"

    5. Create src/components/profile/profile-tabs.tsx (per D-10):
       - Uses shadcn Tabs component
       - 3 tabs: "내 정보", "발 프로필", "주문 내역"
       - Default active tab: "내 정보"
       - Tab switching: instant swap, no animation (per UI-SPEC interaction patterns)
       - Renders MyInfoTab, FootProfileTab, OrderHistoryTab in respective tab panels
       - Receives session data as prop to pass to MyInfoTab

    6. Create src/app/(main)/mypage/page.tsx:
       - Server component that calls `auth()` from src/lib/auth.ts
       - If no session: redirect to /login using next/navigation redirect()
       - If session exists: render ProfileTabs with session.user data
       - Page heading: "마이페이지" (but could also be omitted since tabs serve as navigation)

    7. Create test files:
       a. src/components/__tests__/foot-profile-tab.test.tsx:
          - Test: renders empty state title "아직 발 측정을 하지 않았어요"
          - Test: renders CTA button "발 측정 시작하기"
          - Test: CTA links to "/scan"

       b. src/components/__tests__/order-history-tab.test.tsx:
          - Test: renders empty state title "아직 주문 내역이 없어요"
          - Test: renders CTA button "상품 둘러보기"
          - Test: CTA links to "/catalog"

       c. src/components/__tests__/bottom-tab-bar.test.tsx:
          - Test: renders 4 tab labels (홈, 상품, 측정, 마이)
          - Test: renders links to /, /catalog, /scan, /mypage
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/empty-state.tsx` contains props for `icon` and `title` and `body` and `ctaText` and `ctaHref`
    - `src/components/profile/foot-profile-tab.tsx` contains `아직 발 측정을 하지 않았어요` and `발 측정 시작하기` and `/scan`
    - `src/components/profile/order-history-tab.tsx` contains `아직 주문 내역이 없어요` and `상품 둘러보기` and `/catalog`
    - `src/components/profile/profile-tabs.tsx` contains `내 정보` and `발 프로필` and `주문 내역`
    - `src/components/profile/my-info-tab.tsx` contains `계정 삭제` and `로그아웃` and `signOut`
    - `src/app/(main)/mypage/page.tsx` contains `auth()` and `redirect` and `ProfileTabs`
    - All test files pass: foot-profile-tab, order-history-tab, bottom-tab-bar
  </acceptance_criteria>
  <done>Profile page renders with 3 tabs. Foot profile tab shows empty state with CTA to /scan. Order history tab shows empty state with CTA to /catalog. My info tab shows user data with logout and account deletion. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push database schema to Neon Postgres</name>
  <files>
    drizzle.config.ts,
    src/lib/db/schema.ts
  </files>
  <read_first>
    drizzle.config.ts,
    src/lib/db/schema.ts,
    .env.local
  </read_first>
  <action>
    1. Verify DATABASE_URL in .env.local points to a valid Neon Postgres instance.
       If DATABASE_URL is still placeholder: STOP and inform user they need to:
       - Create a Neon account at https://neon.tech
       - Create a new project
       - Copy the connection string to .env.local DATABASE_URL

    2. Push schema to database using Drizzle Kit:
       ```bash
       npx drizzle-kit push
       ```
       If interactive prompt appears, try:
       ```bash
       echo "yes" | npx drizzle-kit push
       ```
       If that fails, try the non-interactive approach:
       ```bash
       npx drizzle-kit generate
       npx drizzle-kit migrate
       ```

    3. Verify tables were created by checking drizzle output for:
       - users table created
       - accounts table created
       - sessions table created
       - verification_tokens table created

    NOTE: This task requires a valid DATABASE_URL. If the env var is not set or is a placeholder,
    this task MUST pause and request the user to configure the database connection.
  </action>
  <verify>
    <automated>npx drizzle-kit push 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - drizzle-kit push completes without errors
    - Output mentions users, accounts, sessions, verification_tokens tables
    - No connection errors in output
  </acceptance_criteria>
  <done>Database schema is pushed to Neon Postgres. All 4 Auth.js tables (users, accounts, sessions, verification_tokens) exist in the database with correct columns including hashedPassword on users.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 end-to-end</name>
  <files>none -- verification only</files>
  <action>
    Human verifies the complete Phase 1 implementation end-to-end.

    What was built:
    1. Next.js 16 app with Pretendard Korean font, Tailwind CSS, shadcn/ui
    2. Landing page with 4 Korean-language sections (Hero CTA, Value Columns, Process Preview, Bottom CTA)
    3. Mobile bottom tab bar (4 tabs) and desktop top nav
    4. Auth system: login, signup, password reset forms with Zod validation
    5. Profile page with 3 tabs (내 정보, 발 프로필, 주문 내역) and empty states
    6. Database schema pushed to Neon Postgres

    How to verify -- start the dev server: `npm run dev`

    **1. Landing page (UIUX-02, UIUX-03):**
    - Visit http://localhost:3000
    - Verify: Korean headline "당신의 발에 꼭 맞는 인솔, 과학이 설계합니다" displays correctly
    - Verify: Primary CTA button "내 발에 맞는 신발 찾기" is prominent and blue
    - Verify: 3 value columns display (정밀 발 측정, 맞춤 인솔 설계, 완벽한 착용감)
    - Verify: Process preview shows 3 steps (발 측정, 인솔 설계, 배송 완료)
    - Verify: Bottom CTA section has blue background with "지금 시작하기"

    **2. Mobile responsiveness (UIUX-01):**
    - Open Chrome DevTools, toggle mobile view (iPhone 14 Pro)
    - Verify: Bottom tab bar appears with 4 tabs (홈, 상품, 측정, 마이)
    - Verify: 측정 (scan) tab has visually differentiated icon
    - Switch to desktop view: verify bottom tab bar disappears, top nav appears

    **3. Signup flow (ACCT-01):**
    - Visit http://localhost:3000/signup
    - Verify: Form shows name, email, password, confirm password fields
    - Submit with mismatched passwords: verify Korean error "비밀번호가 일치하지 않습니다"
    - Submit with valid data: verify success toast "가입이 완료되었습니다!"

    **4. Login flow (ACCT-02):**
    - Visit http://localhost:3000/login
    - Login with the account just created
    - Verify: Redirects to home page
    - Refresh the browser: verify session persists (still logged in)

    **5. Profile page (ACCT-03, ACCT-04):**
    - Navigate to 마이 tab (or visit http://localhost:3000/mypage)
    - Verify: 3 tabs display (내 정보, 발 프로필, 주문 내역)
    - 발 프로필 tab: verify empty state "아직 발 측정을 하지 않았어요" with CTA
    - 주문 내역 tab: verify empty state "아직 주문 내역이 없어요" with CTA
    - 내 정보 tab: verify user email and name display correctly
  </action>
  <verify>Human visual and functional verification</verify>
  <done>All 5 verification areas pass. User confirms landing page, mobile responsiveness, signup, login, and profile page work correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> MyPage | Authenticated user data displayed |
| Server -> Neon DB | Schema push creates tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | /mypage | mitigate | Server-side auth() check with redirect to /login if no session |
| T-04-02 | Information Disclosure | my-info-tab | mitigate | Only display data from authenticated session; no cross-user data access |
| T-04-03 | Tampering | Account deletion | accept | Deletion not implemented in v1; button shows "currently unsupported" toast |
</threat_model>

<verification>
- `npx next build` compiles without errors
- `npx vitest run` passes all tests (auth validation, landing page, foot profile, order history, bottom tab bar)
- Database tables exist in Neon Postgres
- Full user flow works: landing -> signup -> login -> profile -> tabs -> empty states
</verification>

<success_criteria>
- Profile page renders with 3 tabs and correct empty states
- Foot scan empty state shows CTA linking to /scan
- Order history empty state shows CTA linking to /catalog
- Database schema is pushed successfully
- All vitest tests pass
- Human verifies full end-to-end flow works
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-accounts/01-04-SUMMARY.md`
</output>
