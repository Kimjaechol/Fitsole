---
phase: 01-foundation-accounts
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(main)/layout.tsx
  - src/app/(main)/page.tsx
  - src/app/(main)/catalog/page.tsx
  - src/app/(main)/scan/page.tsx
  - src/components/layout/bottom-tab-bar.tsx
  - src/components/layout/desktop-nav.tsx
  - src/components/landing/hero-section.tsx
  - src/components/landing/value-columns.tsx
  - src/components/landing/process-preview.tsx
  - src/components/landing/bottom-cta.tsx
autonomous: true
requirements:
  - UIUX-01
  - UIUX-02
  - UIUX-03
must_haves:
  truths:
    - "Landing page displays 4 sections: Hero with CTA, Value Columns, Process Preview, Bottom CTA"
    - "All UI text is in Korean with no English fallbacks"
    - "Bottom tab bar shows on mobile with 4 tabs (홈/상품/측정/마이)"
    - "Desktop nav shows on screens >= 768px, bottom tab bar hides"
    - "Layout is max-width 1280px centered on desktop"
    - "Scan tab is visually differentiated from other tabs"
  artifacts:
    - path: "src/app/(main)/layout.tsx"
      provides: "Main layout with bottom tab bar + desktop nav"
      contains: "BottomTabBar"
    - path: "src/app/(main)/page.tsx"
      provides: "Landing page with 4 sections"
      contains: "HeroSection"
    - path: "src/components/layout/bottom-tab-bar.tsx"
      provides: "Mobile bottom navigation"
      contains: "홈"
    - path: "src/components/landing/hero-section.tsx"
      provides: "Hero section with primary CTA"
      contains: "내 발에 맞는 신발 찾기"
  key_links:
    - from: "src/app/(main)/layout.tsx"
      to: "src/components/layout/bottom-tab-bar.tsx"
      via: "import and render"
      pattern: "BottomTabBar"
    - from: "src/app/(main)/page.tsx"
      to: "src/components/landing/hero-section.tsx"
      via: "import and render"
      pattern: "HeroSection"
---

<objective>
Build the app shell (mobile-first layout with bottom tab bar and desktop nav) and the Korean-language landing page with all 4 sections.

Purpose: Create the visual foundation and navigation structure that users interact with. This is the first thing users see -- the landing page communicates FitSole's value proposition.
Output: Working landing page with responsive navigation, all Korean copy from UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-accounts/01-UI-SPEC.md
@.planning/phases/01-foundation-accounts/01-RESEARCH.md
@.planning/phases/01-foundation-accounts/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- root layout and utilities the executor needs -->
From src/app/layout.tsx:
- Root layout with Pretendard font, lang="ko"
- Font variable: --font-pretendard

From src/lib/utils.ts:
- cn() utility for className merging

From globals.css custom properties:
- --background: #FFFFFF
- --secondary: #F8FAFC
- --accent: #2563EB
- --accent-secondary: #059669
- --destructive: #DC2626
- --border: #E2E8F0
- --muted: #64748B

shadcn/ui components available: button, input, card, tabs, label, separator, avatar, sonner
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build bottom tab bar, desktop nav, and main layout shell</name>
  <files>
    src/components/layout/bottom-tab-bar.tsx,
    src/components/layout/desktop-nav.tsx,
    src/app/(main)/layout.tsx,
    src/app/(main)/catalog/page.tsx,
    src/app/(main)/scan/page.tsx
  </files>
  <read_first>
    src/app/layout.tsx,
    src/lib/utils.ts,
    .planning/phases/01-foundation-accounts/01-UI-SPEC.md
  </read_first>
  <action>
    1. Create src/components/layout/bottom-tab-bar.tsx (per D-09, UIUX-01):
       - Fixed position bottom, 56px height (44px touch target + 12px safe area per UI-SPEC)
       - 4 tabs: 홈 (Home icon), 상품 (ShoppingBag icon), 측정 (ScanLine icon), 마이 (User icon) -- using Lucide React icons
       - Active tab: text-[#2563EB] (accent), inactive: text-[#64748B] (muted)
       - Scan tab (측정): visually differentiated with accent background circle (bg-[#2563EB] rounded-full p-2 with white icon) per UI-SPEC interaction patterns
       - Tap feedback: active:scale-95 transition-transform duration-100
       - Use Next.js Link component for navigation: /, /catalog, /scan, /mypage
       - Use `usePathname()` from next/navigation to determine active tab
       - Hidden on desktop: `md:hidden` (>= 768px)
       - Include `pb-[env(safe-area-inset-bottom)]` for iPhone home indicator
       - Client component ("use client")

    2. Create src/components/layout/desktop-nav.tsx (per D-12):
       - Hidden on mobile: `hidden md:flex`
       - Horizontal top navigation bar with same 4 items
       - Max-width 1280px centered (per D-12)
       - Background: white with bottom border (#E2E8F0)
       - Active link: text-[#2563EB], inactive: text-[#64748B]
       - Use Next.js Link component and usePathname() for active state
       - Include FitSole logo/text on the left
       - Client component ("use client")

    3. Create src/app/(main)/layout.tsx:
       - Import and render DesktopNav at top
       - Import and render BottomTabBar at bottom
       - Main content area: `max-w-[1280px] mx-auto` on desktop (per D-12)
       - Content padding: `px-4` mobile, `md:px-6` tablet, `lg:px-8` desktop (per UI-SPEC)
       - Bottom padding on mobile: `pb-[72px] md:pb-0` to prevent content hiding behind tab bar (per RESEARCH pitfall 4)

    4. Create placeholder pages:
       - src/app/(main)/catalog/page.tsx: Simple Korean placeholder "상품 카탈로그 (준비 중)" -- for Phase 3
       - src/app/(main)/scan/page.tsx: Simple Korean placeholder "발 측정 (준비 중)" -- for Phase 2
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/bottom-tab-bar.tsx` contains `홈` and `상품` and `측정` and `마이` and `md:hidden` and `56px` or `h-14`
    - `src/components/layout/desktop-nav.tsx` contains `hidden md:flex` and `max-w-[1280px]` and `FitSole`
    - `src/app/(main)/layout.tsx` contains `BottomTabBar` and `DesktopNav` and `pb-[72px]` or `pb-18` and `max-w-[1280px]`
    - `src/app/(main)/catalog/page.tsx` contains `준비 중`
    - `src/app/(main)/scan/page.tsx` contains `준비 중`
    - Bottom tab bar uses Lucide icons: Home, ShoppingBag, ScanLine, User
    - Scan tab has accent background circle differentiation
  </acceptance_criteria>
  <done>App shell with responsive navigation is complete. Bottom tab bar renders on mobile with 4 tabs, desktop nav renders on wider screens. Layout is 1280px max-width centered. Placeholder pages exist for catalog and scan.</done>
</task>

<task type="auto">
  <name>Task 2: Build landing page with 4 sections using Korean copy from UI-SPEC</name>
  <files>
    src/components/landing/hero-section.tsx,
    src/components/landing/value-columns.tsx,
    src/components/landing/process-preview.tsx,
    src/components/landing/bottom-cta.tsx,
    src/app/(main)/page.tsx,
    src/components/__tests__/landing-page.test.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-accounts/01-UI-SPEC.md,
    src/app/(main)/layout.tsx
  </read_first>
  <action>
    1. Create src/components/landing/hero-section.tsx (per D-01, D-02, D-03):
       - Full viewport height on mobile: `min-h-screen` or `min-h-dvh`
       - Vertically centered content (flexbox)
       - Headline: "당신의 발에 꼭 맞는 인솔, 과학이 설계합니다" (32px/display, bold, per UI-SPEC)
       - Subtext: "스마트폰으로 발을 측정하고, 맞춤 인솔로 편안함을 경험하세요" (16px/body)
       - Primary CTA button: "내 발에 맞는 신발 찾기" (per D-01)
         - Use shadcn Button component
         - bg-[#2563EB] text-white, 48px height, font-bold
         - This is THE primary visual anchor of the entire application (per UI-SPEC visual hierarchy)
       - Clean white background (per D-02)

    2. Create src/components/landing/value-columns.tsx (per D-03):
       - 3-column grid on desktop (`lg:grid-cols-3`), single column stack on mobile
       - Use shadcn Card component for each column
       - Card backgrounds: #F8FAFC (secondary color)
       - Column 1: icon (Ruler or ScanLine) + "정밀 발 측정" + "스마트폰 카메라로 발의 길이, 볼넓이, 아치를 정확히 측정합니다"
       - Column 2: icon (Pencil or Layers) + "맞춤 인솔 설계" + "AI 기반 알고리즘이 당신만을 위한 인솔을 설계합니다"
       - Column 3: icon (Shield or CheckCircle) + "완벽한 착용감" + "90일 만족 보장, 맞지 않으면 무료 재제작해 드립니다"
       - Icons in emerald-600 (#059669, accent-secondary) per UI-SPEC
       - Section padding: 48px vertical (2xl spacing token)

    3. Create src/components/landing/process-preview.tsx (per D-03):
       - Horizontal 3-step flow on desktop with connecting lines, vertical on mobile
       - Step 1: "발 측정" with foot/scan icon
       - Step 2: "인솔 설계" with design/layers icon
       - Step 3: "배송 완료" with truck/package icon
       - Icons in emerald-600 (#059669)
       - Connecting lines/arrows between steps
       - Section padding: 48px vertical

    4. Create src/components/landing/bottom-cta.tsx (per D-03):
       - Accent background section: bg-[#2563EB] (per UI-SPEC visual hierarchy #2)
       - White text, centered layout
       - CTA button: "지금 시작하기" -- white bg on blue section, or outline white
       - Padding: 64px vertical (3xl spacing)

    5. Create src/app/(main)/page.tsx:
       - Import and render all 4 sections in order: HeroSection, ValueColumns, ProcessPreview, BottomCTA
       - Wrap in Framer Motion for fadeIn page transition: opacity 0->1, 200ms ease-out (per UI-SPEC interaction patterns)
       - No English text anywhere

    6. Create src/components/__tests__/landing-page.test.tsx:
       - Test: Landing page renders hero headline "당신의 발에 꼭 맞는 인솔"
       - Test: Landing page renders primary CTA "내 발에 맞는 신발 찾기"
       - Test: Landing page renders all 3 value column titles
       - Test: Landing page renders all 3 process steps
       - Test: Landing page renders bottom CTA "지금 시작하기"
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/landing-page.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/landing/hero-section.tsx` contains `내 발에 맞는 신발 찾기` and `당신의 발에 꼭 맞는 인솔, 과학이 설계합니다` and `min-h-screen` or `min-h-dvh`
    - `src/components/landing/value-columns.tsx` contains `정밀 발 측정` and `맞춤 인솔 설계` and `완벽한 착용감` and `lg:grid-cols-3`
    - `src/components/landing/process-preview.tsx` contains `발 측정` and `인솔 설계` and `배송 완료`
    - `src/components/landing/bottom-cta.tsx` contains `지금 시작하기` and `bg-[#2563EB]` or `bg-accent`
    - `src/app/(main)/page.tsx` contains `HeroSection` and `ValueColumns` and `ProcessPreview` and `BottomCTA`
    - Landing page test file passes with at least 4 assertions
    - No English text in any user-facing component
  </acceptance_criteria>
  <done>Landing page displays all 4 sections with correct Korean copy from UI-SPEC. Hero CTA is the primary visual anchor. Value columns show 3 benefits. Process preview shows 3 steps. Bottom CTA provides secondary conversion point. All copy is Korean. Implements UIUX-02 and UIUX-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan is UI-only with no data processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Landing page | accept | No sensitive data on landing page; all content is public marketing copy |
</threat_model>

<verification>
- `npx next build` compiles without errors
- `npx vitest run` passes landing page tests
- Landing page displays 4 Korean-language sections
- Bottom tab bar shows on mobile viewport, hides on desktop
- Desktop nav shows on desktop, hides on mobile
</verification>

<success_criteria>
- Landing page renders with Hero CTA, Value Columns, Process Preview, Bottom CTA
- All UI text is in Korean (no English fallbacks)
- Bottom tab bar has 4 tabs with Lucide icons, scan tab visually differentiated
- Desktop nav renders at >= 768px with max-width 1280px
- Content has appropriate bottom padding to avoid tab bar overlap on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-accounts/01-02-SUMMARY.md`
</output>
