---
phase: 02-foot-scanning
plan: 08
type: execute
wave: 1
depends_on: [02-07]
gap_closure: true
files_modified:
  - src/app/(main)/scan/new/page.tsx
  - src/app/api/scan/process/route.ts
  - src/app/api/scan/upload/route.ts
  - src/lib/scan/upload.ts
autonomous: true
requirements: [SCAN-08, SCAN-09, SCAN-10]
must_haves:
  truths:
    - "After both videos upload, frontend calls POST /api/scan/process to trigger orchestration"
    - "Gait video is uploaded separately to /api/scan/upload with type=gait metadata and forwarded to Python /gait/analyze as multipart UploadFile"
    - "Pressure estimation call includes weight, gender, and age collected from user in scan flow"
    - "Complete flow works end-to-end: scan -> upload foot video -> upload gait video -> process -> results"
  artifacts:
    - path: "src/app/(main)/scan/new/page.tsx"
      provides: "Biometric input collection (weight/gender/age) and POST /api/scan/process trigger after uploads"
    - path: "src/app/api/scan/process/route.ts"
      provides: "Fixed gait call (multipart UploadFile) and pressure call (includes weight/gender/age)"
    - path: "src/app/api/scan/upload/route.ts"
      provides: "Routes gait video uploads to /gait/analyze instead of /scan/process"
    - path: "src/lib/scan/upload.ts"
      provides: "uploadGaitVideo function with type metadata distinction"
  key_links:
    - from: "src/app/(main)/scan/new/page.tsx"
      to: "/api/scan/process"
      via: "fetch POST after both uploads complete"
      pattern: "api/scan/process"
    - from: "src/app/api/scan/upload/route.ts"
      to: "Python /gait/analyze"
      via: "multipart form proxy when type=gait"
      pattern: "gait/analyze"
    - from: "src/app/api/scan/process/route.ts"
      to: "Python /pressure/estimate"
      via: "JSON body with weight, gender, age from DB"
      pattern: "weight.*gender.*age"
---

<objective>
Close 3 integration gaps from 02-VERIFICATION.md that block SCAN-08, SCAN-09, SCAN-10:

1. **Orphaned orchestration** — /api/scan/process is never called from frontend. Fix: frontend triggers it after uploads complete.
2. **Gait API mismatch** — /api/scan/process sends JSON to /gait/analyze but Python expects UploadFile. Fix: upload route distinguishes gait video and proxies to /gait/analyze as multipart; process route reads gait result from DB instead of calling gait endpoint directly.
3. **Missing biometric inputs** — /pressure/estimate requires weight/gender/age but process route omits them. Fix: collect biometrics in scan flow step 4 before processing, pass to process route, forward to pressure endpoint.

Purpose: Without these fixes, gait analysis and pressure estimation never execute — the scan flow produces SfM measurements only.
Output: 4 modified files that complete the end-to-end pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-foot-scanning/02-CONTEXT.md
@.planning/phases/02-foot-scanning/02-VERIFICATION.md

@src/lib/scan/types.ts
@src/lib/scan/upload.ts
@src/lib/scan/store.ts
@src/lib/db/schema.ts
@src/app/(main)/scan/new/page.tsx
@src/app/(main)/scan/processing/[id]/page.tsx
@src/app/api/scan/upload/route.ts
@src/app/api/scan/process/route.ts
@services/measurement/app/api/gait.py
@services/measurement/app/api/pressure.py

<interfaces>
<!-- Python endpoint contracts (the source of truth for API calls) -->

From services/measurement/app/api/gait.py:
```python
@router.post("/analyze")
async def analyze_gait(
    video: UploadFile,   # <-- MUST be multipart file upload, NOT JSON
    scanId: str,         # <-- form field
):
    # Returns: { scanId, gaitPattern, ankleAlignment, archFlexibilityIndex }
```

From services/measurement/app/api/pressure.py:
```python
class PressureEstimateRequest(BaseModel):
    scanId: str
    weight: float        # <-- REQUIRED, gt=0, le=300 (kg)
    footLength: float    # <-- REQUIRED
    ballWidth: float     # <-- REQUIRED
    archHeight: float    # <-- REQUIRED
    gender: str          # <-- REQUIRED: 'male', 'female', 'other'
    age: float           # <-- REQUIRED, gt=0, le=150
```

From src/lib/db/schema.ts (relevant columns):
```typescript
// gaitAnalysis table has: walkingVideoUrl (text)
// pressureDistribution table has: inputWeight (real), inputGender (text), inputAge (real)
// footScans table has: videoUrl (text) — stores foot video URL
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix upload routing and add gait video upload function</name>
  <files>src/lib/scan/upload.ts, src/app/api/scan/upload/route.ts</files>
  <read_first>src/lib/scan/upload.ts, src/app/api/scan/upload/route.ts, services/measurement/app/api/gait.py</read_first>
  <action>
**Modify `src/lib/scan/upload.ts`** — Add a separate `uploadGaitVideo` export:
- Copy the `uploadScanVideo` function signature and TUS config (same chunk size, retry delays)
- Change the TUS metadata to include `type: 'gait'` alongside filename, filetype, scanId
- This metadata field is how the upload route distinguishes foot video from gait video
- Keep the existing `uploadScanVideo` unchanged (it works correctly for foot video)

**Modify `src/app/api/scan/upload/route.ts`** — Route gait videos to `/gait/analyze`:
- After extracting formData, also read a `type` field: `formData.get("type") as string | null`
- If `type === "gait"`:
  - Build a new FormData with the video file as field name `video` (matching Python's `UploadFile` parameter name) and `scanId` as a text field
  - POST to `${MEASUREMENT_SERVICE_URL}/gait/analyze` with the multipart FormData body (no Content-Type header — let fetch set the multipart boundary)
  - On success, parse the JSON response and save gait results to the `gaitAnalysis` DB table: `{ scanId, gaitPattern, ankleAlignment, archFlexibilityIndex, walkingVideoUrl: videoUrl }`
  - Update footScans.processingStage to `'analyzing_gait'` before the call, then to `'estimating_pressure'` after success
  - Return `{ scanId, status: "gait_complete" }`
- Else (default / `type !== "gait"`):
  - Keep existing behavior: proxy to `/scan/process` for SfM processing
  - This path is already working correctly

Do NOT change the auth check, IDOR prevention, MIME validation, or file size validation — those apply to both paths.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
  - grep -q "uploadGaitVideo" src/lib/scan/upload.ts
  - grep -q "type.*gait" src/lib/scan/upload.ts
  - grep -q "gait/analyze" src/app/api/scan/upload/route.ts
  - grep -q "gaitAnalysis" src/app/api/scan/upload/route.ts
  - grep -q "video.*formData\|backendForm.*video" src/app/api/scan/upload/route.ts
  </acceptance_criteria>
  <done>Upload route distinguishes foot vs gait video by type metadata. Gait video is proxied to Python /gait/analyze as multipart UploadFile. Gait results saved to DB. Existing foot video upload path unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Fix process route and wire frontend trigger with biometric collection</name>
  <files>src/app/api/scan/process/route.ts, src/app/(main)/scan/new/page.tsx</files>
  <read_first>src/app/api/scan/process/route.ts, src/app/(main)/scan/new/page.tsx, src/lib/db/schema.ts, services/measurement/app/api/pressure.py</read_first>
  <action>
**Modify `src/app/api/scan/process/route.ts`** — Fix the two broken API calls:

1. **Remove the direct gait/analyze call entirely** (lines 146-166 in current file). The gait analysis is now handled by the upload route (Task 1) when `type=gait`. Instead, after SfM processing succeeds, check if gait results already exist in the DB for this scanId:
   ```
   const existingGait = await db.select().from(gaitAnalysis).where(eq(gaitAnalysis.scanId, scanId)).limit(1);
   ```
   If no gait results exist yet, skip the gait stage (it will be populated by the gait upload). Update processingStage to `'estimating_pressure'`.

2. **Fix the pressure/estimate call** (lines 174-198 in current file):
   - Expand the `processRequestSchema` to accept optional biometric fields: `weight: z.number().positive().max(300).optional()`, `gender: z.enum(["male", "female", "other"]).optional()`, `age: z.number().positive().max(150).optional()`
   - When calling `${MEASUREMENT_SERVICE_URL}/pressure/estimate`, include weight, gender, age in the JSON body alongside scanId, footLength, ballWidth, archHeight
   - If weight/gender/age are not provided in the request body, try to read them from the pressureDistribution table (in case they were saved from a previous attempt) or skip pressure estimation with a warning log
   - Save the biometric inputs to pressureDistribution table: `inputWeight`, `inputGender`, `inputAge`

Keep ALL existing logic: auth check, IDOR prevention, status guards (completed/processing), SfM call, error handling, status updates.

**Modify `src/app/(main)/scan/new/page.tsx`** — Add biometric input and process trigger:

1. **Add biometric collection UI in step 4** (before the "결과 보기" button):
   - Add state variables: `weight` (number), `gender` ('male'|'female'|'other'), `age` (number)
   - Render a simple form with 3 inputs in Korean:
     - 체중 (kg): number input, placeholder "예: 65", min=1 max=300
     - 성별: 3 buttons (남성/여성/기타) that set gender to male/female/other
     - 나이: number input, placeholder "예: 30", min=1 max=150
   - Style with existing Tailwind patterns (same as other scan UI components)
   - These fields are required before "결과 보기" can be tapped — disable the button until all 3 are filled
   - Add a brief label above the form: "압력 분석을 위해 추가 정보가 필요합니다" (per D-09: pressure estimation requires weight, foot size, arch height, gender, age)

2. **Modify `handleFinish`** to trigger processing before navigating:
   - After both uploads are complete (step 4 is only reachable when both are done), call `POST /api/scan/process` with:
     ```
     fetch('/api/scan/process', {
       method: 'POST',
       headers: { 'Content-Type': 'application/json' },
       body: JSON.stringify({
         scanId: scanIdRef.current,
         footSide: currentFoot,
         weight: parseFloat(weight),
         gender,
         age: parseFloat(age),
       }),
     })
     ```
   - Show a loading state on the button ("처리 시작 중..." with disabled state) while the fetch is in progress
   - On success: navigate to `/scan/processing/${scanId}` (existing behavior)
   - On error: show an error toast or set an error state with Korean message "처리를 시작할 수 없습니다. 다시 시도해 주세요."
   - Do NOT await the full processing — just trigger it. The processing page polls status.

3. **Use `uploadGaitVideo` for step 3 walking video**:
   - Import `uploadGaitVideo` from `@/lib/scan/upload`
   - In `handleRecordingComplete`, when `step === 3`, call `uploadGaitVideo` instead of `uploadScanVideo`
   - This ensures the walking video gets the `type: 'gait'` metadata and routes to `/gait/analyze`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
  - grep -q "api/scan/process" src/app/(main)/scan/new/page.tsx
  - grep -q "weight" src/app/(main)/scan/new/page.tsx
  - grep -q "gender" src/app/(main)/scan/new/page.tsx
  - grep -q "age" src/app/(main)/scan/new/page.tsx
  - grep -q "uploadGaitVideo" src/app/(main)/scan/new/page.tsx
  - grep -q "weight" src/app/api/scan/process/route.ts
  - grep -q "gender" src/app/api/scan/process/route.ts
  - grep -q "age" src/app/api/scan/process/route.ts
  - grep -q "inputWeight\|inputGender\|inputAge" src/app/api/scan/process/route.ts
  - grep -rn "api/scan/process" src/app/ | grep -v route.ts | grep -q "."
  </acceptance_criteria>
  <done>Frontend collects weight/gender/age in step 4, triggers POST /api/scan/process with biometrics after uploads complete. Process route passes biometrics to Python /pressure/estimate. Gait video uses uploadGaitVideo for correct routing. The orphaned orchestration route is now called from the frontend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Next.js /api/scan/process | User-provided biometric data (weight, gender, age) crosses trust boundary |
| Next.js -> Python /gait/analyze | Video file proxied as multipart upload |
| Next.js -> Python /pressure/estimate | Biometric data forwarded to ML model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Tampering | process/route.ts biometric inputs | mitigate | Zod validation on weight (0-300), age (0-150), gender enum; Python Pydantic re-validates |
| T-02-19 | Information Disclosure | Biometric data (weight/gender/age) | mitigate | Biometrics only transit Next.js->Python on localhost; stored in DB with same auth protection as scan data; Python logs suppress PII (T-02-09 in pressure.py) |
| T-02-20 | Spoofing | Gait video upload | mitigate | Existing auth + IDOR checks in upload route apply to gait path; MIME type validation unchanged |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `grep -rn "api/scan/process" src/app/` shows at least one match in scan/new/page.tsx (no longer orphaned)
3. `grep "gait/analyze" src/app/api/scan/upload/route.ts` confirms gait routing exists
4. `grep "weight.*gender.*age\|gender.*weight\|age.*weight" src/app/api/scan/process/route.ts` confirms biometrics are forwarded
5. `grep "uploadGaitVideo" src/app/(main)/scan/new/page.tsx` confirms gait video uses separate upload function
</verification>

<success_criteria>
- /api/scan/process is called from scan/new/page.tsx after uploads complete (gap 1 closed)
- Walking video reaches Python /gait/analyze as multipart UploadFile via upload route (gap 2 closed)
- Weight, gender, age collected in scan flow and forwarded to /pressure/estimate (gap 3 closed)
- TypeScript compilation passes
- No changes to Python backend (contracts are correct, only Next.js callers were wrong)
- No changes to DB schema (columns already exist: walkingVideoUrl, inputWeight, inputGender, inputAge)
</success_criteria>

<output>
After completion, create `.planning/phases/02-foot-scanning/02-08-SUMMARY.md`
</output>
