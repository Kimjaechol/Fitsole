---
phase: 02-foot-scanning
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/scan/scan-stepper.tsx
  - src/components/scan/camera-viewfinder.tsx
  - src/components/scan/recording-button.tsx
  - src/components/scan/guidance-overlay.tsx
  - src/components/scan/circular-guide.tsx
  - src/components/scan/a4-paper-guide.tsx
  - src/components/scan/walking-guide.tsx
  - src/components/scan/quality-badge.tsx
  - src/components/scan/rescan-prompt.tsx
  - src/components/scan/scan-onboarding.tsx
  - src/components/scan/foot-side-selector.tsx
  - src/lib/scan/camera.ts
autonomous: true
requirements: [SCAN-01, SCAN-03]
must_haves:
  truths:
    - "Camera viewfinder opens fullscreen with back camera on smartphone"
    - "Recording button starts/stops video recording with 3-second countdown"
    - "Real-time guidance overlay shows blur/lighting warnings during recording"
    - "Circular guide SVG animates to show camera trajectory during foot scan"
    - "A4 paper guide shows rectangle overlay for foot placement"
    - "Scan stepper shows 4-step progress (dots + labels)"
    - "Onboarding carousel displays on first visit only"
    - "Quality badge shows good/fair/poor with correct colors"
  artifacts:
    - path: "src/components/scan/camera-viewfinder.tsx"
      provides: "Fullscreen camera component with getUserMedia"
      contains: "getUserMedia"
    - path: "src/components/scan/recording-button.tsx"
      provides: "72px recording button with states"
      contains: "72"
    - path: "src/components/scan/scan-stepper.tsx"
      provides: "4-step progress indicator"
      contains: "발 위치"
    - path: "src/lib/scan/camera.ts"
      provides: "Camera initialization and quality check helpers"
      exports: ["initCamera", "checkFrameQuality"]
  key_links:
    - from: "src/components/scan/camera-viewfinder.tsx"
      to: "src/lib/scan/camera.ts"
      via: "imports initCamera for getUserMedia setup"
      pattern: "import.*camera"
    - from: "src/components/scan/recording-button.tsx"
      to: "src/lib/scan/store.ts"
      via: "updates scan status on start/stop"
      pattern: "useScanStore"
---

<objective>
Build all scan flow UI components: camera viewfinder, recording controls, guidance overlays, step progress, onboarding, and quality feedback. These are the building blocks for the scan flow pages (wired in Plan 05).

Purpose: Isolate component creation from page composition. Each component is independently testable.
Output: 11 scan UI components + camera utility library
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-foot-scanning/02-CONTEXT.md
@.planning/phases/02-foot-scanning/02-UI-SPEC.md
@.planning/phases/02-foot-scanning/02-RESEARCH.md

@src/lib/scan/types.ts
@src/lib/scan/store.ts
@src/components/ui/button.tsx
@src/components/ui/card.tsx

<interfaces>
From src/lib/scan/types.ts (created in Plan 01):
- ScanStatus: 'idle' | 'onboarding' | 'positioning' | 'recording_foot' | 'recording_gait' | 'uploading' | 'processing' | 'results' | 'error'
- FootSide: 'left' | 'right'
- QualityWarning: { type, message }

From src/lib/scan/store.ts (created in Plan 01):
- useScanStore(): { scanSession, currentStatus, qualityWarnings, isOnboarded, startScan(), setStep(), setStatus(), addQualityWarning(), markOnboarded(), ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and create camera utility + core scan components</name>
  <files>src/lib/scan/camera.ts, src/components/scan/scan-stepper.tsx, src/components/scan/camera-viewfinder.tsx, src/components/scan/recording-button.tsx, src/components/scan/guidance-overlay.tsx, src/components/scan/circular-guide.tsx, src/components/scan/a4-paper-guide.tsx</files>
  <read_first>src/lib/scan/types.ts, src/lib/scan/store.ts, .planning/phases/02-foot-scanning/02-UI-SPEC.md, src/components/ui/button.tsx</read_first>
  <action>
**First, install required shadcn/ui components:**
```bash
npx shadcn@latest add progress dialog alert badge toggle skeleton -y
```

**Create `src/lib/scan/camera.ts`** per RESEARCH code example:
- `initCamera()`: Call navigator.mediaDevices.getUserMedia with constraints: video { facingMode: { ideal: 'environment' }, width: { ideal: 1920 }, height: { ideal: 1080 }, frameRate: { ideal: 30 } }, audio: false. Per Pitfall 6, limit to 1080p/30fps. Return MediaStream.
- `checkFrameQuality(canvas: HTMLCanvasElement)`: Check brightness (average pixel luminance from getImageData, dark if < 50). Return { blur: false, dark: boolean }. Per RESEARCH, full blur detection is server-side with OpenCV Laplacian.
- `stopCamera(stream: MediaStream)`: Stop all tracks.
- Export all three functions.

**Create `src/components/scan/scan-stepper.tsx`** per UI-SPEC:
- 'use client' component
- Props: { currentStep: number (1-4), completedSteps: number[] }
- 4 steps with Korean labels: ['발 위치', '영상 촬영', '보행 촬영', '결과'] per UI-SPEC copywriting
- Horizontal row of dots: inactive = 12px slate-200, active = 16px accent (blue-600) with scale-up animation (200ms ease-out via Framer Motion or CSS transition), completed = 12px emerald-600 with checkmark (Lucide Check icon 8px)
- Labels below dots in label size (14px, text-slate-500)
- role="progressbar" with aria-valuenow/max per UI-SPEC accessibility

**Create `src/components/scan/camera-viewfinder.tsx`** per UI-SPEC:
- 'use client' component
- Props: { onFrame?: (canvas: HTMLCanvasElement) => void, children?: ReactNode }
- Fullscreen (100vh, 100vw) per UI-SPEC layout
- Initialize camera via initCamera() from camera.ts on mount
- Render video element with ref, autoplay, playsInline
- Dark overlay (rgba(0,0,0,0.5)) outside scan area per UI-SPEC interaction patterns
- Back arrow button (top-left, 44px touch target, Lucide ArrowLeft) per UI-SPEC
- Children slot for overlaying guides/buttons
- Cleanup: stop camera on unmount
- aria-label="발 촬영 화면" per UI-SPEC accessibility
- Handle NotAllowedError and NotFoundError with Korean error messages per UI-SPEC copywriting (카메라 접근이 거부되었습니다, 카메라를 사용할 수 없습니다)

**Create `src/components/scan/recording-button.tsx`** per UI-SPEC:
- 'use client' component
- Props: { isRecording: boolean, disabled?: boolean, onStart: () => void, onStop: () => void }
- 72px diameter circle per UI-SPEC component dimensions
- Idle: accent fill (bg-blue-600), white Video icon (Lucide)
- Recording: red-600 fill, white square stop icon, pulse animation (scale 1.0->1.05 at 1s interval via CSS animation) per UI-SPEC
- Disabled: 50% opacity
- Tap start: navigator.vibrate(50) if available per UI-SPEC
- aria-label="촬영 시작" / "촬영 중지" per UI-SPEC accessibility

**Create `src/components/scan/guidance-overlay.tsx`** per UI-SPEC:
- 'use client' component
- Props: { message: string, type: 'info' | 'warning' | 'error' }
- Semi-transparent text overlay on viewfinder, positioned top-center
- White text on semi-transparent dark bg per UI-SPEC visual hierarchy
- Warning type: amber-600 text
- Error type: red-600 text

**Create `src/components/scan/circular-guide.tsx`** per UI-SPEC:
- 'use client' component
- Props: { progress: number (0-100) }
- SVG circle (280px diameter per UI-SPEC) with dashed stroke, accent color at 30% opacity
- Animated dot traveling along circle path showing camera trajectory per UI-SPEC
- Progress arc fills as user completes motion (strokeDasharray/strokeDashoffset)
- Arc color: 30% opacity -> solid accent as progress increases

**Create `src/components/scan/a4-paper-guide.tsx`** per D-15, UI-SPEC:
- 'use client' component
- Props: { detected: boolean }
- SVG rectangle overlay matching A4 proportions (297:210 = 1.414 aspect ratio)
- Green border when detected=true, red border when detected=false per UI-SPEC
- Centered on viewfinder
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getUserMedia" src/lib/scan/camera.ts
    - grep -q "1920" src/lib/scan/camera.ts
    - grep -q "1080" src/lib/scan/camera.ts
    - grep -q "발 위치" src/components/scan/scan-stepper.tsx
    - grep -q "영상 촬영" src/components/scan/scan-stepper.tsx
    - grep -q "보행 촬영" src/components/scan/scan-stepper.tsx
    - grep -q "progressbar" src/components/scan/scan-stepper.tsx
    - grep -q "use client" src/components/scan/camera-viewfinder.tsx
    - grep -q "발 촬영 화면" src/components/scan/camera-viewfinder.tsx
    - grep -q "72" src/components/scan/recording-button.tsx
    - grep -q "촬영 시작" src/components/scan/recording-button.tsx
    - grep -q "vibrate" src/components/scan/recording-button.tsx
    - grep -q "280" src/components/scan/circular-guide.tsx
    - grep -q "1.414\|297\|210" src/components/scan/a4-paper-guide.tsx
    - test -f src/components/ui/progress.tsx
    - test -f src/components/ui/dialog.tsx
    - test -f src/components/ui/badge.tsx
    - test -f src/components/ui/toggle.tsx
    - test -f src/components/ui/skeleton.tsx
  </acceptance_criteria>
  <done>Camera utility with 1080p/30fps constraints, scan stepper with Korean labels and accessibility, fullscreen camera viewfinder with error handling, 72px recording button with pulse animation, guidance overlay, circular motion guide with progress, A4 paper guide with detection indicator. All shadcn components installed.</done>
</task>

<task type="auto">
  <name>Task 2: Walking guide, quality badge, rescan prompt, onboarding, and foot selector</name>
  <files>src/components/scan/walking-guide.tsx, src/components/scan/quality-badge.tsx, src/components/scan/rescan-prompt.tsx, src/components/scan/scan-onboarding.tsx, src/components/scan/foot-side-selector.tsx</files>
  <read_first>src/lib/scan/types.ts, src/lib/scan/store.ts, .planning/phases/02-foot-scanning/02-UI-SPEC.md, src/components/scan/scan-stepper.tsx</read_first>
  <action>
**Create `src/components/scan/walking-guide.tsx`** per D-05, D-17, UI-SPEC:
- 'use client' component
- Props: { stepCount: number, totalSteps: number (default 10) }
- Instruction for floor-level camera placement: illustration area + text "휴대폰을 바닥에 놓고 5-10걸음 걸어주세요" per UI-SPEC copywriting
- Step counter: "{stepCount}/10 걸음" in body text on semi-transparent bg per UI-SPEC
- Walking direction arrow indicator

**Create `src/components/scan/quality-badge.tsx`** per D-19, SCAN-06, UI-SPEC:
- Props: { score: number, label: 'good' | 'fair' | 'poor' }
- 36px height pill badge per UI-SPEC component dimensions
- Three color states per UI-SPEC: 'good' = emerald-600 fill + white text "우수", 'fair' = amber-600 fill + white text "보통", 'poor' = red-600 fill + white text "부족"
- role="status" aria-live="polite" per UI-SPEC accessibility

**Create `src/components/scan/rescan-prompt.tsx`** per D-19, SCAN-06, UI-SPEC:
- Props: { reasons: QualityWarning[] (from types.ts), onRescan: () => void }
- Warning banner with heading "다시 촬영이 필요합니다" per UI-SPEC copywriting
- List specific guidance messages from reasons array (Korean text per UI-SPEC: "조명을 밝게 해주세요", "더 천천히 돌아주세요", "흔들림이 감지되었습니다", etc.)
- "다시 촬영하기" CTA button (accent fill) per UI-SPEC
- Uses shadcn Alert component with destructive variant

**Create `src/components/scan/scan-onboarding.tsx`** per D-14, UI-SPEC:
- 'use client' component
- Props: { onComplete: () => void, onSkip: () => void }
- 3-step horizontal swipeable carousel per UI-SPEC interaction patterns
- Step 1: Lucide Footprints icon 80px + "A4 용지 위에 맨발을 올립니다"
- Step 2: Lucide Video icon 80px + "발 주위를 15-20초 돌며 촬영합니다"
- Step 3: Lucide PersonStanding icon 80px + "5-10걸음 걸어 보행을 분석합니다"
- "시작하기" CTA on last step per UI-SPEC copywriting
- "온보딩 건너뛰기" skip link on all steps (muted text, top-right) per UI-SPEC
- Dot indicators at bottom showing current position
- Calls markOnboarded() on store when complete or skip per UI-SPEC (localStorage flag)

**Create `src/components/scan/foot-side-selector.tsx`** per D-20, SCAN-05, UI-SPEC:
- 'use client' component
- Props: { scannedFoot: FootSide | null, onSelectFoot: (side: FootSide) => void }
- Two foot icons side by side (left/right)
- Scanned foot: filled icon with checkmark
- Remaining foot: outline icon, tappable
- Labels: "왼발" / "오른발" per UI-SPEC copywriting
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "걸음" src/components/scan/walking-guide.tsx
    - grep -q "우수" src/components/scan/quality-badge.tsx
    - grep -q "보통" src/components/scan/quality-badge.tsx
    - grep -q "부족" src/components/scan/quality-badge.tsx
    - grep -q "aria-live" src/components/scan/quality-badge.tsx
    - grep -q "다시 촬영이 필요합니다" src/components/scan/rescan-prompt.tsx
    - grep -q "다시 촬영하기" src/components/scan/rescan-prompt.tsx
    - grep -q "A4 용지 위에 맨발을 올립니다" src/components/scan/scan-onboarding.tsx
    - grep -q "시작하기" src/components/scan/scan-onboarding.tsx
    - grep -q "온보딩 건너뛰기" src/components/scan/scan-onboarding.tsx
    - grep -q "markOnboarded\|onComplete\|onSkip" src/components/scan/scan-onboarding.tsx
    - grep -q "왼발" src/components/scan/foot-side-selector.tsx
    - grep -q "오른발" src/components/scan/foot-side-selector.tsx
  </acceptance_criteria>
  <done>Walking guide with step counter, quality badge with 3 color states, rescan prompt with Korean guidance, 3-step onboarding carousel with localStorage persistence, left/right foot selector with visual indicators. All Korean copy matches UI-SPEC exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser camera API | Camera access requires user permission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | camera-viewfinder.tsx | mitigate | Camera stream stays local, never transmitted except through explicit upload action. Stop all tracks on unmount. |
| T-02-08 | Denial of Service | camera.ts | accept | getUserMedia constraints limit resolution to 1080p to prevent memory issues on low-end devices |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All shadcn components installed (progress, dialog, alert, badge, toggle, skeleton)
- All Korean copy matches UI-SPEC copywriting contract exactly
- Camera constraints limit to 1080p/30fps per RESEARCH Pitfall 6
</verification>

<success_criteria>
- 11 scan UI components created matching UI-SPEC specifications
- Camera utility with 1080p/30fps per Pitfall 6
- All Korean text matches UI-SPEC copywriting contract
- Accessibility attributes on all interactive elements per UI-SPEC
- Recording button is 72px with pulse animation per UI-SPEC
- Circular guide is 280px per UI-SPEC
- Quality badge has 3 color-coded states per UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/02-foot-scanning/02-03-SUMMARY.md`
</output>
