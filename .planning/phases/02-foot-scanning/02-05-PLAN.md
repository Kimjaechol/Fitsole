---
phase: 02-foot-scanning
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - src/app/(main)/scan/page.tsx
  - src/app/(main)/scan/new/page.tsx
  - src/app/(main)/scan/processing/[id]/page.tsx
  - src/lib/scan/upload.ts
  - src/components/scan/processing-animation.tsx
  - src/components/scan/scan-history.tsx
autonomous: true
requirements: [SCAN-01, SCAN-03, SCAN-06]
must_haves:
  truths:
    - "User can launch guided scan flow from /scan page via CTA button"
    - "First-time users see onboarding carousel before scan flow"
    - "Scan flow page shows 4-step stepper with camera viewfinder"
    - "Step 1 shows A4 paper guide, Step 2 shows circular guide + recording"
    - "Step 3 shows walking guide with 3-second countdown"
    - "Video uploads via tus-js-client with progress indicator"
    - "Processing page polls status every 5 seconds via TanStack Query"
    - "Scan history shows previous scan results"
  artifacts:
    - path: "src/app/(main)/scan/page.tsx"
      provides: "Scan home page with onboarding or history"
      contains: "ScanOnboarding"
    - path: "src/app/(main)/scan/new/page.tsx"
      provides: "Multi-step scan flow with camera"
      contains: "CameraViewfinder"
    - path: "src/app/(main)/scan/processing/[id]/page.tsx"
      provides: "Processing status polling page"
      contains: "useQuery"
    - path: "src/lib/scan/upload.ts"
      provides: "TUS video upload wrapper"
      contains: "tus"
  key_links:
    - from: "src/app/(main)/scan/new/page.tsx"
      to: "src/lib/scan/store.ts"
      via: "reads/updates scan state machine"
      pattern: "useScanStore"
    - from: "src/app/(main)/scan/new/page.tsx"
      to: "src/lib/scan/upload.ts"
      via: "uploads recorded video after recording stops"
      pattern: "uploadScanVideo"
    - from: "src/app/(main)/scan/processing/[id]/page.tsx"
      to: "src/app/api/scan/status/[id]/route.ts"
      via: "polls processing status every 5s"
      pattern: "api/scan/status"
---

<objective>
Wire scan components into page routes and build the scan flow orchestration: scan home page (onboarding vs history), multi-step scan flow page (camera + 4 steps), video upload via TUS, and processing status polling page. This connects all the components from Plan 03 into a working user flow.

Purpose: Create the actual scan user journey per D-14 through D-20. User can navigate from /scan -> /scan/new -> /scan/processing/[id].
Output: 3 scan pages + upload utility + processing animation + scan history
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-foot-scanning/02-CONTEXT.md
@.planning/phases/02-foot-scanning/02-UI-SPEC.md
@.planning/phases/02-foot-scanning/02-RESEARCH.md

@src/lib/scan/types.ts
@src/lib/scan/store.ts
@src/components/scan/scan-stepper.tsx
@src/components/scan/camera-viewfinder.tsx
@src/components/scan/recording-button.tsx
@src/components/scan/guidance-overlay.tsx
@src/components/scan/circular-guide.tsx
@src/components/scan/a4-paper-guide.tsx
@src/components/scan/walking-guide.tsx
@src/components/scan/quality-badge.tsx
@src/components/scan/rescan-prompt.tsx
@src/components/scan/scan-onboarding.tsx
@src/components/scan/foot-side-selector.tsx

<interfaces>
From src/lib/scan/types.ts (Plan 01):
- ScanStatus, FootSide, ScanSession, ProcessingStage, QualityWarning

From src/lib/scan/store.ts (Plan 01):
- useScanStore(): all state and actions

From src/app/api/scan/create/route.ts (Plan 01):
- POST /api/scan/create { footSide } -> { id, footSide, status }

From src/app/api/scan/status/[id]/route.ts (Plan 01):
- GET /api/scan/status/[id] -> { id, status, processingStage, qualityScore, qualityLabel, errorMessage }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan home page, upload utility, and processing components</name>
  <files>src/app/(main)/scan/page.tsx, src/lib/scan/upload.ts, src/components/scan/processing-animation.tsx, src/components/scan/scan-history.tsx</files>
  <read_first>src/app/(main)/scan/page.tsx, src/lib/scan/types.ts, src/lib/scan/store.ts, src/components/scan/scan-onboarding.tsx, src/components/empty-state.tsx, .planning/phases/02-foot-scanning/02-UI-SPEC.md, .planning/phases/02-foot-scanning/02-RESEARCH.md</read_first>
  <action>
**Replace `src/app/(main)/scan/page.tsx`** per D-14, SCAN-01:
- 'use client' component (needs store access)
- Check isOnboarded from useScanStore
- If NOT onboarded: show ScanOnboarding component
- If onboarded: show scan home with:
  - Heading "발 측정" (heading size 24px/700)
  - If previous scans exist (fetch from /api/scan/history or show ScanHistory component): show ScanHistory
  - If no scans: show EmptyState component with heading "아직 발 측정을 하지 않았어요", body "스마트폰 카메라로 간편하게 발을 측정해 보세요", CTA "발 측정 시작하기" per UI-SPEC copywriting
  - CTA links to /scan/new
  - "새로 측정하기" button in top-right for return users per UI-SPEC

**Create `src/lib/scan/upload.ts`** per RESEARCH Pattern 4:
- Function `uploadScanVideo(file: Blob, scanId: string, onProgress: (percent: number) => void, onComplete: (url: string) => void, onError: (error: Error) => void): { abort: () => void }`
- Use tus-js-client (import * as tus from 'tus-js-client')
- Endpoint: '/api/scan/upload' (proxied to Python backend)
- Chunk size: 5MB per RESEARCH Pattern 4
- Retry delays: [0, 1000, 3000, 5000] per RESEARCH
- Metadata: { filename: 'scan-video.webm', filetype: 'video/webm', scanId }
- Return object with abort() function for cancellation
- Handle onError with Korean message per UI-SPEC: "영상 업로드에 실패했습니다"

**Create `src/components/scan/processing-animation.tsx`** per D-18, UI-SPEC:
- 'use client' component
- Props: { stage: ProcessingStage, progress: number }
- Centered layout per UI-SPEC interaction patterns
- Animated foot outline SVG (simple foot shape that draws itself progressively)
- 4 processing stages displayed as vertical list per UI-SPEC:
  1. "영상 분석 중..." (analyzing_video)
  2. "3D 모델 생성 중..." (generating_model)
  3. "측정값 계산 중..." (calculating_measurements)
  4. "보행 분석 중..." (analyzing_gait)
- Current stage highlighted with accent color, completed stages with emerald checkmark
- shadcn Progress bar (8px height, accent fill) below animation
- Percentage text below (body 16px)
- "약 3-5분 소요됩니다" muted text per UI-SPEC
- aria-live="polite" on stage text per UI-SPEC accessibility

**Create `src/components/scan/scan-history.tsx`** per UI-SPEC:
- Props: { scans: ScanResult[] }
- Heading "이전 측정 기록" per UI-SPEC
- List of scan cards showing: date (formatted in Korean), quality badge, foot side (왼발/오른발)
- Each card links to /scan/results/[id]
- Empty state handled by parent (scan page shows EmptyState)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ScanOnboarding\|scan-onboarding" src/app/\(main\)/scan/page.tsx
    - grep -q "isOnboarded" src/app/\(main\)/scan/page.tsx
    - grep -q "발 측정 시작하기" src/app/\(main\)/scan/page.tsx
    - grep -q "아직 발 측정을 하지 않았어요" src/app/\(main\)/scan/page.tsx
    - grep -q "tus" src/lib/scan/upload.ts
    - grep -q "5.*1024.*1024\|5242880\|chunkSize" src/lib/scan/upload.ts
    - grep -q "retryDelays" src/lib/scan/upload.ts
    - grep -q "영상 분석 중" src/components/scan/processing-animation.tsx
    - grep -q "3D 모델 생성 중" src/components/scan/processing-animation.tsx
    - grep -q "약 3-5분 소요됩니다" src/components/scan/processing-animation.tsx
    - grep -q "aria-live" src/components/scan/processing-animation.tsx
    - grep -q "이전 측정 기록" src/components/scan/scan-history.tsx
  </acceptance_criteria>
  <done>Scan home page with onboarding/history switching, TUS upload utility with 5MB chunks and retry, processing animation with 4 stage indicators, scan history list component. All Korean text per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-step scan flow page and processing status page</name>
  <files>src/app/(main)/scan/new/page.tsx, src/app/(main)/scan/processing/[id]/page.tsx</files>
  <read_first>src/lib/scan/types.ts, src/lib/scan/store.ts, src/lib/scan/camera.ts, src/lib/scan/upload.ts, src/components/scan/scan-stepper.tsx, src/components/scan/camera-viewfinder.tsx, src/components/scan/recording-button.tsx, src/components/scan/circular-guide.tsx, src/components/scan/a4-paper-guide.tsx, src/components/scan/walking-guide.tsx, src/components/scan/guidance-overlay.tsx, src/components/scan/foot-side-selector.tsx, src/components/scan/processing-animation.tsx, .planning/phases/02-foot-scanning/02-UI-SPEC.md</read_first>
  <action>
**Create `src/app/(main)/scan/new/page.tsx`** per D-14 to D-20, SCAN-01, SCAN-03:
- 'use client' component (camera access, state management)
- Full scan flow orchestration using Zustand store state machine
- Layout: fullscreen during camera steps, no bottom tab bar per UI-SPEC layout
- ScanStepper at top showing current step

**Step 1 (positioning) per D-15:**
- CameraViewfinder with A4PaperGuide overlay
- GuidanceOverlay: "A4 용지 위에 발을 올려주세요" per UI-SPEC
- Subtitle: "밝은 곳에서 A4 용지 위에 맨발을 올려주세요"
- Button to proceed when ready: "다음 단계로"

**Step 2 (recording_foot) per D-16:**
- CameraViewfinder with CircularGuide overlay
- RecordingButton centered at bottom
- 3-second countdown overlay before recording starts per D-16, UI-SPEC (large centered numbers, Display 32px)
- GuidanceOverlay: "발 주위를 천천히 돌아주세요" + "15-20초 동안 발 주위를 원을 그리며 촬영해 주세요"
- Recording timer display (elapsed seconds)
- Real-time frame quality check via checkFrameQuality from camera.ts -- show amber warnings for dark frames
- Auto-stop after 20 seconds or manual stop via RecordingButton
- After recording: brief preview (2s auto-play loop per UI-SPEC), then proceed
- On stop: trigger video upload via uploadScanVideo from upload.ts
- Use MediaRecorder API to capture video from camera stream

**Step 3 (recording_gait) per D-05, D-17:**
- WalkingGuide instructions first (phone placement)
- 3-second countdown (large centered "3초 후 촬영이 시작됩니다" per UI-SPEC)
- CameraViewfinder with step counter overlay
- RecordingButton for walking video
- Auto-stop after 30 seconds or manual stop
- After recording: upload walking video

**Step 4 (Left/Right handling) per D-20:**
- After first foot scan, show FootSideSelector
- If second foot not scanned, loop back to Step 1 for other foot
- If both feet scanned (or user chooses to skip second), navigate to /scan/processing/[id]

**Navigation:**
- Back arrow exits with confirmation dialog (shadcn Dialog): "스캔을 중단하시겠습니까?" with "중단" and "계속하기" buttons
- Page transitions: slide-left per UI-SPEC (CSS transform or Framer Motion, 200ms ease-out)

**Error handling:**
- Camera permission denied: shadcn Dialog with Korean text per UI-SPEC error states
- Upload failure: toast (destructive variant) with retry per UI-SPEC

**Create `src/app/(main)/scan/processing/[id]/page.tsx`** per D-18, SCAN-06:
- 'use client' component
- Extract scanId from URL params
- Use TanStack Query (useQuery) to poll GET /api/scan/status/[id] every 5 seconds per UI-SPEC state management + RESEARCH Pitfall 7
- Display ProcessingAnimation component with current stage and progress
- When status becomes 'completed': redirect to /scan/results/[id]
- When status becomes 'failed': show error state with "3D 모델 생성에 실패했습니다. 다시 촬영해 주세요" and CTA linking back to /scan/new per UI-SPEC
- User can navigate away -- processing continues server-side per UI-SPEC (toast notification when complete)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" src/app/\(main\)/scan/new/page.tsx
    - grep -q "CameraViewfinder\|camera-viewfinder" src/app/\(main\)/scan/new/page.tsx
    - grep -q "RecordingButton\|recording-button" src/app/\(main\)/scan/new/page.tsx
    - grep -q "ScanStepper\|scan-stepper" src/app/\(main\)/scan/new/page.tsx
    - grep -q "CircularGuide\|circular-guide" src/app/\(main\)/scan/new/page.tsx
    - grep -q "A4PaperGuide\|a4-paper-guide" src/app/\(main\)/scan/new/page.tsx
    - grep -q "WalkingGuide\|walking-guide" src/app/\(main\)/scan/new/page.tsx
    - grep -q "FootSideSelector\|foot-side-selector" src/app/\(main\)/scan/new/page.tsx
    - grep -q "MediaRecorder" src/app/\(main\)/scan/new/page.tsx
    - grep -q "uploadScanVideo\|upload" src/app/\(main\)/scan/new/page.tsx
    - grep -q "A4 용지 위에 발을 올려주세요\|A4" src/app/\(main\)/scan/new/page.tsx
    - grep -q "useQuery\|polling\|refetchInterval" src/app/\(main\)/scan/processing/\[id\]/page.tsx
    - grep -q "ProcessingAnimation\|processing-animation" src/app/\(main\)/scan/processing/\[id\]/page.tsx
    - grep -q "api/scan/status" src/app/\(main\)/scan/processing/\[id\]/page.tsx
    - grep -q "completed" src/app/\(main\)/scan/processing/\[id\]/page.tsx
  </acceptance_criteria>
  <done>Complete 4-step scan flow page with camera viewfinder, A4 guide (step 1), circular guide + recording (step 2), walking video (step 3), left/right foot handling (step 4). Processing page with TanStack Query 5s polling and stage animation. All Korean text per UI-SPEC. Error handling for camera permission and upload failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser camera -> upload | Recorded video transmitted to server |
| Client -> status API | Scan ID from URL used to query status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information Disclosure | Camera stream | mitigate | Camera stream only transmitted via explicit upload action. Stream stopped on unmount. |
| T-02-12 | Tampering | processing/[id] | mitigate | Status endpoint validates userId ownership (from Plan 01 route) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- /scan page shows onboarding for first-time users
- /scan/new page has all 4 steps with correct components
- /scan/processing/[id] polls every 5 seconds
- All Korean copy matches UI-SPEC
</verification>

<success_criteria>
- Scan home shows onboarding (first visit) or history (return visit) per D-14
- 4-step scan flow with A4 guide, circular guide, walking guide per D-15/D-16/D-17
- 3-second countdown before recording per D-05, D-16
- Video recording via MediaRecorder and upload via TUS per D-01
- Left/right foot scanned independently per D-20
- Processing page with 5s polling and 4 stage indicators per D-18
- Error handling for camera, upload, and processing failures per UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/02-foot-scanning/02-05-SUMMARY.md`
</output>
