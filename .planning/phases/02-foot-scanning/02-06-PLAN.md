---
phase: 02-foot-scanning
plan: 06
type: execute
wave: 3
depends_on: [02-01]
files_modified:
  - src/components/scan/foot-model-3d.tsx
  - src/components/scan/measurement-card.tsx
  - src/components/scan/measurement-overlay.tsx
  - src/components/scan/pressure-heatmap.tsx
  - src/components/scan/heatmap-legend.tsx
  - src/app/(main)/scan/results/[id]/page.tsx
autonomous: true
requirements: [SCAN-07, SCAN-10]
must_haves:
  truths:
    - "3D foot model loads and renders interactively with OrbitControls (rotate/zoom)"
    - "Measurement values displayed alongside 3D model with all 6 static measurements"
    - "Pressure heatmap overlays on 3D foot model with color-coded zones"
    - "Heatmap legend shows pressure scale from low (blue) to high (red)"
    - "Results page shows both feet with gait analysis results"
    - "CTA button at bottom invites user to get shoe recommendations"
  artifacts:
    - path: "src/components/scan/foot-model-3d.tsx"
      provides: "React Three Fiber 3D foot model viewer"
      contains: "Canvas"
    - path: "src/components/scan/measurement-card.tsx"
      provides: "Individual measurement display card"
      contains: "mm"
    - path: "src/components/scan/pressure-heatmap.tsx"
      provides: "Pressure heatmap visualization on 3D model"
      contains: "heatmap"
    - path: "src/app/(main)/scan/results/[id]/page.tsx"
      provides: "Full results page with 3D viewer + measurements + gait + pressure"
      contains: "FootModel3D"
  key_links:
    - from: "src/app/(main)/scan/results/[id]/page.tsx"
      to: "src/app/api/scan/results/[id]/route.ts"
      via: "fetches full scan results"
      pattern: "api/scan/results"
    - from: "src/components/scan/foot-model-3d.tsx"
      to: "three"
      via: "React Three Fiber Canvas with OrbitControls"
      pattern: "Canvas|OrbitControls"
---

<objective>
Build the 3D foot model viewer (React Three Fiber), measurement display components, pressure heatmap visualization, and the results page that combines everything. This is the payoff screen where users see their scan results per D-21 through D-24.

Purpose: Deliver the visual output of the entire scanning pipeline -- interactive 3D model, measurement overlay, pressure heatmap, and gait analysis results.
Output: 5 visualization components + results page
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-foot-scanning/02-CONTEXT.md
@.planning/phases/02-foot-scanning/02-UI-SPEC.md
@.planning/phases/02-foot-scanning/02-RESEARCH.md

@src/lib/scan/types.ts
@src/lib/scan/store.ts

<interfaces>
From src/lib/scan/types.ts (Plan 01):
- ScanResult: { id, footSide, status, qualityScore, qualityLabel, measurements, gaitAnalysis, pressureData, modelUrl, createdAt, completedAt }
- FootMeasurement: { footLength, ballWidth, instepHeight, archHeight, heelWidth, toeLength }
- GaitAnalysisResult: { gaitPattern, ankleAlignment, archFlexibilityIndex }
- PressureData: { heatmapData (number[][]), highPressureZones }

From src/app/api/scan/results/[id]/route.ts (Plan 01):
- GET /api/scan/results/[id] -> full ScanResult with joined measurements, gait, pressure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3D foot model viewer and measurement visualization components</name>
  <files>src/components/scan/foot-model-3d.tsx, src/components/scan/measurement-card.tsx, src/components/scan/measurement-overlay.tsx, src/components/scan/pressure-heatmap.tsx, src/components/scan/heatmap-legend.tsx</files>
  <read_first>src/lib/scan/types.ts, .planning/phases/02-foot-scanning/02-UI-SPEC.md, .planning/phases/02-foot-scanning/02-RESEARCH.md</read_first>
  <action>
**First, install R3F packages** (if not already installed):
```bash
npm install @react-three/fiber @react-three/drei three @types/three
```

**Create `src/components/scan/foot-model-3d.tsx`** per D-21, SCAN-07, RESEARCH code example:
- 'use client' component
- Props: { modelUrl: string, showHeatmap?: boolean, heatmapData?: number[][], children?: ReactNode }
- Wrapper div: h-[360px] w-full rounded-lg bg-slate-50 md:h-[480px] per UI-SPEC component dimensions
- React Three Fiber Canvas with camera position [0, 0.3, 0.4], fov 45 per RESEARCH example
- Suspense fallback: shadcn Skeleton at same height (loading state per UI-SPEC)
- Environment preset="studio" from drei
- Load model via useGLTF(modelUrl) from drei
- OrbitControls: enablePan=false, minDistance=0.2, maxDistance=1.0 per RESEARCH example
- Initial view: top-down 45-degree angle per UI-SPEC
- aria-label="3D 발 모델 - 드래그하여 회전, 핀치하여 확대" per UI-SPEC accessibility
- Children slot for rendering measurement lines overlay

**Create `src/components/scan/measurement-card.tsx`** per D-22, UI-SPEC:
- Props: { label: string, value: number, unit: string (default 'mm'), confidence?: 'high' | 'medium' | 'low' }
- Card component per UI-SPEC: label (14px, text-slate-500) + value (24px, font-bold) + unit (14px, text-slate-500)
- 48px height per UI-SPEC component dimensions (measurement value row)
- Optional confidence badge (shadcn Badge, emerald for high, amber for medium, red for low)

**Create `src/components/scan/measurement-overlay.tsx`** per D-22, UI-SPEC:
- 'use client' component (renders inside R3F Canvas)
- Props: { measurements: FootMeasurement }
- Three.js Line segments for dimension arrows on the 3D model:
  - Foot length: vertical line along Z axis
  - Ball width: horizontal line at widest point
  - Arch height: vertical line at arch
- Text labels (using drei Html or Text component) showing values in mm
- Uses Three.js LineBasicMaterial with accent color

**Create `src/components/scan/pressure-heatmap.tsx`** per D-10, D-23, SCAN-10, UI-SPEC:
- 'use client' component (renders inside R3F Canvas or as overlay)
- Props: { data: number[][], highPressureZones: {x: number, y: number, intensity: number, label: string}[] }
- Color-coded pressure zones on plantar (sole) view per UI-SPEC heatmap colors:
  - Low: blue (#3B82F6), Medium-low: cyan (#06B6D4), Medium: green (#22C55E)
  - Medium-high: yellow (#EAB308), High: orange (#F97316), Very high: red (#EF4444)
- Render as a textured plane in Three.js mapped to foot sole
- Create texture from heatmapData grid using canvas 2D rendering
- Apply gradient interpolation between color anchor points
- Text labels accompany color zones per UI-SPEC accessibility (not color-only)

**Create `src/components/scan/heatmap-legend.tsx`** per UI-SPEC:
- Props: none (static legend)
- Vertical gradient strip, 32px width per UI-SPEC component dimensions
- Labels: "낮음" (low, blue) at bottom, "높음" (high, red) at top per UI-SPEC
- Gradient from #3B82F6 through #06B6D4 -> #22C55E -> #EAB308 -> #F97316 -> #EF4444
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Canvas" src/components/scan/foot-model-3d.tsx
    - grep -q "OrbitControls" src/components/scan/foot-model-3d.tsx
    - grep -q "useGLTF" src/components/scan/foot-model-3d.tsx
    - grep -q "360" src/components/scan/foot-model-3d.tsx
    - grep -q "480" src/components/scan/foot-model-3d.tsx
    - grep -q "3D 발 모델" src/components/scan/foot-model-3d.tsx
    - grep -q "Skeleton" src/components/scan/foot-model-3d.tsx
    - grep -q "mm" src/components/scan/measurement-card.tsx
    - grep -q "font-bold\|700" src/components/scan/measurement-card.tsx
    - grep -q "Line\|line" src/components/scan/measurement-overlay.tsx
    - grep -q "#3B82F6\|3B82F6" src/components/scan/pressure-heatmap.tsx
    - grep -q "#EF4444\|EF4444" src/components/scan/pressure-heatmap.tsx
    - grep -q "32" src/components/scan/heatmap-legend.tsx
    - grep -q "낮음" src/components/scan/heatmap-legend.tsx
    - grep -q "높음" src/components/scan/heatmap-legend.tsx
  </acceptance_criteria>
  <done>3D foot model viewer with OrbitControls and skeleton loading, measurement cards with label/value/unit, measurement dimension lines overlay on 3D model, pressure heatmap with 6-color gradient per UI-SPEC, heatmap legend with Korean labels. All dimensions per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Scan results page with full measurement display</name>
  <files>src/app/(main)/scan/results/[id]/page.tsx</files>
  <read_first>src/lib/scan/types.ts, src/components/scan/foot-model-3d.tsx, src/components/scan/measurement-card.tsx, src/components/scan/measurement-overlay.tsx, src/components/scan/pressure-heatmap.tsx, src/components/scan/heatmap-legend.tsx, src/components/scan/quality-badge.tsx, .planning/phases/02-foot-scanning/02-UI-SPEC.md</read_first>
  <action>
**Create `src/app/(main)/scan/results/[id]/page.tsx`** per D-21 to D-24, SCAN-07:
- 'use client' component
- Fetch scan results from GET /api/scan/results/[id] using TanStack Query (or fetch on mount)
- Loading state: Skeleton placeholders per UI-SPEC

**Layout per UI-SPEC:**
- Standard layout (with bottom tab bar, max-width 1280px)
- Sticky header: "측정 결과" heading (Display 32px/700) + left/right foot toggle (tabs or buttons showing "왼발" / "오른발")
- Mobile: single column layout per UI-SPEC results layout
- Desktop: two columns (3D model left 60%, measurements/heatmap right 40%) per UI-SPEC

**3D Model Section:**
- FootModel3D component with modelUrl from scan results
- MeasurementOverlay rendered inside (dimension lines on model)
- shadcn Toggle for heatmap: "압력 분포 보기" per UI-SPEC
- When toggled on: PressureHeatmap overlay + HeatmapLegend

**Measurements Section:**
- Grid of MeasurementCard components (2 columns on mobile, 3 on desktop per UI-SPEC)
- 6 static measurements with Korean labels per UI-SPEC copywriting:
  - 발 길이: footLength mm
  - 볼 넓이: ballWidth mm
  - 아치 높이: archHeight mm
  - 발등 높이: instepHeight mm
  - 뒤꿈치 넓이: heelWidth mm
  - 발가락 길이: toeLength mm
- QualityBadge showing scan quality score per D-19

**Gait Analysis Section per D-07, D-12:**
- Section heading "보행 분석"
- Display gait pattern classification (보행 패턴): normal/overpronation/supination with Korean translations
- Display ankle alignment (발목 정렬): neutral/pronation/supination
- Display arch flexibility (아치 유연성): index value with descriptive text

**Asymmetry Notice per D-20, SCAN-05:**
- If both feet scanned and asymmetry detected: shadcn Alert component with amber variant
- Text: "좌우 비대칭이 감지되었습니다" per UI-SPEC

**Bottom CTA per D-24:**
- Full-width accent button, fixed to bottom on mobile (above bottom tab bar)
- Text: "이 데이터로 맞춤 신발을 추천받으시겠습니까?" per UI-SPEC copywriting
- Links to /catalog (future Phase 3)
- "결과 저장하기" secondary button per UI-SPEC

**Page entry animation:** fade-in 300ms ease-out per UI-SPEC
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "측정 결과" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "FootModel3D\|foot-model-3d" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "MeasurementCard\|measurement-card" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "PressureHeatmap\|pressure-heatmap\|압력 분포 보기" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "발 길이" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "볼 넓이" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "아치 높이" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "발등 높이" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "뒤꿈치 넓이" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "보행 패턴\|보행 분석" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "발목 정렬" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "아치 유연성" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "좌우 비대칭이 감지되었습니다" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "이 데이터로 맞춤 신발을 추천받으시겠습니까" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "결과 저장하기" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "왼발" src/app/\(main\)/scan/results/\[id\]/page.tsx
    - grep -q "오른발" src/app/\(main\)/scan/results/\[id\]/page.tsx
  </acceptance_criteria>
  <done>Complete results page with interactive 3D foot model, 6 measurement cards with Korean labels, toggleable pressure heatmap with legend, gait analysis section (pattern/alignment/flexibility), asymmetry detection notice, left/right foot tabs, and bottom CTA per D-24. Layout responsive (single column mobile, two column desktop).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> results API | Scan ID from URL used to fetch results |
| External GLB model | 3D model loaded from storage URL into browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Information Disclosure | results/[id] | mitigate | Results API validates userId ownership (Plan 01 route). Only model owner sees results. |
| T-02-14 | Tampering | GLB model loading | accept | Models stored in controlled S3/R2 bucket, URLs not user-modifiable. Low risk. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- 3D viewer renders with OrbitControls
- All 6 measurement labels match UI-SPEC Korean text
- Heatmap colors match UI-SPEC exactly (6 anchor colors)
- Results page has both mobile and desktop layouts
</verification>

<success_criteria>
- Interactive 3D foot model with rotate/zoom per D-21
- 6 measurement values displayed with Korean labels per D-22
- Pressure heatmap toggleable with color-coded zones per D-23
- CTA "이 데이터로 맞춤 신발을 추천받으시겠습니까?" per D-24
- Left/right foot tabs per D-20
- Asymmetry notice per SCAN-05
- Gait analysis display (pattern/alignment/flexibility) per D-07, D-12
- Responsive layout (mobile single column, desktop two column) per UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/02-foot-scanning/02-06-SUMMARY.md`
</output>
